---
aliases:
  - The Shittim Chest 試験設計
tags: [project, shittim-chest, test, quality, detailed-design]
status: decided
created: 2026-07-16
updated: 2026-07-28
---

# 試験・品質保証設計

## 1. Test policy

- domain/applicationは外部SDKなしでdeterministicに試験し、line/branch coverageを90%以上にする。
- 別AWS test accountは作らない。外部serviceを使う確認は本番deploy後の許可Guild内専用smoke channelとOpenAI production projectで、固定した無害な入力だけを使用する。
- 本番token、実user content、本番DynamoDB tableへのwrite権限をCIへ持ち込まない。PRはfake、stub、DynamoDB Local、CDK assertionまでとする。
- retry、時刻、ID、random、外部応答はProtocol経由で制御し、flaky testを再試行で隠さない。
- Scale-to-ZeroのPR受入はlocal/mock、DynamoDB Local、CDK assertion/nag/credentialなしsynthまでとする。AWS bootstrap/deploy/resource write、Discord Application/Interactions Endpoint変更、実Discord interactionは行わず、これらの結果を成功として記録しない。

## 2. Test layers

| Layer | Scope | Environment | Gate |
|---|---|---|---|
| Unit | domain、state machine、validation、chunk、tie rule | pytest/Hypothesis | PR必須 |
| Application | use case、Protocol fake、cancel/retry/recovery | pytest-asyncio | PR必須 |
| Contract | OpenAI schema、Discord payload、DynamoDB serializer | fixture/respx/stub | PR必須 |
| Persistence | transaction、condition、GSI、outbox、migration | DynamoDB Local | PR必須 |
| Container | non-root、health、SIGTERM、ARM64 image | container runtime | PR必須 |
| Infrastructure | CDK assertion、snapshot、nag | synth/local | PR/変更時 |
| Integration | Discord/OpenAI/AWS SDK compatibility | production限定smoke | deploy後必須 |
| E2E | commandから最終決定・永続化 | production smoke scope | 初回受入・major変更 |
| Fault | SIGTERM/SIGKILL、429、timeout、部分失敗 | local/container | release必須 |
| Security | secret、IAM、authorization、prompt injection | CI/opt-in | release必須 |

## 3. CI quality gate

1. `uv lock --check`、Ruff format/check、`ty check`、import-linter。tyは`src`、`tests`、`tools`を全検査する唯一のtype-check gateであり、STEP-03以降はdomain/application依存方向をPR必須gateとする。
2. pytest unit/application/contract/persistence、coverage 90%以上。
3. pip-audit、署名/digest固定Betterleaksのfull-history scan、生成fixture contract、public surface scan、Dependency Review、lock由来CycloneDX SBOMのstrict schema・project・全package inventory検証。
4. sdist/wheel build、exactly-one wheelのisolated install/import smoke。native ARM64 production/fault image build、security/health、fault recovery、image SPDXを`container-arm64`で検証する。
5. Markdown/frontmatter/fence/Wiki link/heading/公式資料表、license scope、workflow syntaxをCIで検証する。Obsidian正本とのbyte一致はlocal pre-PRで検証し、public runnerへVault accessを与えない。
6. TypeScript test/typecheck、CDK assertions、`cdk synth --strict`、cdk-nagはCDK実装後に追加する。
7. GitHub managed security設定、required check、fork permissionをrepository APIとnegative testで検証する。

### 3.1 Domain unit test definitions

| ID | Procedure | Pass criteria |
|---|---|---|
| `UT-DOM-001` | 10 phaseの直積100組を実行し、通常、cancel、failの許可edgeと拒否edgeを照合する | 許可は設計どおり21組だけで、phase飛越し、逆行、自己遷移、terminal後遷移を全て拒否する |
| `UT-DOM-002` | 全非terminal phaseでcheckpoint/resumeを行い、terminal、二重checkpoint、checkpointなしresume、checkpoint中phase遷移も試す | 合法操作は同じphaseへ復旧し、不正操作は安定error codeで副作用なく失敗する |
| `UT-DOM-003` | UUIDv7生成・parse、非UUIDv7、UTC/naive/非UTC/逆行時刻、schema version境界を試す | UUIDv7、aware UTC、正の非boolean schema versionだけを受理する |
| `UT-DOM-004` | frozen/slotsと全合法edgeのproperty testを実行する | 元stateを変更せず、`debate_id`と`schema_version`を全遷移で維持し、line/branch coverage 90%以上となる |
| `UT-DOM-005` | 全非terminal phaseからFAILEDへ遷移後、新しい`AttemptId`でretryし、連続retry、source ID再利用、全非FAILED source、時刻境界も試す | 元FAILED attemptを変更せず、同じ`DebateId`、直前attemptへの`retry_of`、`failed_from_phase`からの再開を満たし、不正retryを安定error codeで拒否する。過去attemptを含むdebate内一意性はAT-015の条件付きPutで検証する |

### 3.2 Application unit test definitions

| ID | Procedure | Pass criteria |
|---|---|---|
| `UT-APP-001` | fake Discord/Clock/ID/Repository/Evidence/OpenAIでacceptから決定まで実行する | 8 phaseが順序どおり条件付き保存され、Evidenceは1回、3人格処理は各1回、Python選出winnerをDecisionが維持する |
| `UT-APP-002` | 自己投票、不正score、重複・不足票、1対1対1、完全同点、ballot順序を試す | 不正票を安定codeで拒否し、得点、正確性、安全性、実用性、`participant-b/a/c`順で一意に決定する |
| `UT-APP-003` | runtime非READY、allowlist拒否、空/1001文字、非開始userのcancel/retryを試す | 永続化前または副作用前にfail closedとなり、開始userまたは`Manage Messages`保持者だけを許可する |
| `UT-APP-004` | phase/session timeout、TaskGroup子失敗、外部task cancellationを注入する | timeout codeを区別し、兄弟taskを残さず、process cancellationはcheckpoint後に`CancelledError`を再送出する |
| `UT-APP-005` | FAILED attemptをretryし、checkpointed attemptと保存済みartifactをresumeする | source FAILEDは不変、新AttemptId/`retry_of`を持ち、完了済みEvidence/opinion/proposal/voteを再生成しない |
| `UT-APP-006` | candidate order fakeを投票者ごとに変更・破損させる | 自己候補を除いた2候補だけを各投票へ渡し、欠落・重複・未知candidate集合を拒否する |
| `UT-APP-007` | ACCEPTED debateへstarter/thread/control-panel IDをbindingし、同値再送、別値rebind、開始後bindingを試す | 3 IDを別fieldへ一括保存し、同値再送だけ冪等、部分状態・rebind・開始後bindingを副作用なしで拒否する |
| `UT-APP-008` | pending outboxを持つrecoverable attempt、長いretry待機、nonretryable error、fencing競合、terminal transaction action別競合をfakeで実行する | phase前にdrainし、待機中もleaseをrenewしてactive-processing deadlineへ算入しない。attempt CASだけをbounded retryし、outbox/counter/fencing条件失敗はaction/codeをcontent-free logへ残してhot retryしない |

### 3.3 Discord contract test definitions

| ID | Procedure | Pass criteria |
|---|---|---|
| `CT-DISCORD-001` | Guild、非空channel allowlist、4 generic slot、Application IDの正常・欠落・重複・不正snowflakeを入力する | 実値をlogへ出さず、正確に4 slotかつdistinct IDだけを受理し、Guild/channel判定がfail closedとなる |
| `CT-DISCORD-002` | 1〜2,001文字、長い段落・行・単語をmessage splitterへ渡す | 2,000文字以下、段落・行・単語・hard splitの優先順、複数chunkの`[n/m]`、同一入力のbyte一致を満たす |
| `CT-DISCORD-003` | UUIDv7 nonce、UTF-8 content hash、v1 panel custom IDを生成・parseし、非UUIDv7、不正operation ID、別namespace/versionを渡す | nonceは22文字かつ25文字以下、SHA-256は64桁、custom IDは100文字以下で可逆、不正入力はfail closedとなる |
| `CT-DISCORD-004` | logical postを複数chunkのprepared outboxへ変換し、nonce数不足、slot/thread不正、直前schema outboxを読込む | chunk順、slot、hash、nonce、operation IDが決定的で、adapter間依存なし。現行schema直前versionをup-convertし、より古いversionはfail closed |
| `CT-DISCORD-005` | discord.py 2.7.1のmessage parameter builderとpublisher send引数をnetworkなしで検査する | `AllowedMentions.none()`が`parse=[]`、22文字nonce指定が`enforce_nonce=true`となり、publisherは1回だけSDK sendを呼ぶ |
| `CT-DISCORD-006` | 未永続operation、claim不能、正常claim/send/mark、既にSENTをpublisherへ渡す | 永続・fenced operationだけを送信し、claim不能は副作用なし、成功時は返却message IDを条件付き保存する |
| `CT-DISCORD-007` | 送信成功後のmark失敗、claim expiry、同一/不一致nonce-contentのthread履歴を注入する | 同一Bot author/nonce/content hashの既存messageを採用して追加send 0件、不一致は`DISCORD_OUTBOX_CONFLICT`でfail closedとなる |
| `CT-DISCORD-008` | `max_ratelimit_timeout`不一致、`RateLimited`、SDK枯渇後の429、408/409/5xx、Discord処理timeout、403、locked/missing/wrong-Guild thread、cancelを注入する | 4 clientを30秒上限へ固定し、Retry-Afterまたは既定delayでoutboxを1回だけ再scheduleする。45秒delivery timeoutが60秒claimより短く、permission/4xx/lockedは自動retry・unlockせず、cancelを再送出する |
| `CT-DISCORD-009` | 4 clientを構築し、Intent、Application ID、mentions、rate-limit上限、READY、Guild/channel、token欠落・重複、1 client終了をfakeで試す | GUILDSだけを有効にし、4 READY時だけRuntime claim/drainが可能で、HTTP受付はREADYと分離。token不正は接続前に拒否し、1 client終了時は全start taskを回収して全clientをcloseする |
| `CT-DISCORD-010` | Guild command schema/hash、同値/差分sync、Gatewayへの予期外command配信、Ingress Runtimeの通常channel/thread初回作成、binding済みreplay、履歴recovery、作成失敗、429をoffline Discord mockで試す | questionは1〜1000文字、syncはhash差分時だけ。HTTPで永続済みのRequestだけがstarter/Public Thread/panelを一度作成・bindingし、`RateLimited.retry_after`／`Retry-After`をdurable retryへ渡して指定時刻前の再試行を行わない。STARTING応答は受付済み・準備中と静的な待ち時間目安を表示する。rate-limit evidenceはoperation、Scope、Limit、Remaining、Reset-Afterとbucket SHA-256を区別し、raw bucket、URL、ID、token、質問、response bodyを含めない |
| `CT-DISCORD-014` | interaction.user.name/display_name、Guild nickありMember、username≠display nameをoffline mockで試す | AcceptDebateRequestへそのまま入り、`str(user)`や再fetch結果を使わない |
| `CT-DISCORD-011` | Cancel/Retry componentへ別Application、Guild、thread、message、attempt、actorを入力し、二重clickを試す | 永続contextとsource attemptが全一致し、開始userまたはManage Messages保持者の場合だけuse caseを呼ぶ。古いpanelを拒否し、retry後panelはnew attempt IDを持つ |
| `CT-DISCORD-012` | debate taskの同一ID二重開始、Cancel、controller close、1 client異常終了を注入する | 同一debate taskは1件だけで、cancel/close時に未追跡taskを残さず、`CancelledError`を握り潰さない |
| `CT-DISCORD-013` | future retry、未失効claim、claim race、retryable/nonretryable failure、複数chunk、cancelをoutbox recoveryへ注入する | 永続利用可能時刻までbusy loopなしで待ち、chunk順にdrainする。retryableは保存済みscheduleを使い、nonretryableはstable code、cancelは再送出する |
| `UT-RUNTIME-001` | process gateをclosed/READY/not-READYで切替え、Guild/channel判定を通す | 起動前・shutdown中はfail closed、open中も物理4 READYとallowlistを迂回しない |
| `UT-RUNTIME-002` | 4 READY起動、schema hash差分、recoverable record、SIGINT/SIGTERM相当callbackをfakeで実行する | command syncは初回1回、startup resumeは所有task、signal再入は冪等で全task/clientをawaitして終了する |
| `UT-RUNTIME-003` | 1 Bot切断を60秒未満/以上、再接続に分けて時間短縮fakeで実行する | Runtime claim/drainは即時停止しHTTP受付は継続。猶予内は進行を維持し、超過時CHECKPOINTED、再接続時に1 recovery taskでresumeし、通信断だけでFAILEDにしない |
| `UT-RUNTIME-004` | supervisor/recovery/checkpoint異常、shutdown 90秒超過を注入する | Runtime claim/drain gateを閉じて例外をprocess境界へ伝播し、checkpoint失敗後もclient cleanupを継続する。HTTP受付は独立して継続し、超過は`RuntimeShutdownTimeout`となりFargate 120秒より前に終了方針へ移る |
| `UT-RUNTIME-005` | runtime/persona/process設定の正常・欠落・未知field・version不一致・重複ID/tokenを検査し、production compositionをfake SDK clientで構築・二重closeする | SDK client作成前にfail closedとなりprivate値を例外/repr/logへ含めない。正常時はDynamoDB/OpenAI/limiterがprocess単位で1つ、Discordが4つ、PolicyがLuna standard固定で、cleanupは冪等である |
| `IT-RUNTIME-001` | 実Unix child processへSIGTERMまたはSIGKILLを送り、同じ永続stateを読むreplacement processを起動する | SIGTERMはRuntime admission停止、checkpoint、interaction/client cleanup後に終了する。SIGKILLはcleanupなしのactive stateを残し、replacementが検出・resumeして次のSIGTERMでcheckpointする。HTTP受付はRuntime processと独立し、標準出力へprivate値を含めない |

### 3.3.1 HTTP Interaction ingress contract

| ID | Procedure | Pass criteria |
|---|---|---|
| `CT-HTTP-001` | 正しいraw body、timestamp、Ed25519署名を持つDiscord PINGをHTTP handlerへ渡す | 外部SDKやDynamoDB副作用なしで規定のPONGを即時返す |
| `CT-HTTP-002` | 正常署名、改ざんbody、誤公開鍵、署名欠落・不正hexを試す | 正常だけを受理し、不正入力は本文・鍵をlogへ出さず401でfail closedとなる |
| `CT-HTTP-003` | 空白、Unicode、field順を含むraw JSONと再serializeしたJSONを署名検証へ渡す | API Gateway由来raw bytesだけを検証し、parse/re-encodeしたbodyで署名を代替しない |
| `CT-HTTP-004` | 許容window内外、未来、malformedなDiscord timestampを固定Clockで試す | window内だけを受理し、期限外・未来・不正値を副作用前に拒否する |
| `CT-HTTP-005` | 同じinteraction ID/内容を再送し、同じIDで異なる内容も送る | 同一内容は保存済み結果を冪等に返し、内容不一致とreplayを拒否して新requestを作らない |
| `CT-HTTP-006` | persistence item、log、metric、status record、exception、artifactをscanする | Interaction tokenが全境界で0件。Status Publisherはchannel/message IDとmoderator Bot tokenだけを使う |
| `CT-HTTP-007` | DiscordIngress LambdaのSnapStart、published version、固定alias、API Gateway integration/permission、bundle checksum変更をsynth templateで検査する | SnapStartはIngressだけ、API Gatewayはaliasだけをinvokeし、`$LATEST`とProvisioned Concurrencyは0。実測ZIP SHA-256がversionへ一致し、bundle変更で新versionになる |
| `CT-HTTP-008` | fresh module import、SnapStart restore後の初回invoke、warm invokeをfake SDK factoryとcontent-free logで試す | snapshotへSDK client、credential、SSM値、request dataを保持せず、restore/cold/warm区分とhandler時間だけを記録する。署名、raw body、質問、tokenは記録しない |

### 3.3.2 Ingress queue・status contract

| ID | Procedure | Pass criteria |
|---|---|---|
| `CT-QUEUE-001` | 同一GuildへFIFO順に20件を受付後、21件目を並行投入する | 20件だけを原子的に保存し、21件目はephemeral busyで拒否する。global active debate slot 3とは別counterである |
| `CT-QUEUE-002` | STARTING、3分timeout、RECOVERED、ACCEPTED、FAILED、REJECTEDのstatus要求を重複・順不同で処理する | 永続operation IDとversion/CASに従い単調に更新し、stale更新を拒否する |
| `CT-QUEUE-003` | 同一interaction、status operation、claimを重複実行し、同じIDへ異なるpayloadを与える | 同値再送だけ冪等、異なるpayloadと二重counter増減を拒否する |
| `CT-QUEUE-004` | drainerを複数ownerで起動し、claim expiry、fencing喪失、期限切れrequestを混在させる | FIFOとclaim/fencingを維持し、同時実行は3 slot以下、15分超過requestを実行しない |

### 3.4 OpenAI adapter contract test definitions

| ID | Procedure | Pass criteria |
|---|---|---|
| `CT-OPENAI-001` | 公式OpenAI Python SDKとnetworkに接続しないmock HTTP transportで初回意見、最終案、投票、決定事項の応答を返す | `responses.parse()`が全schemaをdomain型へ変換し、決定事項はPythonが選出したwinnerを維持する |
| `CT-OPENAI-002` | refusal、response/message incomplete、parsed output欠落、未知field・型・lengthの不正schemaを返す | raw provider contentを露出せず、`refusal`、`incomplete`、`invalid_output`の安定codeへ区別して変換する |
| `CT-OPENAI-006` | Question Routerへ最新事実、表記揺れ、高risk語、一般的な今日の提案、明示的な時間非依存質問、未知表現を渡す | version付き規則で`required`、`optional`、`none`へ分類し、未知表現は`optional`、rules version/reasonをEvidenceへ保存する |
| `CT-OPENAI-007` | typed web search responseへaction sources、URL citation、digestを含める | 1 request、最大4 tool calls、`store=false`で要約と重複排除sourceをimmutable Evidenceへ変換する |
| `CT-OPENAI-008` | optional/required検索でprovider errorを発生させる | optionalは`optional_unavailable`を保存して続行し、requiredは`required_evidence_unavailable`でFAILEDとなる |
| `CT-OPENAI-003` | 429、認証、権限、model不在、5xx/transport失敗をSDK境界に注入する | SDK内の最大3 attempt後にrate limit、configuration、unavailableを区別し、application層で重複retryしない |
| `CT-OPENAI-004` | 送信request body/headerとusage/failure recorderを検査する | `store=false`、`tools=[]`、`tool_choice=none`、Multi-agent fieldと`OpenAI-Beta`ヘッダなし。prompt/output/user IDをtelemetryに含めない |
| `CT-OPENAI-005` | personaの欠落・余分・空・3,500 UTF-8 bytes超過、concurrency範囲外、自己投票・未知candidateを入力する | API呼出し前またはdomain変換時にfail closedになり、最大並列数が6を超えない |
| `CT-OPENAI-009` | Terra standardとLuna proのmock requestを送る | model IDを区別し、proだけが`reasoning.mode=pro`、両方にMulti-agent field/headerがない |
| `CT-OPENAI-010` | production既定configと将来のbootstrapを検査する | 全生成phaseが`PRODUCTION_POLICY=luna_standard`を使用し、Terra/proをruntime入力から選択できない |
| `CT-OPENAI-011` | 3つのprivate persona設定をdeploy前検証し、同じ質問・Evidenceでcontract testする | 正規化prompt hashが全て異なり、判断lensは異なる一方、安全制約、Evidence境界、出力schemaは一致する。prompt本文はlog・artifactへ出ない |
| `UT-ESC-001` | 投票順を入れ替え、分裂・低軸・低平均の単独/複合ballotを評価する | `escalation-shadow-v1`が本文や表示名に依存せず同じsignalを返す |
| `ST-ESC-EVAL-001` | `--live`、API key、repository外outputのgateと10件fixtureを検査する | 通常CI/API keyなしでは実通信せず、raw回答をrepositoryへ書けない |
| `ST-ESC-EVAL-002` | scorer出力とPolicy keyを同一・親子・repository内pathへ指定する | 全てfail closedとなり、別directory treeだけを許可する |
| `ST-ESC-EVAL-003` | 成功、major failure、operational failure、preference-only、詳細rubric、artifact不一致を集計する | preference-onlyはA/B/tie勝数、同数時は費用・p95 latencyで選び、詳細modeだけ1〜5を要求する。operational failure、invalid input/keyはfail closedとし、集計へ本文を含めない |
| `ST-ESC-EVAL-004` | 対話reviewを中断・再開し、各caseでA/B/tieと不正入力を試す | Policy名・性能値を表示せず、各選択をowner-only fileへatomic保存し、既採点caseを飛ばして再開できる |

## 4. Functional scenarios

### Normal

| ID | Scenario | Expected result |
|---|---|---|
| FT-001 | 許可Guild/channelで`/shittim` | HTTP ingressがraw bodyを署名検証し、FIFO/operation result/Statusを原子的に永続化した後、3秒以内にtype 4 ephemeral応答。Runtime READY/recovery後にだけstarter/Public Thread/panelを1回作成しaccepted debateへbinding |
| FT-002 | Evidence不要 | 3人格の初回意見へ進む |
| FT-003 | Evidence必要 | source付き要約を保存・投稿する |
| FT-004 | 3人格討論 | phase順、各artifact、checkpointが一致する |
| FT-005 | 匿名投票 | 投票者名を公開せず有効票を集計する |
| FT-006 | 同票 | 決定済みtie-break規則で一意に決める |
| FT-007 | 最終決定 | Discord投稿とoutbox完了、debate completed |
| FT-008 | 同時受付と実行 | FIFO最大20件を受け付け、実行中はglobal 3 slot以下、21件目はbusy response |
| FT-009 | restart | recoverable recordを自動再開し重複投稿しない |

### Abnormal

| ID | Condition | Expected result |
|---|---|---|
| AT-001 | question 0文字/1001文字 | public messageを作らずephemeral validation error |
| AT-002 | 別Guild/allowlist外/非text channel | fail closed |
| AT-003 | channel allowlist空 | startup失敗、新規受付なし |
| AT-004 | 1 Bot非READY | HTTP受付は永続し、Runtimeの新規claim/drainだけ停止。既存状態を破壊しない |
| AT-005 | Discord/OpenAI 429 | SDK/Retry-Afterに従い上限後domain error |
| AT-006 | refusal/incomplete/invalid schema | phase別処理、raw応答を投稿しない |
| AT-007 | archived/locked thread | archivedは仕様通り扱い、lockedを自動解除しない |
| AT-008 | Discord権限剥奪 | publish failureをoutboxへ保持しalarm |
| AT-009 | scale-down/host異常中のSIGTERM/SIGKILL | checkpointから再開しfencingで旧write拒否 |
| AT-010 | Discord送信後DynamoDB失敗 | nonce照合で重複投稿しない |
| AT-011 | DynamoDB保存後Discord失敗 | pending outboxから再送する |
| AT-012 | lease競合/期限切れ | 同時ownerを許さず新fencing tokenだけ有効 |
| AT-013 | 数分を超える停止後のoutbox再送 | 22文字nonce、content hash、chunk sequence、thread履歴から既存投稿を採用 |
| AT-014 | 高risk質問 | category専用事前拒否はせず、provider refusal時だけ迂回せず安全なdomain errorを表示 |
| AT-015 | FAILED操作panelでretryを二重実行、または古いattemptから実行 | 同じthread内で新attemptを1件だけ作成し、日次quotaは増やさずglobal leaseを1つ取得し、operation ID/current-attempt CASで重複・分岐を拒否する |

### 4.1 Persistence contract test definitions

| ID | Procedure | Pass criteria |
|---|---|---|
| `CT-PERSIST-001` | 全artifact、Guild/channel/thread、leaseを持つsnapshotを縦分割itemへ変換し順序を変えて復元する | 内容と型を損なわずcurrent attemptを復元し、PK/SKとGSI属性が設計どおり |
| `CT-PERSIST-002` | 空Evidenceと未取得Evidence、active/terminal attemptをserializeする | Evidence状態を区別し、terminal itemからrecoverable GSI属性を除去する |
| `CT-PERSIST-003` | 直前schema item、未知version、必須属性欠落、duplicate META、partial leaseをreaderへ渡す | 直前schemaだけをcurrentへup-convertし、それ以外は副作用なしでfail closed。v5 debate_metaはname snapshotを`requester_id` fallbackへ置換し、v6でusername/display name欠落または空は拒否する |
| `CT-PERSIST-004` | 400KBを超えるUTF-8 itemをSDK境界へ渡す | AWS API呼出し前に安定した`ItemTooLarge`で拒否する |
| `CT-PERSIST-005` | nonce、content hash、chunk、claim、sent resultが正常/不整合なoutboxとpanel recordを作り読戻す | 22文字base64url、SHA-256、claim/sent不変条件、安定PK/SK、record typeを双方向で強制する |
| `CT-PERSIST-006` | accept/cancel/retry operationを再送し、同じoperation IDを別入力へ再利用する | 同じ結果を返し、新debate/attemptを作らず、別入力への再利用を拒否する |
| `CT-PERSIST-007` | DynamoDB Localで受付、日次quota、3 slot、terminal解放、FAILED retry、recoverable GSI、lease renewを実行する | transactionが原子的に成功し、4件目をbusy、quota 30件到達後をquota超過、旧fencingを拒否する |
| `CT-PERSIST-008` | DynamoDB Localでoutboxのprepare、chunk順、claim、reschedule、再claim、sent、terminal finalize前の未送信checkを実行し、SDK Stubberでtransaction cancelを注入する | 先行chunk未完了と期限前retryを送信せず、完了再送は同じmessageを返す。ordered cancellation reasonをexact action kindへ対応させ、欠落・不明reasonはfail closedにする |
| `CT-PERSIST-009` | future retry、未失効claim、SENTが混在するattempt outboxをDynamoDB Localから`list_pending`する | base tableのstrongly consistent paginated QueryでSENTだけを除外し、future/claimedを含む未送信全件を決定順で返す |
| `CT-SCHEMA-007` | schema v6のdebate/control fixtureをcurrent readerへ渡し、v7へ変換後にserialize/readを往復する | v6だけを損失なくv7へup-convertし、staged terminal delivery、ingress origin、panel refresh、current phaseを保持する |
| `CT-SCHEMA-008` | 空tableへcontrol-record manifest v2 initializerを並行実行し、9 activity、runtime state、deployment lockを読む | 固定11 recordとv2 markerを一度だけ原子的に作成し、guardがScanなしで同じ11 recordを検証する |
| `CT-SCHEMA-009` | manifest v1 marker、partial v2、未知version、malformed recordをinitializer/guardへ渡す | online修復・自動upgrade・deployを行わずfail closed。v1は未deployのためoffline migration手順だけを案内する |
| `QT-DYNAMODB-LOCAL-RUNNER-001` | `tools/run_dynamodb_local.py`で固定digestのDynamoDB Localをrandom loopback portへ起動し、全pytestを実行する | signed `ListTables`でreadyを確認後、`DYNAMODB_ENDPOINT_URL`をchildだけへ注入。全persistence testがskipなしで成功し、PID由来containerが停止・削除される |

ローカルの標準実行は`uv run --frozen python tools/run_dynamodb_local.py`とする。helperはPodmanを優先しDockerへfallbackし、host port `8000`を固定しない。Codexの制限sandboxがrootless Podman runtime directoryをread-onlyとして拒否する場合はhost terminalで同じhelperを実行する。この実行制約はDynamoDB Localまたはrepositoryの失敗として扱わず、CIと同一digest・同一test commandのhost実行で検証する。

## 5. Infrastructure・security・operations・quality tests

| ID | Procedure | Pass criteria |
|---|---|---|
| `IT-IPv4` | CDK templateでPublic Subnet、Public IPv4、IGW route、ingressなし、egress 443をassertし、deploy後にDiscord/OpenAI/AWS endpointsへ接続する | NAT/ALBなしで全endpointへ接続し、desired/running 0でPublic IPv4は0、singleton task稼働中だけ最大1個となる |
| `IT-SCALE-001` | CDK templateとfake ECSで通常desired 0、On-Demand FARGATE、ARM64、CPU 512/Memory 1024、maximum 1をassertし、pending requestを作る | FARGATE_SPOTが0件で0から1へwakeし、NAT/ALB/ECS inboundなし、3 LambdaはVPC外となる |
| `IT-SCALE-002` | 1分Reconcilerを重複起動し、pending/desired 0、ECS API一時失敗、STOPPING中の新規requestを試す | CAS/generationによりSTARTINGへ戻してdesired 1へ収束し、同時taskが1を超えない |
| `IT-SCALE-003` | fixed Clockで受付後2:59、3:00、3分超から15分未満の復旧を進める | 3:00でtimeout表示するが非terminal・queue維持・wake継続、復旧時はRECOVEREDとなる |
| `IT-SCALE-004` | 14:59、15:00、15分超の未実行requestをReconciler/drainerへ渡す | 15:00でterminal FAILEDとqueue decrementが原子的に成立し、その後drainされない |
| `IT-SCALE-005` | 完全終了時刻から29:59、30:00、30:59を進め、停止直前に新規requestも投入する | 29:59では停止せず、30:00以降の周期でdesired 0へ収束し、新規requestはstale stopを拒否して1へ戻す |
| `IT-SCALE-006` | ingress、task、lease、recovery、outbox、status、panel workを一つずつ残してidle判定する | 全要素が空のときだけIDLEとstop_eligible_atを一度設定し、途中の空時間や最後の受付時刻を30分起点にしない |
| `CT-CDK-STATEFUL-001` | credentialなしでTypeScript strict、Vitest、cdk-nag 3、`cdk synth --strict`を実行し、DynamoDB/ECR/Signer template、tag、retention、termination protection、lifecycleをassertする | exact npm lockで全検査がexit 0。DynamoDBはon-demand、PK/SK、gsi1/gsi2、PITR 35日、deletion protection、RETAIN。ECRは除外なし`IMMUTABLE`、既定暗号化、基本scan-on-push無効、registry拡張スキャン`ENHANCED`/`CONTINUOUS_SCAN`が`shittim-chest`だけに適用、untagged/taggedとも直近3世代だけ保持、RETAIN。Signerは`Notation-OCI-SHA384-ECDSA`、135か月、RETAINで、ECR signing ruleが`shittim-chest`だけに適用される。AWS API呼出しとresource作成0件 |
| `ST-Secret` | repository、artifact、image、logをBetterleaksとredaction fixtureで検査し、Parameter参照をassertする | secret値が0件。ECS execution roleはtaskに必要な正確のversion付きparameterだけ、Status Publisher Lambdaはmoderator tokenだけを読め、他Lambda・ECS task role・他pathを拒否する |
| `ST-IAM` | cdk-nag、IAM Access Analyzer `validate-policy`、role別snapshotを実行する | error finding 0件、task/execution/plan/deploy/drift/break-glassの権限が分離される。plan/deployは拡張scan finding取得用のInspector read API 3種だけを持ち、Inspectorのenable/disable権限を持たない |
| `ST-Privacy` | OpenAI request fixtureとCloudWatch log fixtureを検査する | Discord raw user IDと本文がlogへ出ず、`store=false`と最大30日保持説明が存在する |
| `ST-Delete` | test debateをDiscordとDynamoDBから管理手順で削除し、監査metadataを確認する | 本文を監査logへ残さず両保存先から対象だけを削除する |
| `CT-OIDC` | plan/driftのmain subjectとdeployの`production` Environment subjectを実OIDC claim、synth trust policy、negative claimで比較する | roleごとに期待subjectと`aud`だけが許可され、wildcard、別branch、別Environmentを拒否する |
| `CT-Deploy` | plan manifestとattestation作成後にSHA/digest/SBOM/change set/repository identityを改変し、並行runも開始する | 改変runと並行runを拒否し、Environment承認済みの同一manifest/change set/digestだけをdeployする |
| `CT-GUARD-RACE-001` | DynamoDB Localでdeployment break-glass acquireと初回Runtime wakeをbarrierから同時実行する | lock acquireとwake/activity writeの一方だけが成立し、lock中にRuntime stateを破壊しない |
| `CT-GUARD-RACE-002` | DynamoDB Localでdeployment break-glass acquireと既存Requestのre-wakeをbarrierから同時実行する | 一方だけが成立し、古いwakeがlock後のgeneration/statusを書き換えない |
| `CT-GUARD-RACE-003` | successor lockが新fencing tokenを取得した後に旧owner/guard/fenceでreleaseする | stale releaseを拒否し、successor lockとauditを不変に保つ |
| `IT-GUARD-001` | 現行Production Deploy GuardへSTOPPED、IDLE、STARTING、BUSY、missing/malformedな固定11 recordを与える | read-only診断だけを行い、STOPPED/IDLEかつopen lockだけ成功。lock acquire/deploy/releaseのAWS writeは0件 |
| `IT-GUARD-002` | 将来releaseのacquireを同時に2 runから実行する | 1 runだけがguard ID、owner/actor、fencing token付きlockをtransactionで取得し、他方はfail closedとなる |
| `IT-GUARD-003` | lock取得後にchange set、deploy、smoke、rollbackの各境界で観測する | 全境界で同一lockをholdし、smokeまたは必要なrollback完了前にreleaseしない |
| `IT-GUARD-004` | 成功、各段階失敗、cancel相当を注入し、stale owner/guard/fencingでreleaseする | finally相当でexact metadataだけが一度releaseでき、不一致releaseと並行deployを拒否する |
| `IT-GUARD-005` | deployment break-glassを許可理由/不正理由、active runtime、既存lock、malformed recordで実行する | 許可理由と完全audit時だけruntime条件をoverrideし、既存lockとrecord/identity異常はoverrideしない。ECS Exec revisionとは独立する |
| `IT-GUARD-006` | workflow停止後にlock expiryを超え、正確なmetadataあり/なしで復旧する | 自動reclaimせずLOCKEDを維持。完全一致metadataだけidempotent releaseとimmutable auditを作り、不明時はincidentで停止する |
| `CT-SBOM-Graph` | PRでCycloneDXを生成しstrict schema/`uv.lock`一致を検証する。毎週/manualのmain workflowでDependabot graph job由来のGitHub SPDX exportを取得し、比較前後にmain SHAが不変なwindowでPyPI inventoryを比較する | fork/PRはread-only、custom submissionなしでSPDX 2.3の全dependency name/versionがstable mainの試験済みlockと一致する。main移動時はworkflowを失敗させて再実行する |
| `CT-SBOM-Attestation` | push済みimage digestからSPDX SBOMを生成し、build provenanceとSBOM attestationをECR OCI referrerとして保存する。digest、repository、commit、workflow、SBOM、artifact digestをそれぞれ改変してverifyする | 正しいsubjectだけ成功し、全改変を拒否する。OS/runtime dependencyを含み、ECR `list-image-referrers`で両artifactが`ACTIVE`となる |
| `CT-ECR-SUPPLY-CHAIN-001` | 一意tagでimageをpushし、digestを確定後、Managed Signing statusをpoll、Notation strict verify、ECR scan、4種の`list-image-referrers`、task definition image URIを検査する。署名revocation、tag URI、別digest、referrer欠落、critical findingをnegative fixtureで投入する | 期待Signer profileが`COMPLETE`かつNotation検証成功。AWS Signer signature、SPDX SBOM、build provenance、vulnerability assessmentが同じsubjectへ`ACTIVE`で存在し、task definitionはmanifestと同じdigest URIだけ。全negative caseはchange set実行前に拒否される |
| `OT-ECS-IMAGE-ADMISSION-001` | ECS `PRE_SCALE_UP` lifecycle hookへ署名未完了、referrer不足、tag形式、正常digestを投入し、synthesized `HookDetails`の型と内容を検査する | 正常digestだけ`SUCCEEDED`。異常・timeoutは`FAILED`となりtask起動前にrollbackし、`HookDetails`はJSON objectを表すstring、内容を含まない監査eventを残す |
| `CT-Public` | unauthenticated clone/API、remote ref、全履歴、license scope、Obsidian mirror除外を検査する | Public main以外のref、credential、実AWS account ID、実snowflake、絶対home path、private persona値が0件 |
| `CT-Ruleset` | Ruleset API、merge設定、direct updateのnegative checkを行う | PR、conversation resolution、linear historyが必須で、force push/delete/bypassが禁止される |
| `CT-Environment` | Environment APIと非main deploymentを検査する | reviewerはmaintainer、self-review可、admin bypass不可、main以外はdeploy不可 |
| `CT-Fork` | fork相当のuntrusted PR eventでpermissionsと利用可能contextを検査する | read-only tokenだけで、secret、OIDC、self-hosted runner、productionへ到達できない |
| `CT-CodeSecurity` | Secret scanning、Push protection、CodeQL、Dependency graph、Dependabot、private reportingをAPI/UIで確認する | 全managed security機能が有効で、critical/high未処理alertが0件 |
| `OT-Alarm` | desired/running/runtime state、`BotReady`、`HeartbeatAgeSeconds`、Ingress/outbox滞留、Reconciler失敗、EventBridge task停止を投入する | STOPPED/IDLEの0 task・metric欠測は正常、STARTING/BUSYの非READY/staleと収束不良はalarmとなる |
| `CT-EMF-001` | Runtime/ReconcilerのEMF JSONを生成しnamespace、timestamp、metric unit/resolution、dimension set、値域を検査する。空、負数、NaN/Infinity、naive datetime、非productionをnegative入力する | `ShittimChest/Prod`、dimension `Service` 1組、allowlist 10 metricだけを受理し、全negative入力をfail closedで拒否する |
| `CT-EMF-002` | readiness正常/例外、heartbeat正常/未来/欠落、Reconciler成功/失敗/Status trigger失敗を投入し、所有task終了後の残存taskと全EMF fieldを検査する | `BotReady`、heartbeat age、state/desired/ECS/Ingress/Outbox、2 failure metricが決定的で、本文・Discord/Guild/debate identifierが0件、追加AWS API requestが0件 |
| `CT-OPS-001` | credentialなしCDK synthでOperationsStackのparameter、SNS、alarm/composite、dashboard、EventBridge pattern/target、dependencyを検査する | defaultなし`NoEcho` email、単一TLS topic、9 metric/2 composite alarm、明示missing data、task 0 health通知抑制、異常stop codeだけ、helper compute/Container Insights/KMS customer key 0件、cdk-nag finding 0 |
| `CT-COST-001` | credentialなしCDK synthで`us-east-1` CostGovernanceStackの2 Budget、notification、既存monitor ARN parameter、CAD thresholdを検査する | Project 20 USD/account 30 USD、各actual 80/100・forecast 100、Budget Action 0、AWS managed monitor新規作成0、既存ARN参照、DAILY total-impact 10 USD、実email/account ID 0、cdk-nag finding 0 |
| `OT-Runbook` | token rotation、PITR、failed deploy、3/15/30分、outbox、expired deployment lock、2種break-glassをtabletop/drillする | owner、入力、停止条件、rollback、exact release、証跡が揃い、平常taskがExec無効へ戻る |
| `QT-Ruff` | `ruff format --check`と`ruff check`を実行する | exit 0、対象外除外が承認済みだけ |
| `QT-Ty` | `src`、`tests`、`tools`の対象全体へ固定versionの`ty check`を実行する | diagnostic 0。`missing-type-argument=error`と`possibly-unresolved-reference=warn`を維持し、規則カテゴリ全体を無効化していないこと |
| `QT-Layer` | import-linterとarchitecture testを実行する | domain→外部SDK、application→adapterの逆依存が0件 |
| `QT-Lock` | `uv lock --check`と`uv sync --frozen --all-groups`を実行する | lock変更なし、全groupをfrozenで同期できる |
| `ST-Dependency` | lockをhash付きrequirementsへexportし`pip-audit --strict --require-hashes`で検査する | known vulnerability 0、または期限・owner付きの明示的risk acceptanceだけ |
| `FEAS-SBOM-001` | lockをCycloneDX 1.5 JSONへexportし、公式strict schema、root project、全locked PyPI package、dependency refを検証する | CycloneDX 1.5としてvalidで、resolved dependencyの過不足が0。STEP-02以降は`security`の必須gateとする |
| `QT-Package` | sdist/wheelをsourceからbuildし、clean Python環境へwheelをinstallしてpublic importを実行する | build、install、importが全てexit 0で、source treeへの暗黙依存がない |
| `ST-RepoSurface` | Gitのtracked fileと非ignore untracked fileをsecret/個人情報patternでscanし、force-addした生成物名とsymlinkの回帰testを行う | finding 0、tracked fileをdirectory名で回避できず、symlinkを拒否する |
| `QT-Docs` | CIで直下15文書と`100_Ondemand Fargate/`配下3文書、合計18文書のfrontmatter、fence、Wiki link/heading、公開URL、再帰file setを検査し、local pre-PRでObsidian byte一致を検査する | CI構造検査とlocal正本/mirror検査が成功し、想定外の入れ子file/symlinkを拒否し、public runnerはVaultへアクセスしない |
| `QT-Workflow` | actionlintで全workflowを解析し、Action full SHA、read-only permission、trigger、timeout、concurrencyをreviewする | syntax finding 0、PR jobにsecret/OIDC/write/self-hostedが0、`pull_request_target`は許可済み通知workflow 1件以外0 |
| `QT-GH-DISCORD-A` | workflow name/path allowlist、conclusion、失敗job上限、Embed上限、role ID未設定時の全mention無効化、LF保持・Markdown無害化・JST表示、Python 3.12 AST互換性、GitHub pagination、Discord 429/5xx/400をunit/contract試験する | GitHub hosted runnerのsystem `python3`でparse可能、改行が`�`にならずdependency/versionが過剰escapeされず、開始日時がJST、失敗時も`allowed_mentions.parse=[]`、4回でretryが停止し、secretや本文をsummaryへ出さない |
| `QT-GH-DISCORD-B` | 通常/fork/Dependabot PR、merge/close/direct push/API失敗とtarget workflowのnegative policyを試験する | Dependabot target jobはskip、merge由来pushは抑制、head/artifact/cache/write/許可外targetは拒否 |
| `QT-GH-DISCORD-C` | Dependabot/CodeQL/Grypeの0件・pagination・取得失敗・停止・role ID未設定・手動smoke testを検査する | 不完全な件数を通知せず監視失敗でfail closedし、High/Criticalを含めmentionなしで4固定threadへ正しく配信される |
| `ST-Secret-Betterleaks` | Betterleaksをfetch-depth 0のcheckoutへredaction付きで実行する | 全履歴を完走し、未承認finding 0。provider validation APIへの通信0 |
| `CT-Secret-Positive` | 実行時に無効なGitHub token形式をtemporary Git objectへcommitしてBetterleaksを実行する | source/worktree/fixtureへtokenを保存せず、status 1かつ1件以上をredacted reportへ出力 |
| `CT-Secret-Negative` | `replace-me` placeholderだけをtemporary Git objectへcommitしてBetterleaksを実行する | status 0、finding 0 |
| `ST-Betterleaks-Provenance` | archive、checksums、bundleの固定SHA、Sigstore identity/issuer、署名済みarchive digestを検証する | 全照合一致時だけbinaryを実行し、versionがpinと一致。不一致時fail closed |
| `OT-Tool-Version` | weekly/manual read-only workflowでactionlintとBetterleaksのlatest stable tagをGitHub Releases APIと照合する | 全pin一致で成功。差分・API failure・invalid responseは失敗し、自動更新しない |
| `QT-Patch` | `git diff --check`を実行する | whitespace error 0 |

`CT-HTTP-*`、`CT-QUEUE-*`、`CT-SCHEMA-*`、`CT-GUARD-RACE-*`、`IT-SCALE-*`、`IT-GUARD-001`はlocal/mock・DynamoDB Local・CDK templateでPR上の合否を確定できる。`IT-GUARD-002`〜`006`の一部logicはlocal contractで補助するが、live acquire/hold/releaseの合格とは扱わない。実Discord PING/署名、Interactions Endpoint切替え、ECS `0 -> 1 -> 0`、実3/15/30分、live IAM/alarm、lock acquire/hold/release、break-glass、rollbackはAWS/Discord未展開のため未実施であり、rollout後の限定smoke/drillでのみ合否を記録する。

## 6. Formal E2E scenario

入力はprivate `RuntimeConfig`で指定したGuildの許可channelで次とする。

```text
/shittim question:今日の朝ごはんは何がいい？甘いものが食べたい
```

期待する観測点:

1. シッテムの箱が3秒以内にephemeralで受付中を返す。
2. 起点messageとPublic Threadを作成し、操作panelを表示する。
3. Evidence要否を判定する。一般提案として検索不要なら理由を保存し、検索する場合は`web_search_call.action.sources`由来の共通Evidenceを保存してから3人格が候補、懸念、比較を投稿する。
4. 最終案をschema検証して投稿し、3人格が匿名投票する。
5. tie ruleを含む集計後、甘い朝食の具体的な最終決定と理由を投稿する。
6. debate、artifact、vote、outbox、thread/message IDがDynamoDBと一致し、状態が`COMPLETED`になる。
7. `allowed_mentions`が無効で、同一nonceによる重複messageがない。

内容そのものをgolden textで固定せず、schema、根拠と結論の整合、persona separation、危険な断定がないことを評価rubricで採点する。

## 7. Fault injection

STEP-07A/07Bのoffline試験ではEvidence、初回意見、最終案、投票、決定、outbox待機/送信のapplication task cancellationがcheckpointを経て`CancelledError`を再送出するcontractと、runtimeがそのtaskをcancel・awaitするcontractを組み合わせる。future retryと未失効claimの待機中もlease heartbeatが継続し、fencing喪失時に旧workerがFAILEDを書かないことを確認する。STEP-07Cではfake callbackだけでなく実Unix child processへSIGTERM/SIGKILLを送り、graceful cleanupとreplacement recoveryを検証する。STEP-08のcontainer試験では各境界へ実SIGTERMを注入しapplication内部90秒、process全体120秒以内のgraceful stopを確認する。transaction直前/直後とDiscord POST直前/直後にSIGKILLを注入し、fenced leaseとoutboxがat-least-once処理を重複表示へ発展させないことを確認する。

### 7.1 STEP-08 container gate分割

- STEP-08Aはhost architectureのlocal Podmanでproduction/break-glass両targetのbuild、numeric non-root、capability 0、read-only root、書込み可能`/tmp`、productionへのshell/uv/source/test非混入、heartbeat healthのhealthy/unhealthyを検証する。health entrypointはRuntime packageとthird-party SDKをimportせず、ECSと同じ3秒timeout内に終了することをfocused testで固定する。
- Docker-format image configでhealth interval 10秒、timeout 3秒、start period 30秒、retries 3、exec entrypoint、`SIGTERM`、DHI `nonroot` UID/GID `65532:65532`、runtime labelを検査する。
- STEP-08BはGitHub Actionsのnative ARM64 runnerで同じimageをbuild/testし、container境界SIGTERM/SIGKILL、phase/transaction/Discord POST fault、OS/runtime dependencyを含むimage SPDX SBOMを`container-arm64` gateとして実装する。
- dependency layer試験は、初回build後にsourceだけを変更または同一contextで再buildし、`pyproject.toml`と`uv.lock`が不変なら`--no-install-project` dependency sync layerを再利用することを確認する。`uv.lock`変更時は同layerが無効化され、`--frozen`で新lockどおり再構築されることを確認する。cache有無はimage security、fault、SBOMの合格条件を変更しない。
- QEMUだけの成功をARM64 production compatibilityの最終証跡にしない。STEP-08B native ARM64 gateが成功するまでAWS/CDKへimageを接続しない。

| ID | Procedure | Pass criteria |
|---|---|---|
| `CT-CONTAINER-001` | native ARM64 runnerとDocker daemonのarchitecture、production image config、DHI identity、non-root/read-only/capability/healthを検査する | runner/imageともarm64、`nonroot`/UID/GID `65532:65532`、home `/home/nonroot`、SIGTERM、health 10s/3s/30s/3回、root/home書込み失敗、`/tmp/shittim-chest`書込み成功、shell/uv/apt/dpkg/source/test 0 |
| `CT-CONTAINER-002` | 7つの全非終端phaseでPID 1へSIGTERMを送り、graceful exitを計測する | 各phaseが120秒未満、exit 0、同じphaseでcheckpointed |
| `CT-CONTAINER-003` | transactionとDiscord POSTの直前/直後でPID 1へSIGKILLしreplacementを起動する | exit 137、旧worker checkpointなし、transaction event 1件、content hash一致message 1件、outbox completed |
| `ST-IMAGE-SBOM-001` | Dockerfile builderの`SOURCE_DATE_EPOCH=0`環境、全installed wheel RECORDのcanonical三列CSV、checkout時刻依存のuv cache metadata除去、`rewrite-timestamp=true` exporterでbuildしたproduction/break-glass imageをdigest固定SyftでSPDX 2.3 JSON化しvalidatorを実行する | 両SBOMがSyft creator、Debian package、`shittim-chest`とproduction Python packageを含み、productionは主要dev package 0。imageから再取得したapplication RECORDは非空、canonical、`uv_cache.json`行0。両config digest、SBOM、RECORDを30日artifact upload |
| `ST-DHI-VEX-001` | Docker Scoutでpin済みDHI runtime/devのOpenVEXを`--verify --skip-tlog`で取得し、それぞれのproduction/break-glass Grype `--vex`へ適用する | Docker公開鍵signature検証が成功し、両VEXが非空、vendor suppressionは`not_affected`のみ |
| `ST-GRYPE-POLICY-001` | source/production/break-glassそれぞれでfull JSONと`--only-fixed` JSONを生成し、source/production SARIF、`--fail-on high`、target別risk acceptance validatorを実行する。acceptance更新時はexact CI report/VEX/config-digest artifactとvalidator既定のUTC clockで事前再現し、operator local dateを`--today`へ渡さない | full reportが全findingを保持、actionableはfix state `fixed`のみ、両imageのfixable High/Critical 0、未修正High/Criticalは検証済みVEXまたは現行target config digestの期限内acceptanceで全件cover。承認日がUTCで未来、期限超過、いずれかのconfig digest/VEX/findings変化ならfail closed |
| `ST-DHI-DEPENDABOT-001` | DependabotのDocker updateを手動実行し、`dhi.io`認証とDockerfile digest解析を確認する | daily schedule、credential非表示、`tool_version_not_supported`/private registry errorなし |
| `QT-CONTAINER-CACHE-001` | 同一contextでproduction imageを2回buildし、builderのdependency/project syncを観測する | 2回目は両sync layerを再利用し、production image contractとcontainer fault gateが成功 |
| `QT-GHA-BUILDX-CACHE-001` | native ARM64 PR runを完走後、同じheadを再実行してBuildx metadata/log、loaded image、container gate、SBOMを検査する | 1回目がGHA cacheをexportし、2回目が同scopeをimportして主要layerを再利用。cache miss/error時もbuildと全gateは成功 |
| `QT-IMAGE-REPRO-001` | 同じimage入力をbranch cache-hit run、PR merge-ref cold run、main run、Releaseのnative ARM64 Docker exporterでbuildし、production/break-glassのconfig digestとfile-level SBOM checksumを比較する。productionの`runtime-base`と、package導入を分離した最終`break-glass` stageはcache importを拒否して毎回再生成する | 両targetのconfig digestが完全一致し、そのdigestでtarget別risk acceptanceが成功。productionの同一fileは全checksum一致し、全installed RECORDがcanonical。checkout mtime、cache hit/miss、merge方式、Docker exporter経路で変化しない |

## 8. Performance・cost

- concurrency 3でevent loop lag、memory 1 GiB、CPU 0.5 vCPU、DynamoDB/OpenAI/Discord latencyを計測する。
- 1000文字質問と最大出力を使用し、active processingのp99 300秒以内、OOMなしを確認する。
- load testはDiscord/OpenAI rate limitを尊重し、mock主体で行う。実API試験には日次上限を設定する。

## 9. Acceptance criteria

- 全PR gate成功、domain/application coverage 90%以上。
- critical/highの未承認security findingが0件。
- FT/AT、formal E2E、SIGTERM/SIGKILL、PITR drillの結果と証跡がある。
- ARM64 image、CDK synth/diff、production smokeが成功する。
- 全外部仕様に公式URL、確認日、対象version/service、反映内容がある。
- known issueはseverity、workaround、owner、期限を伴い承認されている。
- 各implementation sliceの実行command、tool version、合否、coverage、security finding、未実施範囲を[[20_実装・試験・検証記録]]へ追記し、失敗・再実行も削除せず残す。

## 10. 公式資料確認記録

| 確認日 | 対象version/service | 公式資料 | 設計への反映 |
|---|---|---|---|
| 2026-07-16 | pytest 9.1 | https://docs.pytest.org/en/stable/ | fixture、marker、test organization |
| 2026-07-16 | Hypothesis 6.156 | https://hypothesis.readthedocs.io/en/latest/ | property-based test |
| 2026-07-17 | Ruff 0.15.22 | https://docs.astral.sh/ruff/configuration/ | Python 3.14対象のformat/lint gate |
| 2026-07-27 | Ruff 0.16.0 | https://docs.astral.sh/ruff/formatter/#markdown-code-formatting ; https://github.com/astral-sh/ruff/releases/tag/0.16.0 | Markdown内Pythonフェンスformatを既定受け入れ。明示lint select維持。Protocol stubは`def f() -> T: ...`形式 |
| 2026-07-19 | ty 0.0.61 | https://docs.astral.sh/ty/ | Python 3.14対象の唯一のtype-check gate。project-local設定を使用 |
| 2026-07-19 | ty migration guide | https://docs.astral.sh/ty/coming-from-mypy-or-pyright/ | `missing-type-argument=error`と`possibly-unresolved-reference=warn`をstrict相当として維持。未実装差分は将来のRuff ANN/PYI評価で補完検討 |
| 2026-07-16 | pip-audit 2.10.1 | https://github.com/pypa/pip-audit | `uv.lock`をhash付きrequirementsへexportして全locked dependencyを監査 |
| 2026-07-16 | DynamoDB Local | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html | local persistence test |
| 2026-07-17 | DynamoDB Local 3.3.0 | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.DownloadingAndRunning.html | digest固定containerで`CT-PERSIST-007`〜`008`をPR必須化 |
| 2026-07-19 | DynamoDB Local v3 usage notes | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.UsageNotes.html | `-inMemory`、`-sharedDb`、dummy credential、local endpoint、productionとの差異をrunnerへ反映 |
| 2026-07-16 | AWS CDK testing | https://docs.aws.amazon.com/cdk/v2/guide/testing.html | fine-grained assertion、validation |
| 2026-07-16 | Discord interactions | https://discord.com/developers/docs/interactions/receiving-and-responding | defer、token、response contract |
| 2026-07-16 | OpenAI Structured Outputs | https://platform.openai.com/docs/guides/structured-outputs | schema/refusal/incomplete contract |
| 2026-07-17 | Python Dependabot graph job | https://docs.github.com/en/code-security/concepts/supply-chain-security/dependency-graph-data | custom submissionなしのmanaged snapshot test |
| 2026-07-16 | GitHub SBOM export | https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/export-dependencies-as-sbom | SPDX inventory照合 |
| 2026-07-16 | GitHub artifact attestations | https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations | image digestとSBOMのnegative verification |
| 2026-07-17 | uv CycloneDX 1.5 preview | https://docs.astral.sh/uv/concepts/projects/export/ | strict schema、project、lock inventory test |
| 2026-07-17 | Python 3.14.6 asyncio | https://docs.python.org/3.14/library/asyncio-eventloop.html#unix-signals、https://docs.python.org/3.14/library/asyncio-task.html | signal registration、owned cancellation、timeoutのruntime contractを追加 |
| 2026-07-17 | ECS task definition parameters | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html | container health、read-only root、ARM64、stop timeoutの試験境界 |
| 2026-07-17 | Fargate task differences | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/fargate-tasks-services.html | local tmpfsとFargate bind volumeの差異を明示（2026-07-20にtmpfs supportを確認し14章で訂正） |
| 2026-07-17 | Docker build best practices | https://docs.docker.com/build/building/best-practices/ | multi-stageと最小runtimeの検査項目 |
| 2026-07-17 | GitHub native ARM64 hosted runner | https://github.blog/changelog/2025-08-07-arm64-hosted-runners-for-public-repositories-are-now-generally-available/ | Public repositoryの`ubuntu-24.04-arm`でQEMUなしのbuild/run gate |
| 2026-07-17 | Syft 1.48.0 | https://github.com/anchore/syft/releases/tag/v1.48.0 | arm64 archive digest固定、SPDX 2.3 image inventory |
| 2026-07-19 | ECR signing status / OCI Referrers | https://docs.aws.amazon.com/cli/latest/reference/ecr/describe-image-signing-status.html、https://docs.aws.amazon.com/AmazonECR/latest/APIReference/API_ListImageReferrers.html | digest指定の自動署名・4 artifact positive/negative test |
| 2026-07-30 | ECR signing profile identity / Notation trust policy | https://docs.aws.amazon.com/AmazonECR/latest/APIReference/API_ImageSigningStatus.html、https://docs.aws.amazon.com/signer/latest/developerguide/image-verification.html | ECR signing statusとNotation trust policyの期待identityをバージョンなしsigning profile ARNへ統一し、バージョン付きARNはfail closedで拒否する |
| 2026-07-19 | AWS Signer Notation verification | https://docs.aws.amazon.com/signer/latest/developerguide/image-verification.html | strict trust policy、profile identity、revocationのfail-closed test |
| 2026-07-19 | ECS lifecycle hook | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/lambda-lifecycle-hooks.html | `PRE_SCALE_UP` admissionとrollback test |
| 2026-07-29 | CloudWatch EMF specification | https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch_Embedded_Metric_Format_Specification.html | CT-EMF-001/002のschema、dimension、resolution、timestamp境界 |
