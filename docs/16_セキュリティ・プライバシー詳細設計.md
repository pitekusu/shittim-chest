---
aliases:
  - The Shittim Chest セキュリティ詳細設計
tags: [project, shittim-chest, security, privacy, detailed-design]
status: production-1.0
created: 2026-07-16
updated: 2026-09-04
---

# セキュリティ・プライバシー詳細設計

## 1. Security objectives

- secretとpersonaをrepository、image、artifact、log、Obsidianへ漏らさない。受付、認可、再開に必要な
  requester／Discord identifierはDynamoDBへ暗号化保存し、IAMで限定する。
- Discord request、user question、Web result、model outputをuntrusted dataとして扱う。
- accepted workと永続outputをprocess喪失、retry、duplicate deliveryから守る。
- GitHubからproductionまでのidentity、source SHA、image、Change Setを検証する。
- 個人規模に対して過剰な常時resourceを持たず、承認済みdegraded behaviorで可用性を保つ。

## 2. Trust boundaries

| Boundary | Required validation |
|---|---|
| Discord → Ingress | freshness、raw-body Ed25519 signature、command schema、deduplication |
| Discord Gateway／REST | Bot identity、Guild／Channel、permission、nonce、response／history照合 |
| User／Evidence → OpenAI | delimiter、untrusted-data instruction、Structured Output |
| OpenAI → application | response status、refusal、schema、phase-specific content rules |
| User question → affection evaluator | code-owned rubric、点数操作／persona開示要求の無視、3件all-or-none |
| application → DynamoDB | schema、size、phase、lease、fencing、transaction condition |
| Browser ADMIN → Config Lambda | Session管理者照合、exact Origin、CSRF、idempotency、strict JSON |
| Browser → Memorial API | Session owner固定、exact Origin、CSRF、idempotency、cycle／upload metadata検証 |
| Memorial input → OpenAI | owner確認、MIME／size／SHA-256、画像と直近10質問をuntrusted dataとして分離 |
| GitHub → AWS | immutable OIDC subject、role、source SHA、attestation、Change Set、lock |

Unknown dataをEvidenceやpersistent recordとして採用しない。一方、Evidence検索と帰宅挨拶には
明示した可用性優先fallbackがあり、未知sourceを捨てて空Evidenceで継続、または挨拶を省略する。

## 3. Secret management

- GitHub Actions secret、SSM SecureString、Environment secretを用途別に分離する。
- Runtimeはexact parameter ARNだけを`GetParameters`し、path enumerationを許可しない。
- CDK synth、PR CI、docs syncはsecret valueを取得しない。
- error、repr、trace、workflow outputへtoken、persona、question、private Discord／user IDを含めない。
  opaque Debate IDとprovider response IDはcontent-freeな障害相関にだけ使用する。
- rotationは新versionを作り、metadata validation後にpointerを切り替える。
- runtime promptは6個のSecureStringをimmutable revisionへ完全保存・再読込検証した後、content-freeな
  active Stringだけを最後に切り替える。不完全revision、checksum不一致、pointer不整合はfail closedとする。
- Memorial用OpenAI keyは他のOpenAI用途と分離したSecureStringとして登録し、Workerだけがexact pathを読む。
  setup／Releaseはmetadataだけを確認し、値をrepository、CloudFormation、Lambda environment、logへ出さない。

## 4. Application protections

- domain winnerをPythonで固定し、model outputからwinnerを読み取らない。
- voteは候補順をshuffleし、自分への投票と3票確定前の公開を拒否する。
- question、Evidence、他者output内の命令をprompt hierarchyで無効化する。
- 親愛度の評価rubric、範囲、応答態度帯はcode所有とし、questionやADMIN-managed promptから
  変更させない。評価理由を生成・保存・公開せず、失敗時の部分スコアを採用しない。
- normal debate outputはfield長、chunk数、Unicode、Markdown、mention、URL display境界を検証する。
- farewellは非空／Discord上限、citation 1件、generation fence、mention抑止へ絞ったbest-effort例外である。
- Memorial APIはSessionからownerを決定し、request bodyの識別子でownerを切り替えない。presigned POSTは
  server生成key、許可MIME、最大size、SHA-256、短い期限へ束縛し、生成前にobject metadataを再検証する。
  upload／generate／resetはstrict integerの`expectedCycle`を必須とし、current cycleと異なる遅延requestを
  409でfenceする。
- jpeg／png／webpの本人uploadとtrusted participant画像は実フォーマット、frame、pixel、バイト数を検証し、
  bounded RGB PNGへ正規化する。両画像の検証完了前にOpenAIを呼び出さず、画像と質問からの命令は
  無視する。1 cycle 1回とcheckpointの条件付き更新をapplicationで強制する。
- checkpoint済みの生成画像を`ready`へ確定する直前にも、許可された`memorials/*`内でS3 objectの実在、
  PNG、size、checksum、寸法を再検証する。欠落または不正なobjectをcheckpointだけで成功扱いせず503でfail closedする。
- 固定AI免責文は表示しないが、refusal、content filter、provider safetyを緩和しない。

## 5. AWS and container protections

- IAM roleをIngress、Status、Reconciler、Admission、task execution／task、Release plan／deploy／driftへ分離する。
- resource-level制御不能なActionだけ理由付き`Resource: "*"`を許し、wildcard Actionを使わない。
- task SGはingress 0、HTTPS egressのみ。normal containerはnon-root、read-only root、tmpfsを使う。
- DHI base imageをdigest固定し、SBOM、VEX、Grype、risk acceptance、Notation、attestationを検証する。
- ECR tag immutable、enhanced scanning、lifecycle 3世代、Image Admissionを維持する。

## 6. Data privacy

| Data | Storage | Log policy |
|---|---|---|
| question／debate output／vote | DynamoDB、Discord | 本文を出さない |
| requester／Discord mapping | DynamoDB | public docs／artifact／logへ出さない |
| private affection profile | source DynamoDB | identity HMACのopaque key、3スコア、cycle／解放metadata。raw requester IDを保存しない |
| projected affection profile | Statistics Table | opaque key、3スコア、reset回数、content-free解放metadata |
| debate affection assessment | source DynamoDB／Archive | 評価理由を持たず、Archiveはraw IDを持たない |
| memorial upload原本 | private temporary S3 | 成功またはterminal失敗で削除、lifecycle 1日、本文／画像をlogへ出さない |
| memorial画像／思い出文 | private Media S3／Statistics Table | owner-only期限付きGET、別ownerやpublic URLへ公開しない |
| memorial checkpoint | Statistics Table | opaque owner key、cycle、state、content-free failure／idempotency metadata |
| participant name／persona | private SSM | name／本文を出さない |
| managed runtime prompt | private SSM immutable revision | 本文、差分、revision内容を出さない |
| prompt audit | Statistics Table | content-free checksum／revision metadataだけ |
| token／key／signature | GitHub secret／SSM／request memory | 絶対に出さない |
| Evidence summary／citation | DynamoDB internal | URL／query／summaryを出さない |
| operational metadata | DynamoDB／CloudWatch | allowlist済みcode／count、opaque相関IDのみ |

OpenAI Responses API requestは`store=false`とする。Images APIには対応するstore parameterがないため、
画像provider側の保持を含むdata handlingとabuse monitoringはOpenAIのdata control契約に従う。

## 7. Vulnerability handling

- Dependabot、pip-audit、npm audit、CodeQL、Grype、GitHub secret scanningを継続する。
- npm公式StatuspageでSecurity Auditのactive incidentをexact確認できる間だけ、Node系auditを当該runで
  skipできる。公式状態を検証できない場合はfail closedとし、finding、timeout、任意の環境変数だけでは
  skipしない。残るsecurity／build／test gateは通常どおり実行する。
- fix可能なHigh／CriticalはRelease前に拒否する。unfixable findingはverified VEXまたは期限付きの
  target-specific local acceptanceだけを許す。
- private vulnerability reportingを入口とし、public Issueへcredentialやprivate contentを出さない。

## 8. 公式資料確認記録

| 確認日 | 対象 | 公式資料 | 設計への反映 |
|---|---|---|---|
| 2026-08-14 | AWS IAM | https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html | temporary credentialとleast privilege |
| 2026-08-14 | SSM Parameter Store | https://docs.aws.amazon.com/systems-manager/latest/userguide/systems-manager-parameter-store.html | SecureStringとexact path |
| 2026-08-14 | ECS security | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/security.html | role／network／container境界 |
| 2026-08-14 | OpenAI data controls | https://developers.openai.com/api/docs/guides/your-data | `store=false`とprovider retention |
| 2026-08-14 | ECR signing | https://docs.aws.amazon.com/AmazonECR/latest/userguide/managed-signing.html | signed image verification |
