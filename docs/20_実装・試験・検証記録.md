---
aliases:
  - The Shittim Chest 実装検証記録
tags: [project, shittim-chest, implementation, test-evidence]
status: active
created: 2026-07-16
updated: 2026-08-11
---

# 実装・試験・検証記録

## 1. 本書の役割

本書は、Codexとmaintainerが現在の実装到達点、重要な障害原因、最新の受入結果、未解決事項を短時間で把握するための要約である。

- 要求は[[01_要求仕様書・基本設計書]]、決定理由は[[02_議論事項・意思決定記録]]を正とする。
- interfaceとfailure処理は各詳細設計、合否条件は[[18_試験・品質保証設計]]、実装順序は[[19_実装計画・トレーサビリティ]]を正とする。
- exact command、全check、commit、run ID、artifact digest、失敗・再実行の完全な履歴はGitHub Pull Request／Actionsを正とする。本書へ逐次複製しない。
- 本書には、現行設計の判断に影響する最終結果、再発防止上必要な直接原因、最新のlive受入、未解決事項だけを残す。
- secret、本番runtime値、user content、Discord/OpenAI本文、credential、ローカル絶対pathを記録しない。
- local、CI、Production Release、live acceptanceを区別し、未実施を成功扱いしない。

## 2. 現行実装の到達点

| 領域 | 状態 | 現行の要点 | 証拠の入口 |
|---|---|---|---|
| Domain／application | 実装済み | Pythonがwinnerを決定。phase、retry、deadline、fencing、checkpointを型と状態機械で制御 | PR `#1`、`#15`以降、[[10_アプリケーション・Python詳細設計]] |
| DynamoDB | 実装済み | transaction、lease、operation replay、FIFO ingress、schema migration、unknown schema fail closed | PR `#16`、`#18`、`#85`、[[13_DynamoDB・データ整合性詳細設計]] |
| OpenAI | 実装・production live検証済み | Responses API、Structured Outputs、`store=false`、Luna standard固定、Web Evidence、最大6並列 | PR `#20`〜`#26`、`#167`、`#169`、[[12_OpenAI・プロンプト詳細設計]] |
| Discord | 実装済み | signed HTTP ingress、4 Bot、thread/panel/status、durable retry、rate-limit evidence | PR `#27`〜`#35`、`#85`、`#149`〜`#162`、[[11_Discord詳細設計]] |
| Runtime | 実装・deploy済み | ARM64 On-Demand Fargate、通常`desiredCount=0`、最大1 task、0→1→0、health、reconciler | PR `#33`〜`#39`、`#85`、`#139`、[[14_AWS・CDK詳細設計]] |
| Container／supply chain | 実装済み | DHI、digest pin、SBOM、VEX、Grype、signature、attestation、reproducible build gate | PR `#37`〜`#62`、`#105`〜`#161`、[[15_GitHub・CI-CD詳細設計]] |
| AWS stacks | deploy済み | Stateful、Runtime、Operations、CostGovernance。停止時ECS `0/0/0`、deployment lock、Change Set cleanup | 初回Release以降、[[14_AWS・CDK詳細設計]]、[[17_運用保守・監視・障害対応設計]] |
| Discord討論過程表示 | 実装・本番受入完了 | GenerationCheckpoint、Outbox v2、global順序、初回意見、最終案、投票、moderatorの集計、winner personaによる最終発表 | PR `#164`／`#165`／`#171`／`#172`／`#174`〜`#179`、[[23_Discord討論過程表示実装計画]] |

## 3. 現行の検証・security境界

- required CIはquality、tests、security、package、docs、CDK、ARM64 container、Grypeを含み、CodeQL 3言語をmerge前に確認する。
- domain/application coverageは90%以上を維持する。PR `#165`の最終local suiteはDynamoDB Localを含む1,808 passed、coverage 90.22%だった。正確な継続値は各PRの最新CIを正とする。
- production／break-glassのconfig digest baselineは`security/container-risk-acceptance.json`だけを正とし、本書へ転記しない。現行schemaはv3、local acceptanceは0件である。
- image configが変わるPRはcanonical CIの同一測定で両targetを実測し、両SBOM、verified VEX、risk gateとの対応を確認してから両baselineを同じPRで更新する。
- localのproduction／break-glass full image buildは通常行わず、canonical ARM64 CIを最終判定とする。
- OpenAI、Discord、AWSのlive試験は明示承認された工程だけで行い、mock／contract成功をlive成功と扱わない。

## 4. 現行運用で保持する重要契約

1. Discord署名はraw bodyに対してJSON parse前に検証する。Interaction token、raw body、署名、質問を永続化・log出力しない。
2. Ingressはdurable acceptance前に成功を返さない。DynamoDB transaction、deployment lock、operation binding、FIFO上限を維持する。
3. RuntimeはARM64 On-Demand Fargate、通常`desiredCount=0`、最大1 taskとする。停止時はdesired/running/pending `0/0/0`、lock open、active Change Set 0を確認する。
4. OpenAI sourceと生成物はuntrusted dataである。winnerはPythonだけが決定する。
5. Outboxはnonce、fencing、content照合、有限retryを持つ。必須delivery欠落のままCOMPLETEDへ進めない。
6. Releaseはimmutable artifact、CI同一image config、signature／attestation、Change Set、Environment承認境界を維持する。失敗runをrerunしない。

## 5. 設計へ影響する主要な障害教訓

| 時期 | 直接原因 | 維持する再発防止 |
|---|---|---|
| 初回Production Release | `ecs:DescribeTaskDefinition`はresource-level ARN制限をsupportせずAccessDenied | Image Admission／Release Deployの同Actionだけを独立`Resource: "*"`。application側のtask definition／image exact検証は維持 |
| 初回image release | pytest生成bytecode、dirty workspace、異なるexporter／cache materializationでconfig digestが変化 | immutable fresh checkout、bytecode非生成・context除外、CI同一exporter／compression／cache、両target baseline同時更新 |
| PR `#175` merge後main CI | PR merge-refではbuilder final syncがcache hit、同一treeのmainではcache missとなり、両targetの差分が最終`.venv` COPY layerへ限定された。既存artifactのRECORD／SBOM内容は一致し、local同一inputのcold／warm installではmtimeだけが不一致 | RECORD／`uv_cache.json`正規化後に`.venv`の全regular file、directory、symlinkのmtimeを`SOURCE_DATE_EPOCH`へ固定し、未知file typeと正規化不能をfail closed |
| DHI更新後のPR `#178`〜`#184` | DHI更新による正当なbase layer変更と、production最終venv layerの潜在的な非決定性が同時に現れた。PRとmainは同じGit treeでもbuilder生成entryの細かなmodeが異なり得たが、transfer helperはsource modeを保存していた。コピー先へのexplicit chmodはprocess umaskだけを除去し、source mode差を残した。RECORDとSBOMのfile checksum一致はdirectory／mode一致を証明せず、aggregate canonical tree digestは差の存在だけを示してpathを特定できなかった。BuildKit recordの`containerimage.config`を最終RootFS証拠と誤認し、非対応の`BUILDKIT_MULTI_PLATFORM=1`も試したため切り分けが長期化した | production transferはdirectory `0755`、実行可能regular file `0755`、その他のregular file `0644`へ正規化し、UID/GID、mtime、content、symlink targetを固定する。source modeとprocess umaskが異なる回帰試験を維持する。DHI更新時は両targetを同じcanonical CIで測り、config digest→全RootFS diff ID→canonical tree digest→RECORD／SBOMの順に差分層を絞る。tree digestが違えばexporterやbaselineを原因と決めつけずfilesystem metadata境界として扱う。両SBOM、VEX、risk gateを確認した同一測定値だけでproduction／break-glass baselineを一括更新し、PR／main不一致は正当なDHI更新ではなく再現性failureとして停止する |
| Runtime Change Set | SecureStringをLambda Environmentのplaintext SSM dynamic referenceへ渡した | exact 2 parameterを初期化時に取得し、token-free設定だけをSnapStart snapshotへ保持。secret値は取得・記録しない |
| Runtime停止 | application importを含むhealth subprocessが3秒timeoutでfalse negative | stdlib-only health entrypoint、task固有tmpfs heartbeat、content-free exit code |
| 連続Discord実行 | discord.pyの短い`max_ratelimit_timeout`が残数ありでもpreemptive拒否 | SDK 300秒window、application側45秒timeout、Retry-After durable schedule、content-free bucket evidence |
| Ingress初期応答 | restoreとDynamoDB enqueueが1.2秒soft deadlineを超え、依頼は保存されてもephemeral応答が失敗 | SnapStart継続。PR 2でsoft deadline 2.0秒と段階別content-free timingを導入し、live 1回で微調整判断 |
| Web Evidence | `web_search_call.action.sources[].url=null`をcanonical sourceとして厳格処理し、有効citationがあっても`required_evidence_unavailable` | PR 1で`url_citation`を正本、`action.sources`を補助観測へ限定。有効citation 0件はfail closed |
| Web Evidence real-time feed | OpenAIの正規`oai-weather`応答は`type=api`、URL citationなしであり、URL-only実装がsource 0件と誤判定 | 公式3 feedだけをexact allowlistしstable source URIへ変換。未知provider／形状はfail closed、feed種別をcontent-free記録 |
| SNS email | 外部unsubscribeでCloudFormation resourceは残るがsubscription IDがDeleted | standalone subscription、認証付きconfirmation、active ARN／PendingConfirmationをread-only確認 |
| Infrastructure Drift run `30882818656` | `DetectStackDrift`開始時にdrift roleが`DetectStackResourceDrift`を持たず、最初のStateful stackでAccessDenied | CloudFormationの3必須Actionと、5 stackの現行resource provider schemaから実測したread handler権限を固定。data-plane、`iam:PassRole`、remediation権限は付与しない |

## 6. 最新のProduction／live受入要約

- 2026-08-02の初回成功Releaseで4 stackはstable、ECSは`0/0/0`、deployment lockはopen、未実行Change Setは0へ収束した。以後のReleaseも同じ停止条件を必須とする。
- `/shittim`は討論完了、thread panelとchannel statusのCOMPLETED収束、連続2件のInteraction分離、ECS 0→1→0をlive確認済みである。
- Discord IngressはSnapStartと2.0秒soft deadlineを維持し、PR `#168`以降の複数live受入で「アプリケーションが応答しませんでした」を再現せず、durable受付へ成功した。Provisioned ConcurrencyやSQSは追加していない。
- Web EvidenceはPR `#167`でURL citationを正本化し、PR `#169`で公式real-time feedをexact allowlistへ追加した。以後のlive討論はEvidence経路を含めCOMPLETEDへ収束している。
- Discord討論過程表示は段階Releaseを完了した。最終live acceptanceでは、moderatorの票数・winner発表に続き、保存済みwinnerのBotがpersonaに基づく勝利コメントと最終決定を投稿した。terminal 2 operationsは固定順、各delivery attempt 1でSENTとなり、重複／ABANDONED 0、全Delivery Plan DELIVERED、Outbox／active attempt 0へ収束した。ECS停止状態はこの機能受入の対象外とした。
- 2026-08-11の帰宅挨拶・勝利表現改善PR-1 `#188`では、既存の1回のdecision Structured Output内で、winner persona固有の驚き、歓喜、感謝、高揚感を大げさに表すよう`victory_message` promptを強化した。COMPLETED／FAILED／CANCELLEDから固定AI免責文を削除し、winner判定、schema、generation checkpoint、Outbox順序、provider safetyは変更していない。focused test 32件、Ruff、ty、import contract、docs／public surface gateは成功した。最初のcanonical CI run `31459334069`ではconfig baseline不一致だけが発生し、同一artifactのproduction `sha256:552f556021e4dfff4b39209f48282b21f8593da405dcaff99b422d15b2860ad9`、break-glass `sha256:1ebb668bd00f9c4f23fbbae05a15afba03767febbce99ec7278c5dddf967de0c`を両baselineへ一括反映した。両SBOMはcanonical validatorで有効、verified VEX後のrisk gateはproduction `vendor_vex=15`／local acceptance 0、break-glass `vendor_vex=33`／local acceptance 0で成功した。Production Releaseとlive acceptanceは未実施である。
- 2026-08-11の帰宅挨拶PR-2 local実装では、RuntimeConfig v2／v0003、IDLE停止2分前のTokyo Web search付き先行生成、同一generationの正常な30分STOPPINGだけのparticipant direct sendを追加した。候補はprocess memoryだけに保持し、debate schema／durable Outbox／AWS resource／IAMは変更していない。帰宅挨拶はbest-effortかつat-most-onceで、OpenAI／Discord失敗時も90秒以内shutdownを継続する。focused test 131件とDynamoDB Localを含む全1920件が成功した。最初のcanonical CI run `31462324706`ではsource変更に対応するbaseline不一致だけが発生した。review修正後のrun `31463651668`、`31472196027`、未完了Web searchをfail closedとしたrun `31474134279`に続き、未完了output messageもfail closedとしたrun `31476445924`を最終測定とした。同runのimage artifact ID `9095468253`（digest `sha256:c3a8329d6c449b2efb9bf0d4f9db785a02b03d9687da324a9ddcb185ba5ffb78`）とGrype artifact ID `9095524075`（digest `sha256:62aa6eae158d052c1f16e664150b9ac26683d924c8220ed74cd813781d79617e`）を正本として、production `sha256:1f0d2da5da8cbb14be282c6f525cf42d5d91d0677a0b56e368bf19852158cdfc`、break-glass `sha256:a81e8c79045a639c9a30964370f5cfe10a207c28be48fbcf927b670cddbb0615`を両baselineへ一括反映した。両SBOMはcanonical validatorで有効、verified VEX後のrisk gateはproduction `vendor_vex=15`／local acceptance 0、break-glass `vendor_vex=33`／local acceptance 0で成功した。live OpenAI／Discord／AWS、Production Releaseは未実施である。
- Productionの正確な現在状態はrunbookのread-only確認を正とし、本書の過去snapshotだけでdeploy可否を判断しない。

### 6.1 Operational drill evidence

実装・設定・runbookの存在をlive drill完了として扱わない。現時点の証拠境界は次のとおりである。

| 項目 | 証拠 | 判定 |
|---|---|---|
| Release rollback／cleanup／deployment lock | Production Release run `30693592459`の実failureで後続deployを抑止し、未実行Change Set 0、lock open、既存Stackの安全状態へ収束した。原因修正後のrun `31311389113`はsuccessとなり、同じrelease境界の復旧を確認した | 実障害によるlive recovery evidenceあり。意図的な障害注入runではない |
| Scale-to-zero wake／stop | `/shittim`のlive acceptanceでECS `0→1→0`、討論COMPLETED、Status収束を確認した | 完了 |
| Bot token rotation | rotation順序、停止条件、4 READY確認はrunbook化済み。live token reset／SecureString更新の実施記録はない | 未実施 |
| DynamoDB PITR restore | PITR 35日、deletion protection、RETAINはlive設定確認済み。新tableへのrestore／schema・count検証／切替えの実施記録はない | 未実施 |
| break-glass ECS Exec | 専用image、role、log group、通常taskのExec無効はcode／CDK／CIで検証済み。live ECS Exec sessionと通常revisionへの復帰記録はない | 未実施 |

未実施3件を完了と推定・転記してはならない。各drillは明示承認された独立工程で実行し、秘密値やsession本文を含まない結果だけを本表へ追記する。

## 7. 現在の順序付き作業

Discord受付・Web Evidence・討論過程表示の順序付き是正と帰宅挨拶・勝利表現改善PR-1は完了した。現在は帰宅挨拶PR-2を実装中であり、v0003 private input準備、canonical CI、Production Release、live受入は未実施である。

## 8. 未解決事項

| 項目 | 状態／停止条件 | 次のowner |
|---|---|---|
| CDK transitive audit例外 | 本番安定後、修正版をbundleするCDKへ独立更新し期限付き例外を削除 | dependency保守PR |
| live permission／rate-limit drift | 自動修正しない。content-free evidenceとrunbookで確認 | operator |
| operational live drill 3件 | Bot token rotation、DynamoDB PITR restore、break-glass ECS Execは未実施。個別承認・個別停止条件で1件ずつ実行する | operator／別工程 |

## 9. 歴史証跡の検索先

本書から削除したrun ID、artifact digest、全test数、失敗・再試行の詳細は失われていない。GitHub Pull Request、Actions run、Security／Code scanning、Release evidence artifactを正とする。

| 調査対象 | 主なPR範囲 |
|---|---|
| Domain、DynamoDB、OpenAI、Discord foundation | `#1`〜`#35` |
| Container、CI、DHI、CDK基盤 | `#37`〜`#62` |
| GitHub／Discord通知 | `#71`〜`#78` |
| Scale-to-zero、Operations、Cost、Release | `#85`〜`#137` |
| Production health、rate limit、status収束、SnapStart | `#139`〜`#162` |
| 討論過程表示計画／delivery foundation／段階表示／最終発表 | `#164`、`#165`、`#171`、`#172`、`#174`〜`#179` |

過去runの再監査は、現在の直接原因または安全状態を確定するために必要な最小範囲だけで行う。

## 10. 今後の記録形式

新しいsliceでは次だけを追記する。

1. 目的、scope／non-scope、PR番号。
2. 最終合否と、設計へ影響した直接原因または重要な回帰防止。
3. 最新のlive受入、外部write、安全な停止状態。
4. 未解決事項、owner、次へ進む条件。

exact command、全check一覧、run ID、artifact digest、逐次logはPR／Actionsへ残し、本書へ重複記載しない。解決済みで現行設計の判断に使わない履歴は、次回整理時に削除または1行へ統合する。
