---
aliases:
  - The Shittim Chest 実装検証記録
tags: [project, shittim-chest, implementation, test-evidence]
status: active
created: 2026-07-16
updated: 2026-07-19
---

# 実装・試験・検証記録

## 1. 目的・記録規約

本書は各implementation sliceで実施した試験と検証の実測証跡を累積する。設計上の合否条件は[[18_試験・品質保証設計]]、実装順序と要求対応は[[19_実装計画・トレーサビリティ]]を正とする。

- sliceごとにID、scope、branch/base、tool version、command、result、coverage、security finding、生成artifact、未実施範囲、既知問題、PR/CI結果を記録する。
- 失敗、再実行、条件付きskipを削除せず、後続結果から参照できる形で残す。
- secret、本番runtime値、user content、Discord/OpenAI本文、credential形式、ローカル絶対home pathを記録しない。
- local合格とGitHub CI、production integration、deploy合格を区別し、未実施を成功扱いしない。
- 本書はpublic-safeなObsidian正本からrepository `docs/`へmirrorする。

## 2. STEP-01 Python domain foundation

### 2.1 Metadata

| Field | Value |
|---|---|
| Step ID | `STEP-01-PYTHON-DOMAIN-FOUNDATION` |
| Status | Completed・PR `#1` squash merged |
| Date | 2026-07-16 |
| Branch | `agent/python-domain-foundation` |
| Base commit | `29229b130081597f55f177619cd334668d6f03cb` |
| Requirement | RQ-002、RQ-004、RQ-011、RQ-015、RQ-017 |
| Test ID | UT-DOM-001〜005、QT-Lock、QT-Ruff、QT-Mypy、ST-Dependency、FEAS-SBOM-001、QT-Package、ST-RepoSurface、QT-Docs、QT-Patch |

### 2.2 Scope

- Python 3.14/uv project、lock、source layout、sdist/wheel build基盤。
- UUIDv7 `DebateId`と`AttemptId`、UTC/schema version validation。
- 10 phase、21許可edge、Spot checkpoint/recovery、terminal invariantを持つimmutable state machine。
- FAILED attemptを保持したまま同じdebateで新attemptを作るretry。`retry_of`と`failed_from_phase`を保持する。
- unit/property test、Ruff、mypy strict、coverage、dependency audit、公開surface、Obsidian mirror検証。
- GitHub Dependency Graph、container SBOM、release attestationの実装段階を設計へ反映。

### 2.3 Environment

| Component | Version/condition |
|---|---|
| Python | `3.14.6`、通常GIL build |
| uv / uv_build | `0.11.29` |
| Quality tools | Ruff `0.15.21`、mypy `2.3.0`、pytest `9.1.1`、pytest-cov `7.1.0`、Hypothesis `6.156.6`、pip-audit `2.10.1` |
| OS/architecture | Linux x86_64 local environment |
| Network service | paid API、AWS、Discordへ接続しない |

### 2.4 Local validation results

| ID | Check | Command/observation | Result |
|---|---|---|---|
| `QT-Lock` | Lock | `uv lock --check` | PASS: 43 packages resolved |
| `QT-Lock` | Sync | `uv sync --frozen --all-groups` | PASS: 42 packages checked |
| `QT-Ruff` | Format/lint | `uv run --frozen ruff format --check .`、`ruff check .` | PASS: 10 files formatted、finding 0 |
| `QT-Mypy` | Strict typing | `uv run --frozen mypy` | PASS: source、tests、toolsの10 files、error 0 |
| `UT-DOM-001〜005` | Unit/property test | `uv run --frozen pytest` | PASS: 66 tests |
| `UT-DOM-001〜005` | Domain coverage | pytest-cov line/branch coverage | PASS: domain only 160 statements、30 branches、100% |
| `ST-Dependency` | Dependency audit | hash付きlock exportを`pip-audit --strict`で検査 | PASS: known vulnerability 0 |
| `FEAS-SBOM-001` | Source SBOM feasibility | `uv export --frozen --all-groups --format cyclonedx1.5` | PASS: CycloneDX 1.5、42 components。`/tmp`だけへ出力 |
| `QT-Package` | Package | `uv build --no-sources`、clean venvへwheel install/import | PASS: sdist/wheel build、UUIDv7 import smoke |
| `ST-RepoSurface` | Public safety | `tools/check_public_surface.py` | PASS: 41 public files、finding 0 |
| `QT-Docs` | Documentation | frontmatter/fence/Wiki link、Obsidian mirror byte一致 | PASS: 14 documents |
| `QT-Patch` | Patch hygiene | `git diff --check` | PASS |

### 2.5 SBOM baseline・stage boundary

- 2026-07-16の公開repository SBOM exportはHTTP 200、SPDX 2.3、root packageだけを返した。最初のPRが未mergeでdependency submissionも未実装のため、これは期待したbaselineである。
- 本stepでは`uv.lock`、hash付きdependency audit、package buildを検証し、次step用のCycloneDX 1.5 exportを一時出力で確認した。SBOM fileはcommitせず、workflowやGitHub dependency snapshotも作成しない。
- STEP-02 GitHub CIでPRのCycloneDX 1.5 strict schema/`uv.lock`検証を実装する。Python Dependabot graph jobが完全なmanaged snapshotを生成することを実測したため、custom Dependency Submissionは追加しない。
- STEP-08 Containerで最終ARM64 imageのOS/runtime dependencyを含むSPDX SBOMを生成する。
- STEP-10 Release CI/CDでpush済みimage digestへbuild provenanceとSBOM attestationを別々に生成・検証する。

### 2.6 Findings・resolution

| Finding | Resolution | State |
|---|---|---|
| state transition property testの自己参照で意図しないfailureが発生 | 合法edge集合と生成stateを分離し、全直積を独立検証 | Resolved |
| `zip(strict=True)`の入力長が一致しないtest helper | 型付きcase生成へ変更 | Resolved |
| public surface scanがpublic-safeな用語を誤検知 | 秘密値の検出強度を保ったまま境界とfixtureを修正 | Resolved |
| retryの状態意味が未確定 | FAILEDをimmutable terminalとし、新`AttemptId`を同じdebate/threadに追加する方式へ決定 | Resolved |
| reviewerが生成物名directory内のforce-added fileをpublic scanから回避できることを指摘 | filesystem除外を廃止し、Gitのtracked＋非ignore untracked一覧を検査。`dist/`配下のtracked fixtureを回帰test化 | Resolved |
| scanner変更後の初回Ruff runが`UP022`で失敗 | `stdout`/`stderr`個別指定を`capture_output=True`へ変更して再実行 | Resolved |
| `tools/`をmypy対象へ追加した初回runでmodule名重複を検出 | `tools`を明示package化し、security/document toolをstrict gateへ統合 | Resolved |
| 最終SBOM確認の初回one-linerがshell/Python quoting errorで失敗 | f-stringを使わないparse commandへ修正し、CycloneDX確認とbuildを再実行 | Resolved |
| 文書追加後にmirrorと`docs/README.md`の旧13文書表記が一時不一致 | 14文書へ統一し、正本/mirrorのbyte一致を再検証 | Resolved |

### 2.7 Not executed in this step

- GitHub Actions workflow、managed Dependency Graph照合、required check、CodeQL結果。
- Discord/OpenAI/AWS integration、DynamoDB persistence、quota/lease transaction。
- ARM64 container、container SBOM、artifact attestation、ECR push。
- production deploy、smoke test、cost/SLO observation。

### 2.8 Pull Request・GitHub check evidence

| Field | Result |
|---|---|
| Implementation commit | `942ced5ab388f171ef1d0d638778ff104a7beb4f` |
| Draft Pull Request | https://github.com/pitekusu/shittim-chest/pull/1 |
| Base/head | `main` ← `agent/python-domain-foundation` |
| GitHub managed CodeQL | PASS: `Analyze (python)`と`CodeQL`、implementation commit対象 |
| Project CI workflow | Not implemented; STEP-02で追加する |
| PR creation retry | GitHub App integrationはwrite権限不足で403。認証済みGitHub CLIへfallbackし作成成功 |

merge結果、review finding、evidence追記commit後のcheckはPull Requestを外部証跡とし、merge時または次の同期sliceで追記する。

## 3. STEP-02A Dependabot version updates

### 3.1 Metadata

| Field | Value |
|---|---|
| Step ID | `STEP-02A-DEPENDABOT-VERSION-UPDATES` |
| Status | Operational; uv compatibility correction verified by successful Dependabot jobs |
| Date | 2026-07-17 |
| Branch | `agent/enable-dependabot-version-updates` |
| Base commit | `75f9383d3b356024942465d055a09213da453014` |
| Requirement | GitHub managed dependency maintenance、STEP-02 supply-chain foundation |
| Test ID | `CT-CodeSecurity`、`QT-DependabotConfig`、`ST-RepoSurface`、`QT-Docs`、`QT-Patch` |

### 3.2 Scope and behavior

- `.github/dependabot.yml`でrootの`pyproject.toml`と`uv.lock`を対象に、GitHub Dependabot version updatesを有効化する。
- 毎週月曜09:00 JSTに`uv` ecosystemを確認する。
- minor/patch version updateを`python-minor-and-patch`へgroup化し、major updateは個別Pull Requestとして自動mergeせずreviewする。
- version updateの同時open Pull Requestを最大5件とし、`pitekusu`をassigneeにする。
- security updateはversion update groupへ混在させず、GitHub managed security updateの即時経路を維持する。
- 現時点でworkflow、Dockerfile、npm manifestは存在しないため、GitHub Actions、Docker、npm/CDK ecosystemは各manifest導入stepで追加する。

### 3.3 Official references checked

| Checked | Target | Official reference | Design applied |
|---|---|---|---|
| 2026-07-17 | GitHub Dependabot version updates | https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/secure-your-dependencies/configure-version-updates | default branchの`.github/dependabot.yml`、syntax version 2、ecosystem/directory/schedule必須 |
| 2026-07-17 | Dependabot options | https://docs.github.com/en/code-security/reference/supply-chain-security/dependabot-options-reference | `uv` v0対応、weekly timezone、group、update type、open PR limit |
| 2026-07-17 | GitHub Actions updates | https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/secure-your-dependencies/auto-update-actions | workflow導入時は`github-actions`、directory `/`を追加する |

### 3.4 Validation and activation evidence

| Check | Expected/result |
|---|---|
| YAML parse | PASS: PyYAML 6.0.3でmappingとしてparseし、`version=2`、`uv` entry 1件、必須key、schedule、group、open PR limitをassert |
| Manifest scope | PASS: rootの`pyproject.toml`と`uv.lock`を確認し、workflow、Dockerfile、npm manifestが未実装であることを確認 |
| Existing quality gates | PASS: lock/sync、Ruff、mypy strict、66 tests、domain line/branch 100%、pip-audit finding 0、sdist/wheel build |
| Public safety | PASS: 42 public files、finding 0 |
| Documentation | PASS: Obsidian正本とrepository mirror 14文書がbyte一致 |
| Patch hygiene | PASS: `git diff --check` finding 0 |
| GitHub activation | PASS: default branch `main`からGitHub Contents APIで設定fileと全設定値を取得。公式仕様上、この配置によりversion updatesは有効 |
| Managed security coexistence | PASS: Repository APIでDependabot security updates、Secret scanning、Push protectionが引き続きenabled |
| First scheduled job | NOT YET OBSERVED: Dependabot tabのecosystem/最終実行時刻は公開REST API対象外。次回月曜09:00 JST以降にUIで`uv`とjob logを確認する |

### 3.5 Failed and superseded validation runs

| Run | Result | Resolution |
|---|---|---|
| YAML parser first attempt | FAIL before parse: hostに`ruby` commandなし | system PythonのPyYAML 6.0.3へ切替え、同じsemantic assertionがPASS |
| First sandboxed full gate | YAML assertionはPASS、続く`uv lock --check`がread-only cacheで停止 | 固定lockと既存uv cacheを使うhost validationへ切替え、全gateがPASS |

### 3.6 Pull Request and GitHub evidence

| Field | Result |
|---|---|
| Pull Request | https://github.com/pitekusu/shittim-chest/pull/3 |
| Implementation commit | `58edc3b75073317a76af69fb186bc3150bbef3b5` |
| Squash merge commit | `794ebf6f3397b2008478495d90eb061799afbdd3` |
| Merge result | PASS: `main`へsquash merge、remote feature branch削除 |
| GitHub managed checks | PASS: CodeQL `Analyze (python)`、CodeQL aggregate、GitGuardian Security Checks |
| Review state | PASS: unresolved review thread 0、review/comment 0、merge state CLEAN |
| Runtime impact | application、AWS、Discord、OpenAI、paid serviceの変更なし |

### 3.7 First job failure and corrective validation

2026-07-17の初回Dependabot jobはRuff更新時に`tool_version_not_supported`で失敗した。projectの`[tool.uv].required-version ==0.11.29`に対し、GitHub Dependabot updaterが対応するuvは0.11.8だけであり、dependency resolution開始前に停止していた。Ruff自体や`uv.lock`の不整合ではない。

Failure log: https://github.com/pitekusu/shittim-chest/network/updates/1463693967（repository write権限が必要）。

| Check | Result |
|---|---|
| Dependabot updater source | PASS: `dependabot/dependabot-core` mainの`uv/Dockerfile`が`ghcr.io/astral-sh/uv:0.11.8`をpinしていることを確認 |
| Corrective setting | `required-version = ">=0.11.8,<0.12"`。開発・CI基準0.11.29と`uv_build>=0.11.29,<0.12`は変更しない |
| Release integrity | PASS: uv 0.11.8公式release assetのSHA-256が公式checksumと一致 |
| Lock compatibility | PASS: uv 0.11.8の`uv lock --check`で43 packagesを解決 |
| Update simulation | PASS: uv 0.11.8の`uv lock --dry-run --upgrade-package ruff`でRuff 0.15.21から0.15.22への更新を解決 |
| Local baseline | PASS: uv 0.11.29 lock/sync、Ruff、mypy strict、66 tests、domain line/branch 100%、pip-audit finding 0、package build、public surface 42 files/finding 0、14文書mirror |
| Corrective Pull Request | PASS: https://github.com/pitekusu/shittim-chest/pull/5 をsquash merge。merge commit `b11b291` |
| Corrective PR checks | PASS: CodeQL `Analyze (python)`、CodeQL aggregate、GitGuardian、unresolved thread 0 |
| GitHub retry policy | 旧runのActions rerunは同じ`GITHUB_SHA`を使うため実行しない。Dependabot設定file更新をmergeし、修正後mainを対象とする新規jobでsuccessと更新PRを確認する |
| Corrected-main job | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29515760753、head `b11b291`、conclusion `success` |
| Latest-config job | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29515811429、head `65e8653`、conclusion `success` |
| Generated update | PASS: https://github.com/pitekusu/shittim-chest/pull/7 でRuff 0.15.21から0.15.22へのgrouped updateを作成し、個別検証後にsquash merge |
| Verification Pull Request | PASS: https://github.com/pitekusu/shittim-chest/pull/6 をsquash merge。merge commit `65e8653`、CodeQL/GitGuardian合格、unresolved thread 0 |

### 3.8 Ruff 0.15.22 update validation

| Field | Result |
|---|---|
| Pull Request | https://github.com/pitekusu/shittim-chest/pull/7 |
| Inspected head | `762d46faa750dd2d49c656eb6c7393ebf1cd01b6`。Dependabot rebase後の最新headをcurrent mainへ重ねて検証 |
| Official release | https://github.com/astral-sh/ruff/releases/tag/0.15.22、2026-07-16公開、prereleaseでない |
| Change assessment | preview rule、限定的なbug/rule fix、parser性能改善。projectはpreviewを有効化していないため、新preview ruleはgateに追加されない |
| Lock and install | PASS: `uv lock --check`、`uv sync --frozen --all-groups`、runtime `ruff 0.15.22` |
| Quality | PASS: Ruff format/check、mypy strict、66 tests、domain line/branch 100% |
| Security/package | PASS: pip-audit finding 0、sdist/wheel build、public surface 42 files/finding 0 |
| Documentation | PASS: Obsidian正本とrepository mirror 14文書がbyte一致 |
| GitHub checks | GitGuardian PASS、CodeQL NEUTRAL（dependency manifest/lockのみでanalyzable source変更なし）、unresolved thread 0 |
| Merge | PASS: squash merge commit `453c4fe36cd38de8ee904eae1bb48bd46ae72815` |

Superseded validation: Dependabotがrebase中であったため、初回取得head `ddca3aa`は取得直後に`762d46f`へ更新された。旧SHAのcherry-pickはobject未取得で停止し、作業tree変更なし。最新head/baseを再確認して全gateを実行した。

Superseded run: 互換試験artifact展開directoryで初回`uv lock --check`を実行し、`pyproject.toml`未検出で停止した。repository rootで同じchecksum検証済みbinaryを再実行し、上記lock/update試験が合格した。

Superseded lookup: Dependabot公式sourceの初回GitHub code searchはform fieldによりPOSTとなりHTTP 404で失敗した。GETを明示して再実行し、`uv/Dockerfile`のpinを確認した。

## 4. STEP-02 GitHub CI・managed SBOM

### 4.1 Metadata

| Field | Value |
|---|---|
| Step ID | `STEP-02-GITHUB-CI-MANAGED-SBOM` |
| Status | Completed: merged、main CI、managed SBOM comparison、Ruleset protection passed |
| Date | 2026-07-17 |
| Branch | `agent/implement-step-02-ci` |
| Base commit | `26e7a5063aa8705ea7fdd0220fe1809b2d8ac599` |
| Requirement | RQ-009、RQ-011、RQ-015 |
| Test ID | QT-Lock、QT-Ruff、QT-Mypy、UT-DOM-001〜005、ST-Dependency、ST-RepoSurface、FEAS-SBOM-001、CT-SBOM-Graph、QT-Package、QT-Docs、QT-Workflow、QT-Patch |

### 4.2 Implemented scope

- `.github/workflows/ci.yml`に`quality`、`tests`、`security`、`package`、`docs-public-safety`を実装した。PR、main push、manualで全5 jobを実行し、workflow permissionは`contents: read`だけとする。
- PR workflowはGitHub-hosted `ubuntu-latest`だけを使い、secret、OIDC、Environment、AWS/Discord/OpenAI接続、`pull_request_target`を使用しない。PRの旧runだけをcancelし、main runは固有groupで完走させる。
- checkout v7.0.0、setup-uv v8.3.2、upload-artifact v7.0.1、dependency-review-action v5.0.0を公式releaseのfull commit SHAへ固定した。Dependabotの`github-actions` ecosystemで同一行version commentごと更新する。
- Gitleaks 8.30.1とactionlint 1.7.12は公式release archiveのversionとSHA-256を固定する。実行時latestや追加Action allowlistに依存しない。
- `cyclonedx-python-lib[json-validation] 11.11.0`を直接dev dependencyとし、uv CycloneDX 1.5 preview出力を公式strict JSON Schemaで検査する。root project、`uv.lock`の全PyPI package、duplicate/unknown ref、unsupported sourceもfail closedにする。
- source SBOMをPR/main CI artifactとして30日保存する。release image SBOMとattestationはSTEP-08/10へ分離する。
- Python Dependabot graph jobをGitHub managed inventoryとして採用し、custom Dependency Submissionと`contents: write`を使用しない。`dependency-graph.yml`でDependabotと毎時開始時の混雑を避けた毎週火曜12:17 JST/manualにGitHub SPDX 2.3とstable mainのCycloneDX/`uv.lock`を照合する。SPDX exportにcommit SHAがない制約は比較前後のmain SHA確認で補い、main移動時はworkflowを明示失敗させて再実行する。反映遅延は30秒間隔・最大10回のbounded pollingとする。
- `tools/check_docs.py`で14文書のfile set、UTF-8/LF、frontmatter、calendar date、fence、Wiki file/heading、path traversal、公式資料記録を検査する。private Vaultのbyte一致はlocalだけで検査し、CIへVault accessを与えない。
- package jobは生成wheelが1件であることを確認し、別の一時環境へ明示wheelを`--no-index --only-binary`でinstallする。runtime dependency追加時にwheelhouseが不完全なら安全側に失敗する。source tree外から`-I` importし、version、site-packages path、`py.typed`を検査する。

### 4.3 Latest official references

| Checked | Target | Official reference | Applied result |
|---|---|---|---|
| 2026-07-17 | GitHub secure Actions | https://docs.github.com/en/actions/reference/security/secure-use | read-only permission、GitHub-hosted runner、full SHA、fork境界 |
| 2026-07-17 | Python Dependabot graph jobs | https://docs.github.com/en/code-security/concepts/supply-chain-security/dependency-graph-data | full transitive managed graphを使用しcustom submissionを省略 |
| 2026-07-17 | GitHub SBOM export | https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/export-dependencies-as-sbom | SPDX 2.3を週次/manualでread-only取得 |
| 2026-07-17 | Dependency Review | https://docs.github.com/en/code-security/concepts/supply-chain-security/dependency-review | Public PRでlow以上をfail |
| 2026-07-17 | uv GitHub Actions | https://docs.astral.sh/uv/guides/integration/github/ | setup-uv full SHA、uv/Python version固定、frozen sync |
| 2026-07-17 | uv CycloneDX export | https://docs.astral.sh/uv/concepts/projects/export/ | preview出力をstrict schemaとlock inventoryで保護 |
| 2026-07-17 | CycloneDX Python library 11.11.0 | https://pypi.org/project/cyclonedx-python-lib/ | `json-validation` extraでCycloneDX 1.5 strict schema検証 |
| 2026-07-17 | GitHub Pull Request merge | https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/incorporating-changes-from-a-pull-request/merging-a-pull-request | inspected head SHAへ固定したsquash mergeとbranch削除 |
| 2026-07-17 | GitHub Ruleset rules | https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/available-rules-for-rulesets | source App固定required check、strict policy、CodeQL merge protection |
| 2026-07-17 | GitHub Rules REST API | https://docs.github.com/en/rest/repos/rules | 既存rulesetを保持した更新payloadと有効rule再取得 |

### 4.4 Local validation results

| ID | Check | Result |
|---|---|---|
| `QT-Lock` | `uv lock --check`、`uv sync --frozen --all-groups` | PASS: 61 package records resolved、60 registry packages checked |
| `QT-Ruff` | format/check | PASS: 14 Python files、finding 0 |
| `QT-Mypy` | strict typing | PASS: source、tests、toolsの14 files、error 0 |
| `UT-DOM-001〜005` | full pytest | PASS: 92 tests、domain only line/branch 100% |
| `ST-Dependency` | hash付きlock export＋pip-audit strict | PASS: 60 packages、known vulnerability 0 |
| `ST-RepoSurface` | public surface | PASS: 48 files、finding 0 |
| `ST-Secret` | Gitleaks 8.30.1 full history | PASS: implementation commit後の21 commits、約676 KB、leak 0 |
| `FEAS-SBOM-001` | CycloneDX strict schema/project/lock/ref | PASS: CycloneDX 1.5、60 dependency components、inventory差分0 |
| `CT-SBOM-Graph` | current mainのGitHub SPDXとmain lockを実APIで比較 | PASS: SPDX 2.3、42 external dependency＋optional project packageが一致。repository rootは比較対象外 |
| `QT-Package` | sdist/wheel、isolated explicit-wheel import | PASS: wheel 1件、installed metadata 0.1.0、site-packages、`py.typed` |
| `QT-Docs` | public structure＋local canonical mirror | PASS: 14 documents、Wiki heading/pathを含む構造finding 0、byte差分0 |
| `QT-Workflow` | actionlint 1.7.12 | PASS: 2 workflow、finding 0 |
| `QT-Patch` | `git diff --check` | PASS |

### 4.5 Failed and superseded local runs

| Run | Result | Resolution |
|---|---|---|
| Initial `uv lock`/`uv sync` in restricted network | PyPI DNS resolutionで停止 | 承認済みnetwork実行でlockを更新・同期し、frozen再検証 |
| First Ruff check | 新tool 2件のformat/import/pattern finding | formatterと明示修正後に全Ruff gate合格 |
| Tool testだけを指定したpytest | domain coverage対象がimportされず0%で終了 | tool test自体21件は成功。追加回帰testを含む正式なfull suite 92件でdomain 100%を確認 |
| `python tools/check_docs.py` | package import path不足 | 公開commandを`python -m tools.check_docs`へ統一 |
| Public surface first run | test fixture内のversioned purlをemailとして安全側検出 | fixtureをruntime結合にし、credential検出patternを弱めず解消 |
| YAML parser check | hostにRubyがなく実行不能 | 公式actionlint 1.7.12 archive/checksumを検証して2 workflowを解析 |

### 4.6 Pull Request and initial GitHub evidence

| Field | Result |
|---|---|
| Implementation commit | `7b06d2c3c7d5d8f6a10db735c8eec1da0341cc41` |
| Evidence update commit | `10296b56757982791de31a04298970b68b9d06d2` |
| Pull Request | https://github.com/pitekusu/shittim-chest/pull/10 |
| Base/head | `main` ← `agent/implement-step-02-ci` |
| Initial project CI run | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29545080084 |
| Evidence-update CI run | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29545197363 |
| Latest stable CI jobs | PASS: `quality` 12秒、`tests` 14秒、`security` 25秒、`package` 8秒、`docs-public-safety` 9秒 |
| Latest managed security | PASS: CodeQL `Analyze (python)` 46秒、CodeQL aggregate、GitGuardian Security Checks |
| Workflow permission boundary | PASS: repository default tokenはread-only、PR workflowは`contents: read`だけ。selected Actionsとfull SHA pin必須設定を維持 |
| Pull Request state | Ready、mergeable、merge state `CLEAN`。初回headとevidence更新headの全check成功後にDraftを解除 |

`security` job内でDependency Review、pip-audit、Gitleaks、CycloneDX strict validation、source SBOM uploadまで成功した。GitHub Actions artifactはrunに紐付く30日保持の一時成果物であり、release provenanceではない。

### 4.7 Merge and repository acceptance

| Field | Result |
|---|---|
| Final PR head | `ec027693f566dd8f93b00e8db2b53c1bb4739d14`。全5 CI job、CodeQL、GitGuardian合格、review/comment/unresolved thread 0、merge state `CLEAN` |
| Squash merge | PASS: PR `#10`をinspected headへ固定してmerge。main commit `e2fdaad43b21f70fd60fe4ba77021ede38fc5e9a`、remote feature branch削除 |
| Main CI | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29545502266 |
| Main CodeQL | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29545501977 |
| Managed Dependency Graph update | PASS: main commit対象のGitHub managed job https://github.com/pitekusu/shittim-chest/actions/runs/29545504472 |
| Managed SBOM comparison | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29545563028。`verify-managed-sbom`が15秒で成功し、source CycloneDXとGitHub SPDXをartifact化 |
| Required checks | PASS: `quality`、`tests`、`security`、`package`、`docs-public-safety`をGitHub Actions App ID `15368`へ固定し、strict policyを有効化 |
| Code scanning protection | PASS: tool `CodeQL`、security `high_or_higher`、analysis alert `errors`をmerge blockerに設定 |
| Existing main protection | PASS: PR必須、squash限定、conversation resolution、linear history、branch削除/force-push禁止、bypass actorなしを維持 |
| Effective-rule verification | PASS: branch effective-rules APIでRuleset `19047946`の全ruleを再取得。`current_user_can_bypass=never` |

direct push、偽装した同名check、fork PR、同一PR連続push cancellationの破壊的または追加PRを要するnegative testは本merge操作では実施しない。Ruleset APIのeffective rule、App ID固定、PR実走をacceptance evidenceとし、fork受入れ開始時またはsecurity hardening sliceで安全な専用PRを使って追加検証する。`cdk`と`container-arm64`は未実装のためrequired checkへ追加しない。

## 5. STEP-02B Betterleaks段階移行

### 5.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-02B-BETTERLEAKS-MIGRATION` |
| Status | Completed and merged |
| Date | 2026-07-17 |
| Branch/base | `agent/implement-step-02b-betterleaks` / `main` `860e298` |
| Implementation commit | `514df7ace9ce965e89615f69aeace841a4c3aa13` |
| Requirement | Secret detectionの保守継続性、supply-chain provenance、Public repository安全性 |
| Test IDs | `ST-Secret-Parallel`、`CT-Secret-Positive`、`CT-Secret-Negative`、`ST-Betterleaks-Provenance`、`OT-Tool-Version`、`QT-Workflow` |

scopeはBetterleaks/Gitleaks並行scan、release provenance検証、生成fixture contract、release tool pinの集中管理、週次latest検知、文書化である。既存`security` required check名、Gitleaks gate、GitHub managed Secret scanning/Push protectionは維持する。application code、dependency、AWS、Discord、OpenAI、production workflowは変更しない。

### 5.2 Confirmed implementation policy

- Betterleaks 1.6.1とGitleaks 8.30.1をfetch-depth 0の同じcheckoutへ実行し、いずれか一方のfindingでも`security`を失敗させる。
- 両scannerで`--redact`を使用する。Betterleaksの`--validation`は使用せず、検出候補をGitHub、AWS、OpenAI等の外部providerへ送信しない。
- Betterleaks archive、checksums、Sigstore bundleは`.github/tool-versions.json`の固定SHA-256へ照合する。cosign 3.0.6でrelease workflow identityとGitHub Actions issuerを検証し、署名済みchecksumのarchive digestを再照合する。
- positive fixtureはsourceやrepositoryへcredential文字列を保存せず実行時に生成し、negative fixtureと別の一時Git historyへcommitする。report本文は表示せず件数だけ評価する。
- weekly jobはread-only GitHub tokenでlatest stable release tagを確認し、差分時に失敗する。自動download/update/mergeはしない。
- Gitleaks撤去はこのsliceの範囲外とし、観測期間と別ADRを必須とする。

### 5.3 Latest official references

| Checked | Target | Official reference | Applied result |
|---|---|---|---|
| 2026-07-17 | Betterleaks 1.6.1 | https://github.com/betterleaks/betterleaks | latest release、Git scan、redaction、Gitleaks compatibilityを確認 |
| 2026-07-17 | Betterleaks scanning | https://github.com/betterleaks/betterleaks/blob/main/docs/scanning.md | full-history command、JSON report、validation opt-inを確認 |
| 2026-07-17 | Betterleaks security policy | https://github.com/betterleaks/betterleaks/blob/main/.github/SECURITY.md | latest releaseだけがsupport対象のためweekly検知を追加 |
| 2026-07-17 | Gitleaks status | https://github.com/gitleaks/gitleaks | feature complete/security patches継続、Betterleaksへ開発注力を確認 |
| 2026-07-17 | cosign verify-blob | https://docs.sigstore.dev/cosign/verifying/verify/ | certificate identity、OIDC issuer、bundle検証へ反映 |
| 2026-07-17 | Sigstore CI verification | https://docs.sigstore.dev/quickstart/quickstart-ci/ | CIでのkeyless signature verification境界へ反映 |

### 5.4 Interim local validation

| ID | Check | Result |
|---|---|---|
| Tool pin schema | `check_tool_versions.py validate` | PASS: actionlint、Betterleaks、Gitleaksの3 pin、field/version/digest/identity整合 |
| `OT-Tool-Version` | GitHub Releases latest API | PASS: actionlint 1.7.12、Betterleaks 1.6.1、Gitleaks 8.30.1がlatest stableと一致 |
| `CT-Secret-Positive` | runtime生成token形式を各一時Git historyへcommit | PASS: Betterleaks/Gitleaksともstatus 1、finding 1件以上 |
| `CT-Secret-Negative` | safe placeholderだけの一時Git history | PASS: Betterleaks/Gitleaksともstatus 0、finding 0 |
| `ST-Secret-Parallel` | implementation commitを含むfull history 27 commits、約830 KB | PASS: Betterleaks約0.22秒、Gitleaks約0.14秒、両方finding 0 |
| `QT-Workflow` | actionlint 1.7.12 | PASS: `ci.yml`と`tool-versions.yml`を含むworkflow finding 0 |
| Public surface | repository file scan | PASS: 54 files、credential/実ID/home path finding 0 |
| `ST-Betterleaks-Provenance` | 公式release assetとSigstore bundleの実地検証 | PASS: cosign 3.0.6、archive/checksums/bundleの固定SHA-256、certificate identity、GitHub Actions OIDC issuer、署名済みarchive digestが一致し`Verified OK` |
| `QT-Ruff` / `QT-Mypy` | 全source/test/tool | PASS: 18 Python files、format/lint finding 0、strict type error 0 |
| Full pytest | domainとtool regression | PASS: 100 tests、domain line/branch coverage 100% |
| `ST-Dependency` | hash付きlock export＋pip-audit strict | PASS: known vulnerability 0 |
| `FEAS-SBOM-001` | CycloneDX 1.5 strict schema/project/lock/ref | PASS: 60 dependency components、inventory差分0 |
| Package | sdist/wheel build | PASS: `shittim_chest-0.1.0`のsdistとwheelを生成 |
| Docs mirror | Obsidian正本とrepository `docs/` | PASS: 14 documents、byte差分0、構造finding 0 |
| Selected Actions | repository API read/update/read | PASS: 既存5 patternを保持し`sigstore/cosign-installer@*`だけを追加。GitHub-owned許可、verified全許可なしを維持 |

restricted environmentでtool test fileだけを指定したpytestは、対象domainがimportされずcoverage 0%として終了した。tool test自体は全件passしており、正式判定はdomainを含むfull suite 100件・domain coverage 100%で合格した。pip-auditの最初のrestricted runはtemporary environmentのpip更新に必要なnetworkへ接続できず失敗し、承認済みnetwork実行でknown vulnerability 0を確認した。PR CIとmain観測結果はpush後に本節へ追記する。

### 5.5 Pull Request CI findings

| Field | Result |
|---|---|
| Pull Request | https://github.com/pitekusu/shittim-chest/pull/12、Draft |
| Initial head | `6513536e21b9de64ec89edaf2b3125d8aed515e8` |
| Initial CI run | https://github.com/pitekusu/shittim-chest/actions/runs/29547537661 |
| Passed | `quality`、`tests`、`security`、`package`。`security`内でBetterleaks provenance、並行scan、generated contract、Dependency Review、audit、SBOMが合格 |
| `docs-public-safety` failure | actionlint `-version`がversionに続けてbuild情報を複数行返し、出力全体とpinの比較が不一致。先頭行だけを比較するよう修正 |
| CodeQL finding | generated contractが疑似tokenを一時worktree fileへ書く処理をclear-text storageとして検出。抑制せず、worktreeへ書かずstdinからtemporary Git object/index/commitを構成する方式へ変更 |
| Fixed head | `91e1838`。generated contractとfull-history scanを再実行し、29 commits、約833 KB、両scanner finding 0 |
| Successful CI run | https://github.com/pitekusu/shittim-chest/actions/runs/29547746351。`quality` 13秒、`tests` 11秒、`security` 31秒、`package` 11秒、`docs-public-safety` 9秒で全合格 |
| Successful CodeQL run | https://github.com/pitekusu/shittim-chest/actions/runs/29547745022。Actions/Python分析とCodeQL aggregateが全合格し、修正headの新規finding 0 |
| External security check | GitGuardian Security Checks合格 |

どちらも実credentialやproduction dataの漏えいではないが、required gateを弱めず実装を修正した。同じcontract、full local gate、PR CI、CodeQLを再実行し、全て合格した。

### 5.6 Merge・main observation

| Field | Result |
|---|---|
| Final head | `18b307c47afdbe997ff04f740fb190647051b6d0`、required check全合格、unresolved thread 0、merge state `CLEAN` |
| Squash merge | PASS: PR `#12`を確認済みheadへ固定してmerge。main commit `942758f1fd2d640a72e356ae356a1eb064bd4166`、remote feature branch削除 |
| Main CI | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29548049336 |
| Main CodeQL | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29548048637。Actions/Python分析が合格 |
| Release Tool Versions | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29548060162。main上で手動実行し、3 pinがlatest stableと一致 |

PRの複数headとmainで、Betterleaks provenance、full-history、positive/negative contract、redaction、provider validation無効、version監視が全て成功した。Gitleaks公式はfeature completeで今後はsecurity patchのみ、Betterleaksへ注力すると明記しており、Betterleaks公式はlatest releaseのみsecurity support対象としている。この観測をSTEP-02Cの撤去判断根拠とする。

## 6. STEP-02C Gitleaks撤去

### 6.1 Metadata・decision

| Field | Value |
|---|---|
| Step ID | `STEP-02C-RETIRE-GITLEAKS` |
| Status | Completed |
| Date | 2026-07-17 |
| Branch/base | `agent/retire-gitleaks` / `main` `942758f` |
| Implementation commit | `f1d0d4b019da949841ae26018cd9c7d85f57b395` |
| Requirement | Secret detectionの保守継続性、CI保守負担削減、required check継続 |
| Test IDs | `ST-Secret-Betterleaks`、`CT-Secret-Positive`、`CT-Secret-Negative`、`ST-Betterleaks-Provenance`、`OT-Tool-Version`、`QT-Workflow` |

Gitleaks binary pin、download、full-history実行、generated contract引数、latest release監視を撤去する。Betterleaksの署名/digest検証、full-history、redaction、generated positive/negative contract、provider validation無効、GitHub managed Secret scanning/Push protection、既存`security` check名は維持する。AWS、Discord、OpenAI、application dependency、production resourceは変更しない。

### 6.2 Latest official references・accepted trade-off

| Checked | Target | Official reference | Applied result |
|---|---|---|---|
| 2026-07-17 | Betterleaks 1.6.1 | https://github.com/betterleaks/betterleaks | latest release、active development、Git scan、redaction、validation機能を再確認 |
| 2026-07-17 | Betterleaks security policy | https://github.com/betterleaks/betterleaks/blob/main/.github/SECURITY.md | latest releaseのみsupportのためweekly fail-closed監視を維持 |
| 2026-07-17 | Gitleaks project status | https://github.com/gitleaks/gitleaks | feature complete、今後security patchのみ、Betterleaksへ注力を再確認 |

第二scannerによる異種実装の検出差分というdefense-in-depthは失う。一方、両者は同じmaintainer系譜で、STEP-02Bの対象履歴とfixtureでは検出差分がなく、Gitleaksは新機能を受けない。Betterleaksの署名付きlatest release、固定digest、CI contract、GitHub managed secret protectionを維持する方が更新・download・障害点を減らせるため、MVPでは単独化を採用する。具体的なcoverage gapが再現した場合は第二scannerを別ADRで再導入する。

### 6.3 Local validation

| ID | Check | Result |
|---|---|---|
| Active Gitleaks surface | `.github/`、`tools/`、`tests/`、`README.md`をcase-insensitive検索 | PASS: active pin、download、command、argument、version監視0件。履歴文書と撤去判断だけに名称を保持 |
| Tool pin schema | `check_tool_versions.py validate` | PASS: actionlintとBetterleaksの2 pin |
| `OT-Tool-Version` | GitHub Releases latest API | PASS: 2 pinがlatest stableと一致 |
| `CT-Secret-Positive` / `CT-Secret-Negative` | Betterleaks単独generated contract | PASS: positiveはstatus 1/finding 1件以上、negativeはstatus 0/finding 0 |
| `ST-Secret-Betterleaks` | implementation commitを含むfull history、redaction有効 | PASS: 約892 KB、finding 0、provider validation未使用 |
| `QT-Ruff` / `QT-Mypy` | 全source/test/tool | PASS: 18 Python files、format/lint finding 0、strict type error 0 |
| Full pytest | domainとtool regression | PASS: 100 tests、domain line/branch coverage 100% |
| `ST-Dependency` | hash付きlock export＋pip-audit strict | PASS: known vulnerability 0 |
| `FEAS-SBOM-001` | CycloneDX 1.5 strict schema/project/lock/ref | PASS: 60 dependency components、inventory差分0 |
| Package | sdist/wheel build | PASS: `shittim_chest-0.1.0`のsdistとwheelを生成 |
| Docs/public surface | structure、credential/identifier、mirror source | PASS: 14 documents、public finding 0 |
| `QT-Workflow` | actionlint 1.7.12 | PASS: workflow finding 0 |

最初の`ci.yml`編集では単一行`run`のYAML quote境界が不正でactionlintがparse errorを検出した。block scalarへ変更して再実行し、finding 0を確認した。required check名、selected Actions allowlist、repository permission、GitHub managed security設定は変更していない。

### 6.4 Pull Request・main validation

| Target | Result |
|---|---|
| PR | [#13 Retire Gitleaks scanner](https://github.com/pitekusu/shittim-chest/pull/13)をhead `70cb6d0240d82ddbd1cd718fd50a86587c1550ed`で検査し、squash merge |
| PR checks | PASS: `quality`、`tests`、`security`、`package`、`docs-public-safety`、CodeQL Actions/Python、GitHub CodeQL、GitGuardian。未解決review thread 0 |
| Main commit | `d0f601af102b7ff9d395cd6dcdb2fd5c43959b02` |
| Main CI | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29548428650 |
| Main CodeQL | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29548428333 |
| Release Tool Versions | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29548478470。Gitleaksを除いたactionlint/Betterleaksの2 pinを検証 |

PR/mainの全gateと更新監視が成功したため、STEP-02Cを完了とする。active CI/tool surfaceにGitleaksは残さず、名称は過去の検証証跡と撤去判断にだけ保持する。次の実装sliceはSTEP-03 Application coreとする。

## 7. STEP-03 Application core

### 7.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-03-APPLICATION-CORE` |
| Status | Completed・PR `#15` squash merged |
| Date | 2026-07-17 |
| Branch/base | `agent/step-03-application-core` / `main` `1676928` |
| Requirements | RQ-002、RQ-004、RQ-011、RQ-017 |
| Test IDs | `UT-APP-001`〜`UT-APP-006`、`QT-Ruff`、`QT-Mypy`、`QT-Layer`、`ST-Dependency`、`FEAS-SBOM-001`、`QT-Package`、`ST-RepoSurface`、`QT-Docs`、`QT-Workflow` |

投票domain、immutable application model、Protocol、`accept_debate`、`run_debate`、`cancel_debate`、`retry_debate`、`resume_recoverable`、phase/session deadline、TaskGroup並列化、process cancellation checkpoint、保存済みartifact再利用を実装した。`EvidenceService`で検索と生成を分離し、`CandidateOrderer`で候補順random化を注入可能にした。repository writeはexpected snapshotを要求するため、cancelや別workerとの競合を後続の条件付きDynamoDB writeへ写像できる。

Discord/OpenAI/AWS SDK、DynamoDB serializer/transaction/lease/outbox、実Bot、container、CDK、cloud resource、paid APIは変更・作成していない。`DiscordPublisher`はSTEP-04 outboxで具体operation型を確定するProtocol境界だけを定義した。

### 7.2 Latest official references

| Checked | Version | Official reference | Applied result |
|---|---|---|---|
| 2026-07-17 | Python 3.14.6 asyncio | https://docs.python.org/3.14/library/asyncio-task.html | TaskGroupの子失敗時兄弟cancel、timeout、process `CancelledError`再送出 |
| 2026-07-17 | Python 3.14.6 typing | https://docs.python.org/3/library/typing.html | runtime `isinstance`を使わないstructural Protocol |
| 2026-07-17 | Python 3.14.6 UUID | https://docs.python.org/3.14/library/uuid.html | 既存UUIDv7 ID generator境界を維持 |
| 2026-07-17 | pytest-asyncio 1.4.0 | https://pypi.org/project/pytest-asyncio/ | Python 3.14対応を確認しstrict modeで導入 |
| 2026-07-17 | import-linter 2.13 | https://pypi.org/project/import-linter/ | `application -> domain` layer contractをCI `quality`へ追加 |

### 7.3 Local validation

| ID | Check | Result |
|---|---|---|
| `QT-Lock` | `uv lock --check`、全group frozen sync | PASS: 65 package resolve、64 package check。pytest-asyncio 1.4.0、import-linter 2.13、grimp 3.15をlock |
| `QT-Ruff` | format/check | PASS: 30 Python files、format/lint finding 0 |
| `QT-Mypy` | strict | PASS: 30 source files、error 0、未承認ignore 0 |
| `QT-Layer` | import-linter | PASS: 10 modules、14 imports、`application -> domain` contract 1 kept/0 broken |
| `UT-APP-001`〜`006` | pytest-asyncio strict、Hypothesis、既存回帰 | PASS: 142 tests。application service 91%、domain voting 98%、全対象line/branch 95.30% |
| Timeout/cancellation | phase/session timeout、TaskGroup子失敗、external cancel | PASS: error code分離、兄弟task cancel、同phase checkpoint、`CancelledError`再送出 |
| Voting | self/score/duplicate/incomplete/tie/order/candidate set | PASS: Pythonだけで検証・winner決定、完全同点は`participant-b/a/c`順 |
| `ST-Dependency` | hash付きlock export＋pip-audit strict | PASS: known vulnerability 0 |
| `FEAS-SBOM-001` | CycloneDX 1.5 strict schema/project/lock/ref | PASS: 64 dependency components、inventory差分0 |
| `QT-Package` | sdist/wheel build＋隔離install/import | PASS: 1 wheel、version、installed path、`py.typed`、application importを確認 |
| `ST-RepoSurface` / Betterleaks | public surface、generated contract、working tree/history scan | PASS: 67 public files finding 0、positive/negative contract合格、約914.47 KB leak 0 |
| `QT-Docs` | Markdown structure、Obsidian mirror | PASS: 14文書valid・byte一致 |
| `QT-Workflow` | actionlint、patch whitespace | PASS: finding 0、既存required check名を維持 |

### 7.4 Resolved findings・remaining boundary

- 初回testでartifact付きsnapshotをstate専用helperへ渡す型不整合を検出した。`_replace_snapshot(expected, updated)`と`_replace_state`を分離し、mypy strictと正常系8 phaseで修正を確認した。
- nested phase timeoutがouter session timeoutと同じ`TimeoutError`になる挙動を検出した。内部markerへ変換して`phase_deadline_exceeded`と`session_deadline_exceeded`を区別した。
- mypyがtest fakeを異なるnamespace package名で二重検出したため、`tests`と`tests/unit`へpackage markerを追加し、30 files error 0を確認した。
- STEP-03 fake repositoryはexpected snapshot conflictとattempt historyだけを再現する。fencing token、transaction、3 lease slot、outbox整合性、schema migrationはSTEP-04とDynamoDB Local試験で確定する。
- SDK response、Discord payload、OpenAI schemaのcontractは未実装であり、adapter stepまで推測で確定しない。

### 7.5 Pull Request validation

| Target | Result |
|---|---|
| Draft PR | [#15 Implement STEP-03 application core](https://github.com/pitekusu/shittim-chest/pull/15) |
| Implementation head | `70ef84ab6bc4a18002cfb4f9f989502a3f7584d7` |
| CI | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29549907740。`quality`、`tests`、`security`、`package`、`docs-public-safety` |
| CodeQL | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29549906355。Actions/Pythonとも成功 |
| Managed checks | PASS: GitHub CodeQL、GitGuardian |
| Merge state | PASS: squash merge commit `34ccc541d0f3e6095b5842302e8be3c843cbaab4`、remote branch削除、`main` clean |

## 8. STEP-04A Persistence contract・serialization

### 8.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-04A-PERSISTENCE-CONTRACT` |
| Status | Completed・PR `#16` squash merged |
| Date | 2026-07-17 |
| Branch/base | `step-04a-persistence-contracts` / `main` `34ccc54` |
| Requirements | RQ-003、RQ-004、RQ-005、RQ-006、RQ-011、RQ-017 |
| Test IDs | `CT-PERSIST-001`〜`006`、`UT-APP-001`〜`006`、`QT-Ruff`、`QT-Mypy`、`QT-Layer`、`ST-Dependency`、`FEAS-SBOM-001`、`QT-Package`、`ST-RepoSurface`、`QT-Docs` |

Guild/channelとDiscord operation IDを失わないapplication snapshot、debate/attempt作成時刻、optional starter/thread ID、3 slotを型で制限する`LeaseGrant`を追加した。repository Protocolはpersisted resultを返し、accept/cancel/retryのoperation ID再送、別入力へのID再利用拒否、recoverable claim、lease renewalを表現する。

`adapters/dynamodb`へboto3非依存のnative-value serializerを追加し、Debate META、Attempt META、Evidence、初回意見、最終案、投票、決定事項をPK/SKで縦分割する。thread/recoverable GSI属性、schema 1→2 up-convert、未知schema/壊れたrecordのfail closed、outbox/panel recordの双方向変換、22文字nonce、SHA-256、claim/sent不変条件、400KB事前拒否を実装した。

### 8.2 Latest official references

| Checked | Target | Official reference | Applied result |
|---|---|---|---|
| 2026-07-17 | DynamoDB data types | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.NamingRulesDataTypes.html | string/int/bool/null/list/mapのnative値、UTF-8、400KB境界 |
| 2026-07-17 | Item size | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/CapacityUnitCalculations.html | 属性名、UTF-8値、number、list/map overheadを含むSDK呼出し前検査 |
| 2026-07-17 | TransactWriteItems | https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_TransactWriteItems.html | 同一item二重action禁止、10分tokenへ長期冪等性を依存しない契約 |
| 2026-07-17 | DynamoDB Local usage notes | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.UsageNotes.html | Localがtransaction conflictを再現しないためSTEP-04BでSDK stubを併用 |

### 8.3 Local validation

| ID | Check | Result |
|---|---|---|
| `QT-Lock` | `uv lock --check` | PASS: 65 packages resolved、dependency変更なし |
| `QT-Ruff` | Ruff format/check | PASS: 37 Python files、finding 0 |
| `QT-Mypy` | mypy strict | PASS: 37 source files、error 0、未承認ignore 0 |
| `QT-Layer` | import-linter | PASS: `adapters -> application -> domain`方向を維持 |
| `CT-PERSIST-001`〜`006` | serializer、migration、outbox/panel、operation再送 | PASS: 11 DynamoDB contract tests、AWS/network接続なし |
| Regression | full pytest | PASS: 155 tests、domain/application line/branch 93.57% |
| `ST-Dependency` | hash付きlock export＋pip-audit strict | PASS: known vulnerability 0 |
| `FEAS-SBOM-001` | CycloneDX 1.5 | PASS: 64 dependency components、lock inventory一致 |
| `QT-Package` | sdist/wheel build＋隔離install/import | PASS: 1 wheel、version、installed path、`py.typed`、DynamoDB adapter importを確認 |
| `ST-RepoSurface` | public surface | PASS: credential、実Guild/persona、絶対home path finding 0 |
| `QT-Docs` | public mirror structure | PASS。Obsidian byte同期は文書反映後に再検証する |

### 8.4 Failed・superseded runs and boundary

- 初回application-only pytestは対象を絞ったためglobal coverage 83.10%で終了した。全test suiteで再実行し、155 tests、93.57%を確認した。試験failureではなくcoverage対象選択の誤りである。
- sandbox内の既定uv cacheがread-onlyで初回`uv` commandが停止した。`UV_CACHE_DIR=/tmp/uv-cache`へ限定して同じfrozen lockを再実行した。
- sandbox内pip-auditはtemporary environmentのpip upgrade時にnetwork制約で停止した。network許可後に同じhash付きlock exportで再実行し、known vulnerability 0を確認した。
- STEP-04Aはboto3/botocore依存、API request、transaction expression、DynamoDB Local、AWS table/resourceを実装しない。これらはSTEP-04Bで実装・検証する。
- 400KB検査は属性名、UTF-8値、number size、list/map overheadをAWS公式計算規則に従って合算し、上限超過を事前拒否する。実API受理はSTEP-04B contractで追加確認する。

### 8.5 Pull Request・CI evidence

| Field | Result |
|---|---|
| Implementation commit | `d74e64b6c8f33a1779f70725236acbfd7b3c7401` |
| Pull Request | [#16 Implement STEP-04A persistence contracts](https://github.com/pitekusu/shittim-chest/pull/16) |
| Project CI | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29551545586。`quality`、`tests`、`security`、`package`、`docs-public-safety` |
| CodeQL | PASS: https://github.com/pitekusu/shittim-chest/actions/runs/29551544046。Actions/Python分析とCodeQL aggregateが成功 |
| Managed checks | PASS: GitGuardian Security Checks、review/comment 0、unresolved review thread 0、merge state CLEAN |
| Merge | PASS: squash merge commit `54948d7edcd38cd47db2043240da2ac275d7bc0f`、remote feature branch削除、local `main` fast-forward後clean |
| AWS/Discord/OpenAI impact | None。resource、Application、token、paid APIを作成・変更していない |

## 9. STEP-04B DynamoDB persistence adapter

### 9.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-04B-DYNAMODB-ADAPTER` |
| Status | Completed・PR `#18` squash merged |
| Date | 2026-07-17 |
| Branch/base | `step-04b-dynamodb-adapter` / `main` `d910de4` |
| Requirements | RQ-003、RQ-004、RQ-005、RQ-011、RQ-017 |
| Test IDs | `CT-PERSIST-001`〜`008`、AT-012、AT-015、QT-Lock、QT-Ruff、QT-Mypy、QT-Layer、ST-Dependency、FEAS-SBOM-001、QT-Package、ST-RepoSurface、QT-Docs、QT-Workflow |

boto3/boto3-stubs 1.43.50をlockし、同期SDKを`asyncio.to_thread`へ隔離する`DynamoDbDebateRepository`と`DynamoDbOutboxRepository`を実装した。受付はGuild日次quota、3つのglobal slot、Debate/Attempt META、operation resultを1 transactionで作る。phase更新はexpected snapshotとowner/fencing/expiryを条件にし、terminal遷移とslot解放を同一transactionにする。FAILED retryはquotaを増やさず、新attempt、current-attempt CAS、slot、operation resultを原子的に保存する。

recoverable GSIは候補発見だけに使い、base tableのcurrent attemptと期限切れleaseをtransactionで再検証する。leaseは取得ごとにfencing tokenをincrementし、application heartbeatは処理中20秒ごとにrenewする。renewはAttempt METAとslotのowner/token/old expiryを同じtransactionで更新し、phase更新はlease属性を保持する条件付き`Update`として並行renew済みexpiryを巻き戻さない。outboxはprepare、chunk順claim、期限付きreclaim、reschedule、sent完了をcurrent attemptとleaseの`ConditionCheck`でfenceする。operation IDの正本は`OPERATION#<id>/RESULT`へ分離し、strongly consistent `GetItem`で再送結果を取得する。

CIの`tests` jobは公式DynamoDB Local 3.3.0をmulti-arch digest `sha256:d89f8fcc6b1a39cb35976c248ed42a28c66ae00dc043099210f5571e42648ab4`へ固定して起動する。本番table、AWS credential、paid API、Discord/OpenAI、AWS resourceは作成・変更しない。

### 9.2 Latest official references

| Checked | Target | Official reference | Applied result |
|---|---|---|---|
| 2026-07-17 | TransactWriteItems API | https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_TransactWriteItems.html | 100 action/4 MB、同一item二重action禁止、10分idempotency token、ordered cancellation reasonを確認 |
| 2026-07-17 | DynamoDB transaction | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/transactions.html | quota、slot、META、operation result、terminal releaseを原子化 |
| 2026-07-17 | GSI consistency | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/GSI.html、https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html | GSIを候補発見だけに限定し、排他はbase transactionで確定 |
| 2026-07-17 | Query API/pagination | https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_Query.html、https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Query.Pagination.html | `LastEvaluatedKey`がなくなるまで1 MB pageを処理 |
| 2026-07-17 | boto3/boto3-stubs 1.43.50 | https://pypi.org/project/boto3/、https://pypi.org/project/boto3-stubs/ | Python 3.14対応、typed clientとDynamoDB stubをlock |
| 2026-07-17 | DynamoDB Local 3.3.0 | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.DownloadingAndRunning.html、https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocalHistory.html | 公式containerをdigest固定しtransaction/GSI/outbox testへ使用 |

### 9.3 Local validation

| ID | Check | Result |
|---|---|---|
| `QT-Lock` | `uv lock`、frozen全group sync | PASS: 74 packages resolved。boto3/botocore 1.43.50とDynamoDB型stubを追加 |
| `QT-Ruff` | format/check | PASS: source/test finding 0 |
| `QT-Mypy` | strict typing | PASS: 45 source/test/tool files、error 0 |
| `CT-PERSIST-001`〜`006` | serializer/migration/operation contract回帰 | PASS |
| `CT-PERSIST-007` | DynamoDB Local acceptance/quota/slot/terminal/retry/GSI/renew | PASS: quota 30件、同時3件、4件目busy、terminal解放、new fencing token、旧worker拒否、renew済みexpiry保持を確認 |
| `CT-PERSIST-008` | DynamoDB Local outboxとSDK Stubber | PASS: chunk順、claim、retry時刻、reclaim、sent冪等性、transaction cancel mappingを確認 |
| Full pytest | unit/property/contract/integration回帰 | PASS: 163 tests、domain/application line/branch 92.40%（表示92%） |
| `ST-Dependency` | hash付きlock export＋pip-audit strict | PASS: known vulnerability 0 |
| `FEAS-SBOM-001` | CycloneDX 1.5 strict schema/project/lock/ref | PASS: 73 dependency components、lock inventory差分0。local出力は一時fileだけ |
| `QT-Package` | `uv build --no-sources` | PASS: sdistとpure Python wheelを生成 |
| `QT-Layer` / docs/public | import-linter、public surface、Obsidian mirror | PASS: 1 contract kept、82 public files finding 0、14文書byte一致 |
| Secret scan | Betterleaks 1.6.1、変更・新規file、redaction有効、provider validationなし | PASS: 約475.83 KB、leak 0 |
| Runtime boundary | DynamoDB Local終了後のprocess確認 | PASS: 一時containerを停止・自動削除。本番AWS接続なし |

### 9.4 Findings・resolution

- operation resultをDebate partition内へ置くSTEP-04A案では、accept前にdebate IDを知らない再送をoperation IDだけでstrongly consistent readできなかった。専用`OPERATION#<id>/RESULT` keyへ変更し、GSIと10分tokenへ依存しない長期冪等性へ修正した。
- DynamoDB Localは`ClientRequestToken`をtable削除後も同一account内で保持するため、test tableが違っても短い固定operation名だけのtokenが`IdempotentParameterMismatchException`になった。token hashへtable、operation、aggregate、slot/fencingを含め、別table/別transactionの衝突を防いだ。全163 testで再確認した。
- 初回実装のphase更新はAttempt METAを丸ごとPutしたため、並行heartbeatが更新したlease expiryを古いsnapshotで巻き戻す余地があった。lease属性を保持する条件付きUpdateへ変更し、heartbeat中のphase完了、renew後phase更新、旧fencing write拒否を回帰試験へ追加した。
- boto3型stubを通常importした初回wheel smokeは、runtime環境にdev-only `mypy_boto3_dynamodb`がなくimport errorになった。stub importを`TYPE_CHECKING`境界へ移し、lock済み7 runtime packageとproject wheelだけの隔離環境でadapter import成功を再確認した。CIもruntime requirementsをhash検証して先に導入し、project wheel自体は`--no-deps --no-index`でinstallする。
- sandbox内からlocalhost socketが禁止され、最初のLocal試験は`Operation not permitted`で停止した。承認済みhost実行へ切り替え、同じdigestとtest suiteで合格した。
- persistence testだけを実行した初回は対象外application testを省いたためcoverage 46.20%でglobal 90% gateを満たさなかった。5 persistence test自体は全て合格しており、heartbeat回帰を含む正式な全suiteで163 tests、92.40%を確認した。
- DynamoDB Localは本番のtransaction conflict/throttling/IAMを再現しない。transaction cancelはSDK Stubberで補い、IAM/throttling/実service smokeはCDK/deploy stepまで未実施として扱う。
- PR `#18`ではhead commit `4f3882b0a1b659c670efd9eebe2b77202c62246c`に対して5つのproject CI check、CodeQL Actions/Python、CodeQL aggregate、GitGuardianが合格した。review/comment/unresolved threadは0、merge stateは`CLEAN`だった。

### 9.5 Pull Request・main evidence

| Field | Result |
|---|---|
| Implementation commit | `4f3882b0a1b659c670efd9eebe2b77202c62246c` |
| Pull Request | [#18 Implement STEP-04B DynamoDB adapter](https://github.com/pitekusu/shittim-chest/pull/18) |
| PR checks | PASS: `quality`、`tests`、`security`、`package`、`docs-public-safety`、CodeQL Actions/Python、CodeQL aggregate、GitGuardian。未解決review thread 0 |
| Merge | PASS: inspected headを固定してsquash merge。main commit `9aafe6e390d398eda8c8385af11620f33d1764b9`、remote feature branch削除 |
| Main CI | PASS: [run `29553563948`](https://github.com/pitekusu/shittim-chest/actions/runs/29553563948)。5 job、DynamoDB Local integration、source SBOM artifact、Betterleaks full-history scanを完了 |
| Main CodeQL | PASS: [run `29553563821`](https://github.com/pitekusu/shittim-chest/actions/runs/29553563821)。Actions/Python分析が成功 |
| Managed Dependency Graph | PASS: [run `29553565579`](https://github.com/pitekusu/shittim-chest/actions/runs/29553565579)。uv dependency snapshot更新が成功 |
| Runtime impact | None。本番AWS/Discord/OpenAI、credential、cloud resourceを作成・変更していない |

## 10. STEP-05A OpenAI Structured Outputs adapter

### 10.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-05A-OPENAI-STRUCTURED-ADAPTER` |
| Status | merged / GitHub validation PASS |
| Date | 2026-07-17 |
| Base | `main` / `5d484090c47e3bdc9bdea88416839590e2c00012` |
| Requirements | RQ-002、RQ-011、RQ-012、RQ-016 |
| Test IDs | `CT-OPENAI-001`〜`005`、`QT-Lock`、`QT-Ruff`、`QT-Mypy`、`QT-Layer`、`ST-Dependency`、`FEAS-SBOM-001`、`QT-Package`、`ST-RepoSurface`、`QT-Docs`、`QT-Patch` |

stable `AsyncOpenAI.responses.parse()`による初回意見、最終案、投票、決定事項adapterを追加した。Pydantic strict schema、未知field拒否、private persona検証、最大6並列、HTTP/SDK retry上限、provider errorの安定domain code変換、prompt/outputを含まないusage/failure recorder契約を含む。requestは`store=false`、`tools=[]`、`tool_choice=none`、`parallel_tool_calls=false`とする。

Responses API Multi-agent betaは採用しない。`client.beta.responses`、`multi_agent`、`OpenAI-Beta`ヘッダはsourceとmock送信requestのどちらにも含まない。persona並列実行、winner集計、checkpoint、resumeは既存のPython application層を正とする。

OpenAI実API、API key、Web search/Evidence Router、Terra昇格、Discord結合、CloudWatch出力、AWS resourceは使用・作成・変更していない。Terra昇格は[[02_議論事項・意思決定記録]]のP1未決事項とし、実装前にoperatorへ相談する。

### 10.2 Latest official references

| 確認日 | 対象 | 公式資料 | 実装への反映 |
|---|---|---|---|
| 2026-07-17 | OpenAI Python 2.46.0 | https://pypi.org/project/openai/、https://github.com/openai/openai-python | Python 3.14対応、`AsyncOpenAI.responses.parse`引数、error階層、SDK retryを照合 |
| 2026-07-17 | Structured Outputs | https://developers.openai.com/api/docs/guides/structured-outputs | Pydantic schema、parsed output、refusal/incomplete分類 |
| 2026-07-17 | GPT-5.6 | https://developers.openai.com/api/docs/guides/latest-model | 既定modelを高頻度向けLunaに維持し、deploy時に利用可否を再確認 |
| 2026-07-17 | Responses Multi-agent beta | https://developers.openai.com/api/docs/guides/responses-multi-agent | beta APIは非採用。stable ResponsesとPython orchestrationを使用 |
| 2026-07-17 | Pydantic 2.13.4 / httpx 0.28.1 | https://pypi.org/project/pydantic/、https://pypi.org/project/httpx/ | Python 3.14互換、strict schema、mock transportをlock |

### 10.3 Local validation

| ID | Check | Result |
|---|---|---|
| `QT-Lock` | `uv lock --check`、`uv sync --frozen --all-groups` | PASS: 87 packages resolved、86 packages checked。`openai==2.46.0`、`httpx==0.28.1`、`pydantic==2.13.4` |
| `QT-Ruff` | format/check | PASS: 54 files formatted、finding 0 |
| `QT-Mypy` | strict typing | PASS: 54 source/test/tool files、error 0 |
| `QT-Layer` | import-linter | PASS: 24 files、48 dependencies、1 contract kept |
| `CT-OPENAI-001`〜`005` | 公式SDK＋mock HTTP contract | PASS: 15 tests、4 schema、request boundary、refusal/incomplete/invalid、429/auth/permission/model/5xx/transport、最大6並列、cancel再送出、persona/config/domain検証 |
| Full pytest | DynamoDB Local 3.3.0を含む全回帰 | PASS: 178 tests、domain/application line/branch 92.40% |
| `ST-Dependency` | hash付きlock export＋`pip-audit --strict` | PASS: known vulnerability 0 |
| `FEAS-SBOM-001` | CycloneDX 1.5 strict schema/project/lock/ref | PASS: 86 dependency components、一時fileだけを生成 |
| `QT-Package` | sdist/wheel build＋runtime依存23 packageだけの隔離install/import | PASS: pure Python wheel、`py.typed`、version、OpenAI adapter importを確認 |
| `ST-RepoSurface` | tracked/untracked public surface | PASS: 91 files、finding 0 |
| Secret scan | Betterleaks 1.6.1でGit historyと現行directoryを別々redacted scan、provider validationなし | PASS: history約1.27 MB、directory約1.86 MB、leak 0 |
| `QT-Docs` / `QT-Workflow` / `QT-Patch` | 14文書構造、Obsidian byte mirror、actionlint 1.7.12、`git diff --check` | PASS: canonical 14件、mirror current、workflow finding 0、whitespace error 0 |
| Runtime boundary | 固定digestのDynamoDB Local停止、OpenAI/AWS/Discord実通信なし | PASS: 一時containerを停止・自動削除 |

### 10.4 Findings・resolution

- 旧OpenAI詳細設計の`assumptions`、`risks`、`rationale`、`tradeoffs`、`evidence_refs`は現行domain/DynamoDB schemaに保存先がなかった。出力させて破棄する設計にせず、STEP-05Aは保存可能なfieldだけの1対1 strict schemaとした。追加fieldはdomain、serializer、DynamoDB migration、Discord表示を含む別sliceの未決事項とした。
- 対象OpenAI testを最初に通常coverage gate付きで実行した結果、対象外application testを実行しないためglobal coverageが63%となった。最終契約testは`--no-cov`で15 testsを確認し、正式な全suiteで178 tests/92.40%を確定した。
- incomplete fixtureの初回実行は不正なstructured payloadのためSDK parse段階で失敗した。fixtureをschema-validな不完全responseへ修正し、Pydantic `ValidationError`も`invalid_output`へ変換する回帰試験を追加した。
- sandbox内のuv cacheとlocalhost socketはhostの制限で失敗した。sourceまたは依存の不具合ではないため、同じlock、command、DynamoDB digestを承認済みhost実行で再検証して合格した。
- usage recorderはresponse ID、model、operation、latency、token内訳だけを受け取る。prompt、生成本文、Discord user ID、persona promptを型上受け取れない。CloudWatch adapterは後続sliceで実装する。

### 10.5 Pull Request・CI evidence

| Field | Result |
|---|---|
| Commit | `d6ea5611e310a07ac94f88889cbf4a2d1499256d` |
| Pull Request | `#20` merged |
| PR/main checks | PASS: main CI `29555748245`、CodeQL `29555748083`、Dependency Graph `29555749526` |
| External runtime impact | None。OpenAI/AWS/Discordのcredential、paid API、Application、cloud resourceを使用・作成・変更していない |

## 11. STEP-05B Evidence Router・Web search

### 11.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-05B-WEB-EVIDENCE` |
| Status | implementation/validation/PR/main CI PASS |
| Date | 2026-07-17 |
| Base | `main` / `d6ea5611e310a07ac94f88889cbf4a2d1499256d` |
| Requirements | RQ-013、RQ-016 |
| Test IDs | `CT-OPENAI-006`〜`008`、`CT-PERSIST-002`〜`003`、`UT-APP-001` |

`question-router-v2`は追加model callを使わず、現在情報・高risk topicと類似語を`required`、時間・場所・推薦contextを`optional`、明示的な創作・言換え・要約・時間非依存比較だけを`none`へ分類する。未知表現はfail-safeに`optional`へ倒し、rules versionとrouting reasonをEvidence METAへ保存する。検索時はstable Responses APIへ1 requestだけ送り、hosted `web_search`を`tool_choice=required`、`max_tool_calls=4`、`include=["web_search_call.action.sources"]`、`store=false`で使用する。検索と討論生成は同じ`OpenAIRequestLimiter`の注入を必須とし、process合計6並列以下にする。Responses API Multi-agent betaは使用しない。

`EvidenceDigestOutputV1.summary`、source action、URL citationを統合し、要約、検索要否、検索状態、response ID、URL/title/canonical metadata/UTC取得時刻/metadata SHA-256を全participant共通のimmutable Evidenceとして保存する。DynamoDB current schemaをv3へ更新し、直前v2 reader migrationを追加した。optional失敗は`optional_unavailable`で続行し、required失敗は`required_evidence_unavailable`でFAILEDへ保存する。

### 11.2 Latest official references

| 確認日 | 対象 | 公式資料 | 実装への反映 |
|---|---|---|---|
| 2026-07-17 | OpenAI Web search | https://developers.openai.com/api/docs/guides/tools-web-search | hosted `web_search`、source include、URL citation、search/open/find actionを確認 |
| 2026-07-17 | Responses API `POST /v1/responses` | https://developers.openai.com/api/reference/resources/responses/methods/create | `tools`、`tool_choice`、`include`、`max_tool_calls`、`store`のrequest shapeを照合 |
| 2026-07-17 | OpenAI Python 2.46.0 | https://github.com/openai/openai-python | typed web-search outputと`responses.parse()`引数を照合 |

### 11.3 Local validation

| ID | Check | Result |
|---|---|---|
| `QT-Ruff` | `ruff check src tests` | PASS: finding 0 |
| `QT-Mypy` | `mypy src tests` | PASS: 51 source/test files、error 0 |
| Focused regression | Router、application failure mapping、DynamoDB serializer、Web Evidence contract | PASS: 44 tests in 0.55s |
| Full pytest | 全回帰、coverage gate | PASS: 187 passed、5 opt-in skipped、domain/application line/branch 91.50% |
| `CT-OPENAI-006` | required/optional/none classification | PASS: latest weather/current officeholder、空模様/レート/総理の類似語、sweet breakfast、timeless comparison、未知表現fallbackと判定証跡を固定 |
| `CT-OPENAI-007` | typed SDK response fixtureとrequest arguments | PASS: digest/source/citation、1 request、max 4 calls、`store=false` |
| `CT-OPENAI-008` | provider unavailable | PASS: optional続行、required FAILED codeを区別 |
| `CT-PERSIST-002/003` | Evidence round-trip、schema v2→v3、未知version | PASS |
| External runtime | OpenAI/AWS/Discord実通信 | NOT RUN: credential、paid API、cloud resourceを使用せず |

### 11.4 Findings・remaining validation

- 公式Web searchの「1回」はtool action数ではなく、orchestratorが送るResponses API request数として定義した。providerは同一request内でsearch/open/findを複数実行できるため4 callsへ制限した。
- `content_hash`はsource page本文のhashではなく、保存するcanonical source metadataの改変検知用SHA-256である。本文取得を装わない。
- local sandboxでは既存の公式SDK＋同期MockTransport testがPython executor shutdownで停止したため、STEP-05B focused suiteはtyped SDK response fixtureとAsyncMock boundaryで検証した。同じworktreeをhost側で全実行し、Router fallback追加後に187 passed、5 opt-in skipped、91.50% coverageで合格した。package、audit、文書mirrorはPR前の通常gateとGitHub CIで再確認する。
- OpenAI実APIによるopt-in integration、citationのDiscord clickable表示、CloudWatch recorderは後続Discord/runtime sliceで検証する。

### 11.5 Pull Request・CI evidence

| Field | Result |
|---|---|
| Commit | `44a35fa84429b82036d147c48c100bac7e508509` |
| Pull Request | `#21` squash merged、commit `44a35fa84429b82036d147c48c100bac7e508509` |
| PR/main checks | PASS: PR CI `29558401575`、PR CodeQL `29558400538`、main CI `29558517220`、main CodeQL `29558516762` |
| External runtime impact | None。OpenAI/AWS/Discordのcredential、paid API、Application、cloud resourceを使用・作成・変更していない |

## 12. STEP-05C 品質昇格Policy・Shadow評価基盤

### 12.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-05C-ESCALATION-SHADOW` |
| Status | implementation/validation/PR/main CI PASS |
| Date | 2026-07-17 |
| Branch/base | `step-05c-escalation-shadow` / `44a35fa` |
| External impact | None。paid OpenAI API、AWS、Discordへ接続・変更していない |

通常のLuna standard処理を維持し、投票後に`escalation-shadow-v1`で`split_vote`、`winning_axis_low`、`winning_average_low`を独立計算して保存する。assessmentは再実行開始phaseを最終案収集、実行回数を0または1、初期状態を`executed=false`へ制約する。shadow判定は追加OpenAI requestを送らない。

application層へ`luna_standard`、`terra_standard`、`luna_pro`のSDK非依存Policyを追加した。Luna proは`gpt-5.6-luna`へ`reasoning.mode=pro`、effort mediumを指定し、Terra standardはmodeを送らない。usage/failure telemetryへ本文なしのPolicy IDとreasoning modeを追加した。Responses API Multi-agent betaは使用しない。

DynamoDB current schemaをv4へ更新し、assessmentを縦分割itemとして保存する。readerは直前v3をassessmentなしで読み、未知versionを拒否する。`tools/evaluate_escalation.py`は`--live`と`OPENAI_API_KEY`を両方要求し、repository外へblind回答とPolicy keyを分離出力する。10件の公開可能な汎用fixture、Policy別の実行時料金入力、token/latency/推定費用、人間rubric欄を提供するが、このsliceでは有料評価を実行していない。

### 12.2 Official references

| 確認日 | 対象 | 公式資料 | 反映 |
|---|---|---|---|
| 2026-07-17 | GPT-5.6 model family / pro mode | https://developers.openai.com/api/docs/guides/latest-model | Luna/Terraの用途、proがmodelとは独立したmodeであること、代表評価を先行することを確認 |
| 2026-07-17 | Responses API | https://developers.openai.com/api/docs/guides/migrate-to-responses | stable Responses API、`store=false`、typed outputを維持 |

### 12.3 Local validation

| Check | Result |
|---|---|
| Ruff format/check | PASS: finding 0 |
| mypy strict | PASS: 64 source files、error 0 |
| Full pytest | PASS: 196 passed、5 opt-in skipped、domain/application line/branch 91.81% |
| Shadow request count | PASS: 通常討論は初回3、最終案3、投票3、決定1の10 callsのまま |
| Policy contract | PASS: Terra model、Luna pro `reasoning.mode=pro`、Policy telemetryをmock transportで確認 |
| Persistence | PASS: assessment round-trip、schema v3→v4、未知version拒否 |
| Public/docs safety | PASS: public surface 102 files、14 canonical notes、Obsidian mirror byte一致 |
| CycloneDX SBOM | PASS: schema 1.5、86 dependency components、lock inventory一致 |
| pip-audit | PASS: known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Paid evaluation | NOT RUN: `--live`/API keyを使用せず、通常CIには含めない |

対象15 testsだけを通常coverage gate付きで実行した初回は、対象外application moduleを実行しないためglobal coverage 52.71%で終了した。対象tests自体は15件全て成功しており、その後の正式な全suiteで196 passed、5 skipped、91.81%を確認した。これはsource failureではなくglobal gateをfocused runへ適用した結果として記録する。

### 12.4 Remaining decisions

- blind A/Bを実行・採点した後、Terra standardまたはLuna proの採用、production threshold、追加token/deadline上限をoperatorと決める。
- production auto-escalation、Discord表示、CloudWatch metricは未実装である。
- refusal、policy block、schema failure、429、timeoutを別Policyで回避しない。

### 12.5 Pull Request・CI evidence

| Field | Result |
|---|---|
| Commit | `a6f43cbf395eda3124baf8f82ccb0d4abd6e0be1` |
| Pull Request | `#22` squash merged、head `337d7626642627053abf784fb7e1cd0c08b34364` |
| PR checks | PASS: CI `29560115285`、CodeQL `29560114266`、GitGuardian、review thread 0 |
| main checks | PASS: CI `29560181837`、CodeQL `29560181270` |
| External runtime impact | None。paid OpenAI API、AWS、Discordを使用・変更していない |

## 13. STEP-05C.1A Blind評価workflow強化

### 13.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-05C.1A-EVALUATION-WORKFLOW` |
| Status | implementation/local/PR/main validation PASS |
| Date | 2026-07-17 |
| Branch/base | `step-05c1a-evaluation-workflow` / `884277c` |
| External impact | None。OpenAI API key、paid API、AWS、Discordを使用・変更していない |

`tools/evaluate_escalation.py`をv2 artifactへ更新し、採点者用回答とPolicy key・性能指標を互いに分離したrepository外directory treeへ出力する。成功応答を継続しつつrefusal/incomplete/invalid outputをmajor failure、rate limit/timeout/unavailableをoperational failureとして安全なcodeだけで保持する。configuration failureは評価結果に混ぜず実行を中止する。

`tools/score_escalation.py`を追加し、evaluation ID、fixture SHA-256、A/B mapping、rubric整数1〜5、失敗回答未採点を検証する。質問・回答を含まないPolicy別品質平均、preference、major/operational failure、token、推定費用、p95 latencyを出力する。operational failureは再実行、major failureが多いPolicyは不採用候補、品質同等時は費用・latency順とする。

### 13.2 Official references

| 確認日 | 対象 | 公式資料 | 反映 |
|---|---|---|---|
| 2026-07-17 | GPT-5.6 model family / pro mode | https://developers.openai.com/api/docs/guides/latest-model | 代表taskで品質、完全性、token、latency、costを比較する |
| 2026-07-17 | OpenAI API/Python errors | https://developers.openai.com/api/docs/guides/error-codes | refusal等とrate limit・timeout・unavailableを分離する |

### 13.3 Local validation

| Check | Result |
|---|---|
| uv lock | PASS: 87 packages resolved、変更なし |
| Ruff format/check | PASS: 66 files、finding 0 |
| mypy strict | PASS: 66 source files、error 0 |
| import-linter | PASS: 29 files、67 dependencies、1 contract kept |
| Full pytest | PASS: 207 passed、5 opt-in skipped、domain/application line/branch 91.81% |
| Evaluation workflow tests | PASS: 14 tests。gate、path分離、failure分類、nested ExceptionGroup、blind metric除去、rubric、artifact照合、recommendationを検証 |
| CLI fail-closed | PASS: `--live`なしでexit 1、scorer/key artifactを作成しない |
| Public/docs safety | PASS: 14 canonical notes、mirror byte一致、public surface 104 files |
| CycloneDX SBOM | PASS: schema 1.5、86 dependency components、lock inventory一致 |
| pip-audit | PASS: known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Paid evaluation | NOT RUN: API keyと`--live`を使用していない |

### 13.4 Remaining work

- STEP-05C.1Bの実API評価前に利用可能modelと料金を再確認し、operatorの明示承認を得る。
- 10ケースのblind回答を人間採点後、content-free集計を生成して採用Policy、threshold、token/deadline上限を相談する。
- 本番自動昇格、Discord表示、CloudWatch metricは引き続き無効・未実装である。

### 13.5 Pull Request・CI evidence

| Field | Result |
|---|---|
| Commit | `1360411033394a41574877c07efeb8fb62c638d6` |
| Pull Request | `#24` squash merged、head `61118360b22bb0730f1955d263f601aa1069e9f5` |
| PR checks | PASS: CI `29562004889`、CodeQL `29562003209`、GitGuardian、review thread 0 |
| main checks | PASS: CI `29562083701`、CodeQL `29562083042` |
| External runtime impact | None。paid OpenAI API、AWS、Discordを使用・変更していない |

## 14. STEP-05C.1B 実API・preference-only評価

### 14.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-05C.1B-LIVE-PREFERENCE-EVALUATION` |
| Status | live generation / human blind review / local aggregation PASS、後続決定でproductionはLuna standard固定 |
| Date | 2026-07-17 |
| Branch/base | `step-05c1b-preference-only` / `d9a4dee` |
| External impact | OpenAI Responses APIへ明示承認済み有料評価を実行。AWS、Discord、GitHub設定は変更していない |

最新公式資料でTerra/Luna/pro modeと料金を再確認し、`gpt-5.6-terra` standardと`gpt-5.6-luna` `reasoning.mode=pro`を10件ずつ実行した。最初の実行はBilling未設定の`insufficient_quota`で20件すべて生成前に失敗し、token・推定費用は0だった。Billing設定後の診断requestは16 tokenで成功し、再実行では20回答すべて成功した。raw回答、Policy key、API keyはGit・Obsidianへ保存していない。

5軸100項目のrubric入力は単独operatorが継続できる採点方法ではなかったため、10件それぞれでA/B/tieだけを選ぶpreference-onlyへ変更した。`tools/review_escalation.py`はPolicy keyを読まず1件ずつ表示し、各選択をowner-only fileへatomic保存して中断・再開できる。`tools/score_escalation.py --preference-only`は全選択、未入力rubric、artifact identityを検証し、勝数、同数時の費用・p95 latencyで候補を選ぶ。詳細rubric modeは追加調査用に維持する。

### 14.2 Live result

| Metric | Terra standard | Luna pro |
|---|---:|---:|
| Successful runs | 10/10 | 10/10 |
| Blind preference wins | 2 | 4 |
| Ties | 4件（共通） | 4件（共通） |
| Input tokens | 53,924 | 319,950 |
| Output tokens | 21,069 | 71,317 |
| Reasoning tokens | 1,179 | 27,868 |
| p95 latency | 18,465 ms | 25,319 ms |
| Estimated cost | `$0.450845` | `$0.747852` |

評価本体合計はinput 373,874、output 92,386 token、推定`$1.198697`である。preference-only規則による候補は`luna_pro`（4勝対2勝）だったが、これはproduction採用の自動承認ではない。後続決定により本番はLuna standardだけへ固定し、自動昇格、trigger threshold、追加token/deadline、昇格用Discord表示は実装しない。

### 14.3 Validation

| Check | Result |
|---|---|
| Full pytest | 最初のsandbox実行はDynamoDB SDK境界で進捗停止したため明示中断。local service接続可能な再実行はPASS: 214 passed、5 opt-in skipped、domain/application line/branch 91.81% |
| Preference workflow tests | PASS: preference勝数、tie時cost/latency、rubric混在拒否、不正入力、各選択保存、resume、owner-only permission |
| Ruff | PASS: 68 files、format/check finding 0 |
| mypy strict | PASS: 68 source files、error 0 |
| import-linter | PASS: 29 files、67 dependencies、1 contract kept |
| Docs/public surface | PASS: 14 canonical notes、mirror byte一致、public surfaceにraw回答・credentialなし |
| Live artifacts | PASS: evaluation ID・fixture hash一致、10 cases、20 success、failure 0、raw/key/review/summaryはrepository外かつmode `600` |
| CycloneDX SBOM | PASS: schema 1.5、86 dependency components、lock inventory一致 |
| pip-audit | PASS: known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Release tool pins | PASS: 2 tools |

### 14.4 Production decision

2026-07-17、operatorは構成の単純性、費用予測、運用・復旧経路の少なさを優先し、本番昇格を不採用と決定した。全phaseはLuna standardだけを使用する。Terra standard/Luna proは評価再現用に限定し、shadow assessmentは`executed=false`を維持する。昇格threshold、追加token/deadline、昇格表示は実装しない。回答のバリエーションは3つのversion付きprivate persona promptの判断lens、優先順位、反対方法、提案style、口調で作る。

公式prompt指針（https://developers.openai.com/api/docs/guides/latest-model#favor-leaner-prompts、https://developers.openai.com/api/docs/guides/prompt-engineering#coding）を同日に再確認し、役割と責務を明確にしつつ、重複指示を避ける短いpersona promptと代表fixtureによる変更評価を採用した。

## 15. STEP-06A Discord contract foundation

### 15.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-06A-DISCORD-CONTRACT-FOUNDATION` |
| Status | complete。PR `#27` squash merged、PR/main CI・CodeQL PASS |
| Date | 2026-07-17 |
| Branch/base | `step-06a-discord-contracts` / `7f8f4ae` |
| Requirements | RQ-001、RQ-003、RQ-004、RQ-005、RQ-006、RQ-011 |
| Test IDs | `UT-APP-007`、`CT-DISCORD-001`〜`004`、`CT-PERSIST-003/005` |
| External impact | None。Discord Application、実Guild、AWS、OpenAI、GitHub設定へ接続・変更していない |

Discord/AWS SDKに依存しない4 identity slot、runtime config、stable Discord error code、message splitter、UUIDv7 nonce、UTF-8 SHA-256、panel custom ID codec、prepared outbox factoryをapplication層へ追加した。従来DynamoDB adapter配下だったoutbox/panel型もapplication層へ移し、将来のDiscord adapterがDynamoDB adapterをimportしない依存方向へ修正した。

`DebateSnapshot`はstarter message、thread、control panel messageを別fieldとして保持する。`bind_discord_context`は`ACCEPTED`中だけ3 IDを一括保存し、同値再送を冪等に返し、部分binding、別値rebind、開始後bindingを拒否する。DynamoDB schemaはv5とし、直前v4の欠落panel IDと旧`bot_id`をup-convertする。discord.py 2.7.1自体はSTEP-06Aへ追加せず、実送信を扱うSTEP-06B以降へ分離した。

### 15.2 Official sources

| Checked | Target | Official source | Consequence |
|---|---|---|---|
| 2026-07-17 | discord.py 2.7.1 | https://pypi.org/project/discord.py/ | current releaseを確認。STEP-06AはSDK非依存のためdependency追加なし |
| 2026-07-17 | Interactions | https://docs.discord.com/developers/interactions/receiving-and-responding | initial response 3秒制約を再確認。実deferはSTEP-06C |
| 2026-07-17 | Application Commands | https://docs.discord.com/developers/interactions/application-commands | string optionのmin/max lengthを再確認 |
| 2026-07-17 | Message | https://docs.discord.com/developers/resources/message | content 2,000、nonce最大25、`enforce_nonce`直近数分、allowed mentionsを契約へ反映 |
| 2026-07-17 | Components | https://docs.discord.com/developers/components/reference | interactive `custom_id` 1〜100文字をcodec/testへ反映 |

### 15.3 Validation

| Check | Result |
|---|---|
| Full pytest | PASS: host側locked環境で221 passed、5 opt-in skipped、domain/application line/branch 92.70% |
| Sandbox pytest | Superseded: SDK executor終了待ちで停止したため中断。host側で同一locked suiteを完走 |
| Focused contracts | PASS: Discord/application/DynamoDB 43 tests |
| Ruff | PASS: format/check finding 0 |
| mypy strict | PASS: 70 source files、error 0 |
| import-linter | PASS: 30 files、70 dependencies、1 contract kept |
| CycloneDX SBOM | PASS: schema 1.5、86 dependency components、lock inventory一致 |
| pip-audit | PASS: known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Docs/public surface | PASS: 14 canonical notes、mirror byte一致、108 filesに実snowflake/credentialなし |
| External API | Not run。実Discord token、Application、Guildを使用しない |

### 15.4 Remaining scope

- STEP-06Bのdiscord.py publisher、`allowed_mentions`、`enforce_nonce`、429、履歴reconciliationは後続の本書16章でlocal実装・検証済み。
- STEP-06Cの4 client、READY gate、Guild Command、先行defer、thread/panel Interactionは後続の本書17章でlocal実装・offline試験済み。
- STEP-07のSIGTERM、checkpoint、resume、障害注入は未実装。

### 15.5 Pull Request・CI evidence

| Field | Result |
|---|---|
| Implementation commit | `6928ee7` |
| Pull Request | [#27 Implement STEP-06A Discord contracts](https://github.com/pitekusu/shittim-chest/pull/27) |
| Implementation-head CI | PASS: CI `29569953760`、CodeQL `29569951305`、GitGuardian、review thread 0 |
| Final PR head | `d154d0ef8783afbe32289da8b1da8cc73585b4f9`、required checks全合格、unresolved thread 0、merge state `CLEAN` |
| Squash merge | PASS: merge commit `47af41f37640c6646fa20b88c7a0479d7962c1a9`、remote feature branch削除 |
| main checks | PASS: CI `29570175552`、CodeQL `29570174679` |

## 16. STEP-06B Discord publisher

### 16.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-06B-DISCORD-PUBLISHER` |
| Status | merged、PR `#30`、merge commit `96a1ace` |
| Date | 2026-07-17 |
| Base | `main` / `93ad159` |
| Requirements | RQ-004、RQ-005、RQ-011 |
| Test IDs | `CT-DISCORD-005`〜`008`、`AT-005`、`AT-008`、`AT-010`、`AT-011`、`AT-013` |
| External impact | None。実Discord token/Application/Guild、AWS、OpenAIへ接続・変更していない |

discord.py 2.7.1をruntime依存へ追加し、4つのgeneric Bot slotに対応するdistinct client、`DiscordOutboxRepository`、Clock、claim ownerを注入する`DiscordPyPublisher`を実装した。publisherはexpected leased snapshotとattempt内operation IDを受け、永続recordを確認してからclaimする。正常時は`Thread.send(content, nonce=..., allowed_mentions=AllowedMentions.none())`を1回呼び、返却message IDをfenced `mark_sent`へ保存する。

discord.py 2.7.1の公式sourceでnonce指定時に`enforce_nonce=true`となることを確認し、private helperの実payloadをversion contractとして試験した。2回目以降のdelivery attemptではoutbox作成時刻後の履歴を古い順に最大500件照合する。同一Bot author、nonce、content、SHA-256が一致する最古messageを採用し、同一nonceで内容不一致なら送信しない。Discord成功後のDynamoDB完了失敗を注入し、claim expiry後の履歴採用でSDK sendが1回のままであることを確認した。

429はdiscord.py内部のRetry-After処理へ委譲する。4 clientは`max_ratelimit_timeout=30`以外を拒否し、SDKがpre-emptive wait上限を超えた`RateLimited`はその`retry_after`、HTTPException 429はRetry-After header、408/409/5xx/OSErrorは30秒の既定delayでoutboxを再scheduleする。publisher内で同じHTTP requestをloopしない。Discord channel解決、履歴照合、sendは45秒でtimeoutし、共有60秒outbox claimより前にrescheduleする。`mark_sent`はtimeout外でfenced writeする。permission、thread消失、wrong Guild、locked thread、その他4xxは自動再送・unlockしない。

### 16.2 Official sources

| Checked | Target | Official source | Consequence |
|---|---|---|---|
| 2026-07-17 | discord.py 2.7.1 | https://pypi.org/project/discord.py/ | current releaseをruntime dependencyとしてlock |
| 2026-07-17 | Create Message | https://docs.discord.com/developers/resources/message | content 2,000、nonce 25、`enforce_nonce`、`allowed_mentions.parse=[]`を契約へ反映 |
| 2026-07-17 | Rate limits | https://docs.discord.com/developers/topics/rate-limits | 429 delayをhard codeせずRetry-After/SDK処理へ委譲 |
| 2026-07-17 | Message Content Intent | https://docs.discord.com/developers/events/gateway#message-content-intent | Application自身の投稿はprivileged Intentなしでも内容取得可能と確認 |
| 2026-07-17 | discord.py send source | https://github.com/Rapptz/discord.py/blob/v2.7.1/discord/abc.py#L1523-L1706 | 公開`Thread.send`のnonce/allowed_mentions引数を使用 |
| 2026-07-17 | discord.py payload source | https://github.com/Rapptz/discord.py/blob/v2.7.1/discord/http.py#L141-L208 | nonceから`enforce_nonce=true`を生成するshapeをcontract testで固定 |
| 2026-07-17 | discord.py client source | https://github.com/Rapptz/discord.py/blob/v2.7.1/discord/client.py | `max_ratelimit_timeout=30`を必須化し、無制限待機を禁止 |
| 2026-07-17 | discord.py errors source | https://github.com/Rapptz/discord.py/blob/v2.7.1/discord/errors.py | `RateLimited.retry_after`をoutbox再scheduleへ反映 |

### 16.3 Validation

| Check | Result |
|---|---|
| Targeted Discord contract | PASS: 19 tests、network/secretなし |
| Full pytest | PASS: 245 tests、domain/application line/branch 92.74% |
| Ruff | PASS: format/check finding 0 |
| mypy strict | PASS: 75 source files、error 0 |
| import-linter | PASS: 33 files、77 dependencies、1 contract kept |
| uv lock | PASS: 96 packages、discord.py 2.7.1とtransitive dependencyを固定 |
| CycloneDX SBOM | PASS: schema 1.5、95 dependency components、lock inventory一致 |
| pip-audit | PASS: known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Docs/public surface | PASS: 14 canonical notes、Obsidian mirror byte一致、113 filesにfinding 0 |
| DynamoDB Local | PASS: CIと同じ固定digestを一時起動し、完了後に停止・削除 |
| Real Discord API | Not run。token、Application、Guildを使用しない |

### 16.4 Remaining scope

- STEP-06Cの4 client lifecycle、READY gate、Guild Command、3秒defer、starter/thread/panel Interactionは本書17章でlocal実装・offline試験済み。
- STEP-07のprocess signal、outbox drain、checkpoint、resume、fault injectionは未実装。
- CloudWatch metric/alarm、実Bot permission、実Discord rate limit smokeは後続production stepで確認する。

### 16.5 PR・merge evidence

| Evidence | Result |
|---|---|
| Pull Request | [#30 Implement Discord outbox publisher](https://github.com/pitekusu/shittim-chest/pull/30) |
| Required checks | PASS: CI `29573406000`、CodeQL `29573404011`を含む全required check |
| Squash merge | PASS: merge commit `96a1ace8dcc9b9cd23cb0c424d5cc623ef079cb2` |

## 17. STEP-06C Discord interaction runtime

### 17.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-06C-DISCORD-INTERACTIONS` |
| Status | merged、PR `#31`、merge commit `9799cb9` |
| Date | 2026-07-17 |
| Base | `main` / `96a1ace` |
| Requirements | RQ-001、RQ-006、RQ-011、RQ-017 |
| Test IDs | `UT-APP-003/005/007`、`CT-DISCORD-009`〜`012`、`AT-001`〜`003`、`AT-015` |
| External impact | None。実Discord token/Application/Guild、AWS、OpenAI、GitHub設定へ接続・変更していない |

4つのdistinct clientをGUILDS-only Intent、mentions無効、30秒SDK rate-limit上限で生成するbuilder、4 READYとGuild/channel allowlistを返すgateway、4つのstart taskを所有して1 client終了時に全clientをcloseするsupervisorを実装した。tokenはsupervisor境界で4つの非空・相異値だけを受理し、sourceやconfigへ保存しない。

moderatorのGuild Command `/shittim question:<1〜1000文字>`を登録し、canonical schema hashが変わったdeploy時だけ明示syncできるcontrollerを実装した。callback先頭でephemeral deferし、通常text channelだけを受理する。受付後はmentions無効とnonce付きのstarter、Public Thread、attempt-bound Cancel/Retry panelを作り、3 IDを一括bindingする。Interaction応答消失時はmoderator author、nonce、完全一致contentを最大100件の履歴から照合して再利用する。setup失敗時はunbound attemptをbest-effortでCANCELLEDへ移す。

componentはmoderator Application、Guild、永続thread、panel message、debate、source attempt、actor権限を全て照合する。古いpanelから後続attemptを中止・再分岐できない。controllerはdebate taskをdebate IDごとに1件だけ所有し、Cancel/closeでcancel・awaitする。discord.py 2.7.1の`Client.event()`がPython 3.14でdeprecated APIを経由するため、warning-as-errorを緩和せずmoderator専用Client subclassの`on_interaction`へ明示handlerを接続した。

### 17.2 Official sources

| Checked | Target | Official source | Consequence |
|---|---|---|---|
| 2026-07-17 | Discord Interactions | https://docs.discord.com/developers/interactions/receiving-and-responding | initial response 3秒、token 15分を確認しcallback先頭deferへ反映 |
| 2026-07-17 | Application Commands | https://docs.discord.com/developers/interactions/application-commands | Guild command、STRING length、明示syncを実装 |
| 2026-07-17 | Components | https://docs.discord.com/developers/components/reference | custom ID 100文字とcomponent contextを再確認 |
| 2026-07-17 | discord.py 2.7.1 interactions | https://github.com/Rapptz/discord.py/blob/v2.7.1/discord/interactions.py | defer/edit original responseのSDK shapeをoffline contractへ反映 |
| 2026-07-17 | discord.py 2.7.1 commands | https://github.com/Rapptz/discord.py/blob/v2.7.1/discord/app_commands/commands.py | Range annotationとGuild command schemaを照合 |

### 17.3 Validation

| Check | Result |
|---|---|
| Focused application/Discord | PASS: 53 tests、network/secretなし |
| Discord interaction contracts | PASS: 12 tests。schema/defer/setup/replay/history/failure/context/Cancel/Retry |
| Isolated regression | PASS: unit + Discord contract 228 tests、domain/application line/branch 91.19% |
| Ruff | PASS: format/check finding 0 |
| mypy strict | PASS: 79 source files、error 0 |
| import-linter | PASS: 35 files、86 dependencies、1 contract kept |
| uv lock / package | PASS: 96 packages resolved、sdistと`py3-none-any` wheel build |
| Docs/public surface | PASS: Obsidian正本14件とmirror byte一致、117 filesにfinding 0 |
| CycloneDX SBOM | PASS: schema 1.5、95 dependency components、lock inventory一致 |
| pip-audit | PASS: PR `#31` security checkでknown vulnerability 0 |
| Real Discord API | Not run。token、Application、Guildを使用しない |
| Full DynamoDB Local suite | PASS: PR `#31` CIで固定digestを起動し、266 tests/92.55%で合格 |

### 17.4 Remaining scope

- STEP-07のprocess signal、起動時`resume_recoverable`、Gateway再接続deadline、outbox drain、fault injectionは未実装。
- 実Discord Guildでのcommand sync、permission、thread、component、disconnect smoke testはDiscord Application構成後に実施する。
- CloudWatch metric/alarm、container、AWS runtimeは後続stepで実装する。

### 17.5 PR・merge evidence

| Evidence | Result |
|---|---|
| Pull Request | [#31 Implement Discord interaction runtime](https://github.com/pitekusu/shittim-chest/pull/31) |
| Required checks | PASS: CI `29576189309`、CodeQL `29576187924`、GitGuardian。quality/tests/security/package/docs-public-safetyとCodeQL全job合格 |
| CI full suite | PASS: DynamoDB Local固定digest、266 passed、coverage 92.55%、pip-audit known vulnerability 0 |
| Squash merge | PASS: merge commit `9799cb9ad926280acd70b6434aef22b80758d435`、remote feature branch削除 |

## 18. STEP-07A Runtime lifecycle

### 18.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-07A-RUNTIME-LIFECYCLE` |
| Status | merged、PR `#33`、merge commit `0f386f5` |
| Date | 2026-07-17 |
| Base | `main` / `a172f11` |
| Requirements | D-002、D-005、D-006、RQ-011、RQ-012、RQ-017 |
| Test IDs | `UT-APP-004/005`、`CT-DISCORD-009/012`、`UT-RUNTIME-001`〜`004`、`AT-009` |
| External impact | None。実Discord token/Application/Guild、OpenAI、AWS、GitHub設定へ接続・変更していない |

`RuntimeAdmissionGateway`を追加し、process gateと既存の4 Bot物理READY、Guild/channel allowlistを全て満たす場合だけ`accept_debate`へREADYを返すfail-closed境界とした。`RuntimeLifecycle`はDiscord supervisor、interaction controller、application recoveryをSDK非依存Protocolで所有する。起動時は受付を閉じたまま4 READYを待ち、schema hash差分時だけGuild Commandをsyncし、`resume_recoverable`を所有taskとして開始してから受付を開く。

main-thread event loopへSIGINT/SIGTERM handlerを登録する`UnixSignalHandlers`を追加した。callbackは同期的に受付とinteraction dispatchを閉じてshutdown eventを設定し、再入は冪等である。shutdownはreadiness monitor、interaction-owned debate、startup recovery、controller、4 Discord clientの順にcancel/await/closeし、90秒で`RuntimeShutdownTimeout`へ切り替える。ECS `stopTimeout=120`との差30秒をruntime終了余裕として残す。

1 Botでも切断した時点で受付を閉じる。60秒未満なら進行taskを維持し、60秒連続切断ならinteraction/recovery taskをcancel・awaitして既存`run_debate` cancellation contractで同一phaseをCHECKPOINTEDへ保存する。通信断だけでFAILEDへ遷移しない。4 READYへ戻るとCommandを再syncせずrecoverable scanを1 taskだけ再起動し、受付を再開する。supervisor/recovery failureは受付を閉じてprocess境界へ再送出する。

Interaction controllerへ`begin_shutdown`と`checkpoint_active`を追加した。shutdown開始後のSlash CommandとRetryはapplication副作用またはdebate task開始前に`runtime_not_ready`として拒否する。checkpoint taskが通常例外で終了した場合は成功扱いにせずruntime errorへ変換する。`RuntimeLifecycle`はそのerrorを保持しながらcontroller/client/signal cleanupを継続し、完了後に`ExceptionGroup`としてprocess境界へ返す。STEP-07Aではshutdown中に新しいDiscord outbox deliveryを行わず、outbox drain/reconciliation、production bootstrap、実process/container fault injectionは後続STEP-07B/07C/08へ残す。

### 18.2 Official sources

| Checked | Target | Official source | Consequence |
|---|---|---|---|
| 2026-07-17 | Python 3.14.6 Unix signals | https://docs.python.org/3.14/library/asyncio-eventloop.html#unix-signals | main-thread loopの`add_signal_handler`/`remove_signal_handler`でSIGINT/SIGTERMを所有 |
| 2026-07-17 | Python 3.14.6 task groups | https://docs.python.org/3.14/library/asyncio-task.html#task-groups | recovery/checkpoint taskを所有しcancel後にawait |
| 2026-07-17 | discord.py Client readiness | https://discordpy.readthedocs.io/en/stable/api.html#discord.Client.wait_until_ready | `is_ready()`とidentity userを4 Bot受付条件へ維持 |
| 2026-07-17 | Fargate Spot termination | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/fargate-capacity-providers.html | Spot SIGTERM、configured `stopTimeout`後のSIGKILL、singleton停止を再確認 |
| 2026-07-17 | ECS ContainerDefinition | https://docs.aws.amazon.com/AmazonECS/latest/APIReference/API_ContainerDefinition.html | Fargate `stopTimeout`最大120秒に対し内部90秒deadlineを採用 |

### 18.3 Validation

| Check | Result |
|---|---|
| Focused runtime/Discord | PASS: 24 tests、network/secretなし |
| Ruff | PASS: format/check finding 0 |
| mypy strict | PASS: 83 source/test/tool files、error 0 |
| import-linter | PASS: 37 files、89 dependencies、runtimeを最上位とする1 contract kept |
| Full pytest | PASS: 273 passed、5 opt-in DynamoDB Local skipped、domain/application line/branch 92.55%。制限sandboxでは既存mock async testが停止したため、同一workspaceをhost実行して2.23秒で完走 |
| uv lock | PASS: 96 packages、lock変更なし |
| CycloneDX SBOM | PASS: schema 1.5、95 dependency components、lock inventory一致 |
| pip-audit | PASS: known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Docs/public surface | PASS: 14 canonical notes、Obsidian mirror byte一致、public-surface finding 0 |
| Real Discord/AWS | Not run。production Application、token、resource未構成 |

### 18.4 Remaining scope

- STEP-07Bの永続outbox drain/reconciliationとretry schedulingは本書19章のとおりPR `#34`でmerge済み。
- STEP-07Cはproduction bootstrapと実process SIGTERM/SIGKILLを実装・検証済み。STEP-08で各phase/transaction/Discord POST境界のcontainer fault injectionを行う。
- CloudWatch heartbeat/metric、EventBridge Spot event、ECS health/task definitionはSTEP-08/09で実装する。
- 実Discord Bot、production AWS、container fault injectionは未実施である。

### 18.5 PR・merge evidence

| Evidence | Result |
|---|---|
| Pull Request | [#33 Implement runtime lifecycle](https://github.com/pitekusu/shittim-chest/pull/33) |
| Required checks | PASS: `quality`、`tests`、`security`、`package`、`docs-public-safety`、CodeQL actions/Python、GitGuardian全件成功 |
| Final PR head | `72a95a77fe85136b1c3a6d5ab14841ee12e64922`、unresolved review thread 0、merge state `CLEAN` |
| Squash merge | PASS: merge commit `0f386f5c3f6b4b3e6c3af7fbb92ea250c9f1f05e`、remote feature branch削除 |

## 19. STEP-07B Outbox recovery

### 19.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-07B-OUTBOX-RECOVERY` |
| Status | merged、PR `#34`、merge commit `04bbda0` |
| Date | 2026-07-17 |
| Branch/base | `main` / `0f386f5` |
| Requirements | RQ-004、RQ-005、RQ-011、RQ-012 |
| Test IDs | `UT-APP-008`、`CT-DISCORD-013`、`CT-PERSIST-009`、`AT-009`〜`013` |
| External impact | None。実Discord token/Application/Guild、production AWS/OpenAI、GitHub設定を使用・変更していない |

`DiscordOutboxRecovery`を追加し、lease取得済みattemptの`SENT`以外のoperationをstrongly consistentかつpaginatedなbase-table Queryで全件取得する。future `next_retry_at`と未失効claimもreaderで落とさず、永続利用可能時刻までasync待機してから`DiscordPyPublisher`へ保存順で渡す。claim race時だけ1秒pollし、busy loopを避ける。

retryable Discord errorはpublisherが永続化したretry scheduleまたはclaim expiryを再読込みし、同じHTTP requestをapplication側で直接retryしない。nonretryable errorはprovider本文を保存せずstable Discord codeでattemptをFAILEDへする。`RepositoryConflict`はfencing喪失として旧workerがterminal化せず終了し、次ownerのclaim/reconciliationへ委ねる。cancelは握り潰さず、未送信recordを保持する。

`DebateApplication`はoutbox drainをphase処理より先、かつ同じ20秒lease heartbeat配下で実行する。Discord停止や長いretry待機はactive-processingの300秒deadlineへ含めず、drain完了後の討論phaseだけをsession timeoutで囲む。本文なしmetricとして`discord_outbox_recovered`と`discord_outbox_retry_scheduled`を追加した。DynamoDB schema、依存package、AWS resourceは変更していない。

### 19.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-17 | DynamoDB Query API | https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_Query.html | base table `ConsistentRead=true`、1 MB `LastEvaluatedKey` pagination、GSIを排他正本にしない設計を維持 |
| 2026-07-17 | DynamoDB TransactWriteItems | https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_TransactWriteItems.html | claim/mark/rescheduleのMETA・lease ConditionCheckとatomic writeを維持 |
| 2026-07-17 | Discord Message API | https://docs.discord.com/developers/resources/message | Get Channel Messages、Read Message History、nonce最大25、`enforce_nonce`直近数分を再確認 |
| 2026-07-17 | Python 3.14 asyncio timeout/cancellation | https://docs.python.org/3.14/library/asyncio-task.html#timeouts | outbox待機をactive deadline外へ分離し、task cancelを再送出 |
| 2026-07-17 | boto3/boto3-stubs 1.43.50 | https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/dynamodb.html | client再利用、同期Queryのworker thread隔離、typed境界を維持 |

### 19.3 Validation

| Check | Result |
|---|---|
| Outbox recovery contracts | PASS: future retry、claim expiry/race、retryable/nonretryable、chunk順、cancel、poll validation |
| Application recovery | PASS: phase前drain、長期waitのdeadline分離、wait中lease renewal、stable error、fencing引継ぎ |
| DynamoDB Local 3.3.0 | PASS: CI固定digest `sha256:d89f8fcc6b1a39cb35976c248ed42a28c66ae00dc043099210f5571e42648ab4`を一時起動しfuture/claimedを含む`list_pending`を確認後、停止・自動削除 |
| Full pytest | PASS: 287 passed、coverage 92.52%。実network/APIなし |
| Ruff | PASS: 85 files format、lint finding 0 |
| mypy strict | PASS: 85 source/test/tool files、error 0 |
| import-linter | PASS: 38 files、95 dependencies、1 contract kept/0 broken |
| uv lock | PASS: 96 packages、dependency変更なし |
| CycloneDX SBOM | PASS: schema 1.5、95 dependency components、lock inventory一致 |
| pip-audit | PASS: hash固定requirements、known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Real Discord/AWS/OpenAI | Not run。production Application、token、resource、paid API未使用 |

### 19.4 Failed・superseded runs and findings

- 65件のfocused suite自体は全件PASSしたが、全project coverage母集団を含まないため74.18%となり90% gateを満たさなかった。正式判定は最終全287件で92.52%とした。
- 制限sandbox内のfull pytestは既存async mock test付近で出力が途中終了した。host側で同じlocked environmentを再実行し、途中版はDynamoDB Localなしで280 passed/5 skipped、最終版は固定DynamoDB Localありで287 passedを確認した。
- sandbox内pip-auditは一時環境のpip更新通信で失敗した。hash固定requirementsを変更せずhost側で再実行し、known vulnerability 0を確認した。
- 初期案ではoutbox waitを既存300秒session timeout内に置いていたが、Discord長期障害でpending deliveryをFAILED終端化するため不採用とした。lease heartbeatは維持しつつ、phase active-processing timeoutだけをdrain後へ適用した。
- Discord send成功後のfenced `mark_sent`競合をgeneric phase failureへ変換するとpending recordをterminal attemptへ閉じ込めるため、`RepositoryConflict`はterminal化せず後続ownerへ委ねる規則に修正した。

### 19.5 Remaining scope

- STEP-07Cでproduction compositionと実process SIGTERM/SIGKILLを実装済み。STEP-08でcontainer SIGTERM/SIGKILL、Discord POST直前/直後とDynamoDB transaction境界のfault injectionを行う。
- 実GuildでのRead Message History、権限剥奪、locked/archive thread、長時間rate limit smokeはDiscord Application構成後に行う。
- CloudWatch metric emitter、outbox age alarm、dashboardはSTEP-09/11で実装する。
- PR `#34`、required checks、CodeQL、managed Dependency Graphは全件合格し、merge commit `04bbda004a68cfafa177056eaa0ed9bf0e0dac93`となった。

### 19.6 PR・merge evidence

| Evidence | Result |
|---|---|
| Pull Request | [#34 Implement outbox recovery](https://github.com/pitekusu/shittim-chest/pull/34) |
| Required checks | PASS: main CI run `29581007869`、CodeQL run `29581007285`を含む必須check成功 |
| Squash merge | PASS: merge commit `04bbda004a68cfafa177056eaa0ed9bf0e0dac93`、remote feature branch削除 |

## 20. STEP-07C Production composition・process fault injection

### 20.1 Metadata・scope

| Field | Value |
|---|---|
| Step ID | `STEP-07C-PRODUCTION-COMPOSITION` |
| Status | merged、PR `#35`、merge commit `e863ae3` |
| Date | 2026-07-17 |
| Branch/base | `agent/step-07c-production-composition` / `04bbda0` |
| Requirements | RQ-004、RQ-008、RQ-011、RQ-012、RQ-016 |
| Test IDs | `UT-RUNTIME-005`、`IT-RUNTIME-001`、`CT-OPENAI-010`、`ST-Privacy` |
| External impact | None。実Discord token/Application/Guild、production AWS/OpenAI、GitHub設定を使用・変更していない |

`config/models.py`へstrict/frozen/extra-forbidのprocess、runtime、persona設定を追加した。全4 slot、version一致、Guild/channel/Application snowflake、DynamoDB table名、token一意性、prompt UTF-8 3,500 bytesをSDK client作成前に検証する。失敗はprivate値やPydantic本文を出さず安定code `startup_configuration_invalid`で終了する。公開`.env.example`はgeneric placeholderだけを保持する。

`bootstrap.py`を唯一のproduction composition rootとし、DynamoDB client 1つ、`AsyncOpenAI` client 1つ、共通Semaphore 1つ、Discord client 4つをprocess単位で生成・再利用する。repository、publisher、outbox recovery、application、interaction controller、lifecycleを既存Protocol境界で接続し、本番Policyはruntime入力で変更できないLuna standardに固定した。`ProductionRuntime.aclose()`はDiscord、OpenAI、DynamoDB clientを冪等にcloseする。`python -m shittim_chest`は設定不正をexit 2、runtime failureをexit 1、正常終了をexit 0とし、本文なしJSON errorだけを出力する。

`SystemClock`、UUIDv7 generator、暗号学的candidate order、衝突しないlease owner、content-free telemetryをproduction primitiveとして追加した。telemetryはevent、時刻、model/Policy、token、latency、response ID等の許可済みmetadataだけを受理し、質問、回答、persona prompt、credentialを記録しない。root loggerはWARNING以上へ固定し、設定可能なDEBUG/INFOは`shittim_chest` loggerだけへ適用して第三者SDKのHTTP debug logを誤って有効化しない。

実Unix subprocessを使うfault fixtureを追加し、SIGTERMで受付停止、checkpoint、interaction/client cleanupが行われることを確認した。SIGKILLではcleanupされずactive永続stateが残り、replacement processが検出・resumeできることを確認した。これはprocess境界の試験であり、container/ECS境界はSTEP-08へ残す。

### 20.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-17 | Python 3.14.6 Unix signals/subprocess | https://docs.python.org/3.14/library/asyncio-eventloop.html#unix-signals、https://docs.python.org/3.14/library/subprocess.html#popen-objects | main-thread signal ownershipと実child process terminate/killを検証 |
| 2026-07-17 | Pydantic 2.13.4 model validation | https://pydantic.dev/docs/validation/latest/concepts/models/ | strict/frozen/extra-forbid JSON設定境界を採用 |
| 2026-07-17 | OpenAI Responses create | https://developers.openai.com/api/reference/resources/responses/methods/create | process-scoped `AsyncOpenAI`再利用と既存`store=false` request shapeを維持 |
| 2026-07-17 | Discord Gateway | https://docs.discord.com/developers/events/gateway | GUILDS-only Intent、4 client READY後のfail-closed受付を維持 |
| 2026-07-17 | ECS SSM Parameter injection | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/secrets-envvar-ssm-paramstore.html | version付きprivate JSON/credentialをtask起動時環境へ注入する境界を採用 |
| 2026-07-17 | boto3 DynamoDB client close | https://docs.aws.amazon.com/boto3/latest/reference/services/dynamodb/client/close.html | runtime ownerが共有clientを明示close |

### 20.3 Validation

| Check | Result |
|---|---|
| Production config/composition | PASS: valid/invalid/version/duplicate/private-value redaction、実composition build、全client cleanup |
| Process fault injection | PASS: actual SIGTERM graceful checkpoint/cleanup、actual SIGKILL active-state残存とreplacement recovery、entry point fail-closed |
| Full pytest | PASS: CIと同じ`DYNAMODB_ENDPOINT_URL`でDynamoDB Local 5件を含む304 passed、coverage 92.52%。実外部network/APIなし |
| Ruff | PASS: format/check finding 0 |
| mypy strict | PASS: 97 source/test/tool files、error 0 |
| import-linter | PASS: 43 files、110 dependencies、1 contract kept/0 broken |
| uv lock | PASS: 96 packages、dependency変更なし |
| CycloneDX SBOM | PASS: schema 1.5、95 dependency components、lock inventory一致 |
| pip-audit | PASS: hash固定requirements、known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Public surface | PASS: private ID、credential、absolute home path、private persona finding 0 |
| Real Discord/AWS/OpenAI | Not run。production Application、token、resource、paid API未使用 |

### 20.4 Failed・superseded runs and findings

- 最終15件のfocused testは全件PASSした。初回14件もPASSしたが、全project coverage母集団を含まないため41.82%でglobal 90% gateを満たさなかった。focused確認は`--no-cov`で再実行し、正式判定はDynamoDB Localを含む全304件・92.52%とした。
- `DYNAMODB_ENDPOINT_URL`なしの中間full runは297 passed・5 skippedで正常終了した。CI相当の正式runでは固定digestのDynamoDB Local endpointを明示し、skip 0・302 passedを確認した。
- startup validationをSDK client作成後に行う案は、無効設定で外部副作用が発生し得るため不採用とした。全private設定を先に一括検証してからcompositionする。
- model IDをenvironmentへ公開する案は、本番Luna standard固定の決定を迂回できるため不採用とした。評価用Terra/pro Policyはproduction compositionから到達不能に保つ。

### 20.5 Remaining scope

- STEP-08でARM64 production container、read-only/non-root/capability削除、health、container境界SIGTERM/SIGKILL、最終image SBOMを実装する。
- 実Discord Application/token、production AWS/DynamoDB/OpenAI、ECS taskへは接続していない。
- Discord POSTとDynamoDB transactionの直前/直後SIGKILL、CloudWatch EMF/alarm、ECS Spot eventはSTEP-08/09/11で検証する。

### 20.6 PR・merge evidence

| Evidence | Result |
|---|---|
| Pull Request | [#35 Implement production runtime composition](https://github.com/pitekusu/shittim-chest/pull/35) |
| Final PR head | `ba12a1b926f45ae8fa4427250c1d5420d7e86766`、unresolved review thread 0、merge state `CLEAN` |
| PR checks | PASS: CI run `29583410970`、CodeQL run `29583409802`、GitGuardianを含む全check成功 |
| Squash merge | PASS: merge commit `e863ae33ace3e89d63ee5e19015a042bc1d4950e`、remote feature branch削除 |
| Main verification | PASS: CI run `29583519941`、CodeQL run `29583517963`とも成功 |

## 21. STEP-08A Production container foundation

| Field | Value |
|---|---|
| Step ID | `STEP-08A-PRODUCTION-CONTAINER-FOUNDATION` |
| Status | merged、PR `#37`、merge commit `7742f0b` |
| Date | 2026-07-17 |
| Branch/base | `agent/implement-step-08a-container-foundation` / `2603a7c` |
| Requirements | RQ-004、RQ-007、RQ-008、RQ-011、RQ-012、RQ-015 |
| External impact | None。local imageだけを作成し、registry、AWS、Discord、OpenAIへ接続していない |

### 21.1 Implementation

- Python `3.14.6-slim-trixie`とuv `0.11.29`のmulti-arch index digestを直接固定したmulti-stage `Dockerfile`を追加した。
- production imageはUID/GID `10001`、exec entrypoint、`SIGTERM`、dev dependencyなしとし、uv、cache、raw source、test、shell/proc utilityを含めない。
- `break-glass` targetだけへ`/bin/sh`、`cat`、`script`、`ps`を追加し、通常実行userはnon-rootのままにした。
- process ownerが5秒ごとにatomic heartbeatを書き、health commandが20秒以内の更新、PID形式、process生存だけをcontent-freeで検証する。Discord/OpenAI可用性はhealth判定へ含めない。
- `.dockerignore`をallowlist方式にし、build contextからGit履歴、docs、test、local artifact、secret候補を除外した。
- Docker ecosystemをDependabotの週次version update対象へ追加した。

### 21.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-17 | ECS task definition parameters | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html | ARM64、health、read-only root、stop timeoutをtask境界へ分離 |
| 2026-07-17 | Fargate task differences | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/fargate-tasks-services.html | Fargateのtmpfs非対応を確認しSTEP-09 bind volumeへ分離 |
| 2026-07-17 | Python official image 3.14.6 | https://hub.docker.com/_/python | amd64/arm64対応slim-trixie index digestを固定 |
| 2026-07-17 | Docker build best practices | https://docs.docker.com/build/building/best-practices/ | multi-stage、最小base、digest固定、`.dockerignore` |
| 2026-07-17 | uv Docker integration 0.11.29 | https://docs.astral.sh/uv/guides/integration/docker/ | version/digest固定、frozen/non-editable install、cache非同梱 |

### 21.3 Local container validation

| Check | Result |
|---|---|
| production/break-glass build | PASS: Podman Docker-formatで両targetをbuild |
| Image configuration | PASS: UID/GID `10001`、exec entrypoint、`SIGTERM`、health 10s/3s/30s/3回 |
| Runtime security | PASS: read-only root、capability effective set 0、no-new-privileges、`/tmp`だけ書込み可 |
| Runtime minimization | PASS: productionにuv、raw source、test、`ps`、build cacheなし |
| Break-glass tools | PASS: `/bin/sh`、`cat`、`script`、`ps`あり。application userはnon-root |
| Heartbeat health | PASS: event-loop heartbeat中はexit 0、欠落時は本文出力なしでexit 1 |
| Architecture | amd64 local buildのみPASS。native ARM64はSTEP-08B required gateへ保留 |
| Full pytest | PASS: CIと同じDynamoDB Local 3.3.0を含む312 passed、coverage 92.52%、skip 0 |
| Ruff | PASS: 99 files format済み、lint finding 0 |
| mypy strict | PASS: 99 source/test/tool files、error 0 |
| import-linter | PASS: 44 files、111 dependencies、1 contract kept/0 broken |
| uv lock | PASS: 96 packages、lock変更なし |
| CycloneDX source SBOM | PASS: schema 1.5、95 dependency components |
| pip-audit | PASS: hash固定requirements、known vulnerability 0 |
| Package build | PASS: sdistと`py3-none-any` wheel |
| Public docs/surface | PASS: 14 canonical note、140 public files、finding 0、Obsidian mirror byte一致 |

### 21.4 Failed・superseded runs

- sandbox内の初回uv実行はdefault cache pathがread-onlyで開始前に失敗した。`UV_CACHE_DIR=/tmp/uv-cache`を明示して再実行する。
- Podman既定OCI image formatはDockerfile `HEALTHCHECK`を保持しないため、検証・CIは`--format docker`へ統一した。
- 初回helper containerのone-line Python構文を誤りexitした。修正後に同じread-only/capability条件で再実行してPASSした。

### 21.5 Remaining scope

- STEP-08Bでnative ARM64 CI、container SIGTERM/SIGKILLとphase/transaction/Discord POST境界fault、最終image SPDX SBOMを実装する。
- STEP-09でFargate `initProcessEnabled`、read-only root、`/tmp` bind volume、`stopTimeout=120`、capability削除、healthをtask definitionへ接続する。
- image push、ECR、AWS resource、実Discord/OpenAI接続、追加課金は行っていない。

### 21.6 PR・merge evidence

| Evidence | Result |
|---|---|
| Pull Request | [#37 Implement production container foundation](https://github.com/pitekusu/shittim-chest/pull/37) |
| Final PR head | `fddc2cc5327f0e0898b1844204813699118863f2`、unresolved review thread 0、merge state `CLEAN` |
| PR checks | PASS: CI run `29587699969`、CodeQL run `29587696005`、GitGuardianを含む全check成功 |
| Squash merge | PASS: merge commit `7742f0be2c3297e4b2d29d8ff7e65bf3f457044d`、remote feature branch削除 |
| Main verification | PASS: CI run `29587826104`、CodeQL run `29587825160`、Dockerを含むDependabot初回3 runがすべて成功 |

## 22. STEP-08B Native ARM64・container fault・image SBOM

| Field | Value |
|---|---|
| Step ID | `STEP-08B-ARM64-CONTAINER-GATE` |
| Status | 完了・merge済み |
| Date | 2026-07-17 |
| Branch/base | `agent/implement-step-08b-arm64-container-gate` / `2388537` |
| Requirements | RQ-004、RQ-007、RQ-008、RQ-011、RQ-012、RQ-015 |
| Test IDs | `CT-CONTAINER-001`〜`003`、`ST-IMAGE-SBOM-001` |
| External impact | GitHub ActionsとRulesetだけを変更。registry、AWS、Discord、OpenAIへ接続しない |

### 22.1 Implementation

- Public repositoryで無料のnative `ubuntu-24.04-arm`を使う`container-arm64` jobを追加した。QEMUとself-hosted runnerは使用しない。
- productionとCI専用`fault-test` targetをARM64でbuildし、runner/image architecture、numeric non-root、read-only root、`/tmp`、capability削除、no-new-privileges、entrypoint、SIGTERM、healthを検証する。
- fault fixtureは`fault-test` targetだけへcopyする。production/break-glass image、wheel、application packageへtest moduleを混入させない。
- 全7つの非終端phaseでPID 1へSIGTERMを送り、120秒未満のexit 0と同phase checkpointを検証する。
- transactionとDiscord POSTの直前/直後でPID 1へSIGKILLし、replacement後にtransaction event 1件、content hash一致Discord message 1件、outbox completedを検証する。実DynamoDB/Discordではなく既存adapter contractと組み合わせるdurable file fixtureであり、外部APIを呼ばない。
- Syft v1.48.0 arm64 archiveをrelease asset SHA-256で固定し、週次latest release監視へ追加した。production imageからSPDX 2.3 JSONを生成し、Debian OS packageとproduction Python package、dev dependency非混入を検証して30日artifactとして保持する。
- `container-arm64`をmain Ruleset ID `19047946`のrequired status checkへ追加した。release digestのprovenance/SBOM attestationはSTEP-10で実装する。

### 22.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-17 | GitHub native ARM64 runner GA | https://github.blog/changelog/2025-08-07-arm64-hosted-runners-for-public-repositories-are-now-generally-available/ | Public repositoryで`ubuntu-24.04-arm`をnative required gateへ採用 |
| 2026-07-17 | GitHub-hosted runners | https://docs.github.com/en/actions/concepts/runners/github-hosted-runners | GitHub管理runner、public PRへsecret/self-hosted権限なし |
| 2026-07-17 | Syft 1.48.0 | https://github.com/anchore/syft/releases/tag/v1.48.0 | fixed arm64 release archive、SPDX JSON generation |
| 2026-07-17 | Syft image SBOM | https://github.com/anchore/syft | container OS/Python package inventory |
| 2026-07-17 | ECS ARM64 | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-arm64.html | ARM64 production compatibility gateをCDK接続前条件に維持 |
| 2026-07-17 | ECS task definition parameters | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html | SIGTERM、health、read-only、ARM64 task境界を照合 |

### 22.3 Local validation

| Check                         | Result                                                                                                          |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------- |
| production/fault-test build   | PASS: local amd64 Podman Docker-format build                                                                    |
| Container gate                | PASS: 7 phase SIGTERM、4 forced boundary SIGKILL/replacement、security/health contract                            |
| Image SPDX                    | PASS: Syft v1.48.0、87 Debian package、35 production Python package、dev package 0                                 |
| Native ARM64                  | PASS: PR CI run `29590709690`で53秒、main CI run `29590899427`で52秒。QEMUなし                                          |
| ARM64 image artifact          | PASS: PR artifact ID `8410938007`、main artifact ID `8411015500`。2,479,295 bytes、87 Debian/35 Python packageを再検証 |
| Full pytest                   | PASS: DynamoDB Localを含む324 passed、coverage 92.52%、skip 0                                                        |
| Ruff/mypy/import-linter       | PASS: 104 files format/lint、strict typing error 0、1 architecture contract kept                                  |
| Source SBOM/pip-audit/package | PASS: CycloneDX 95 components、known vulnerability 0、sdist/wheel build成功                                         |
| Docs/public/workflow          | PASS: 14 note mirror一致、145 public files finding 0、actionlint finding 0                                          |

### 22.4 Failed・superseded runs

- 初回local inspectはDocker EngineとPodmanでhealth metadata位置が異なり失敗した。同じDocker-format metadataを`Config.Healthcheck`またはtop-level `Healthcheck`から厳密検査するよう修正した。
- 初回fault runはrootless Podman/SELinux bind mountとumask後の0755 directoryによりUID 10001がready markerを書けず失敗した。private temp directoryを明示chmod 0777し、bind mountへ`Z` relabelを指定して再実行し全scenarioがPASSした。
- PR初回ARM64 run `29590491815`はrunner hostのPython 3.12からdomainをimportし、Python 3.14専用`uuid.uuid7()`で停止した。gate toolを標準ライブラリだけで起動可能にし、固定phase一覧とdomain状態機械の一致をunit testへ追加した。修正後のPR/main native ARM64 runはPASSした。
- QEMU cross-buildはnative runtime互換性の証跡にならないため採用しない。

### 22.5 Remaining scope

- STEP-09でARM64 Fargate task definition、`stopTimeout=120`、init、read-only root、`/tmp` bind volume、healthをAWS構成へ接続する。
- STEP-10でECRへpushした単一digestからrelease SBOMを再生成し、provenanceとSBOMを別々にattestする。
- 実AWS、DynamoDB、Discord、OpenAI、registry push、paid APIは未使用である。

### 22.6 PR・merge evidence

| Check | Result |
|---|---|
| Pull Request | PASS: PR `#39`、verified head `7816231e17ee43d61d715756bc5d0dd79302ebd6` |
| PR checks | PASS: CI run `29590709690`、CodeQL run `29590703755`、GitGuardian。review 0件、未解決thread 0件 |
| ARM64 artifact | PASS: artifact ID `8410938007`をAPIから取得し、87 Debian/35 Python packageをvalidatorで再確認 |
| Squash merge | PASS: merge commit `2cca51a7ee6c9ca60274c13c5aad695c549b396e`、remote feature branch削除 |
| Main verification | PASS: CI run `29590899427`、CodeQL run `29590898580`、ARM64 artifact ID `8411015500` |
| Ruleset | PASS: ID `19047946`。既存5 checks、PR/squash/linear history、削除・force-push禁止、CodeQLを維持し`container-arm64`を追加。bypass actor 0 |

## 23. ty parallel type-check gate

| Field | Value |
|---|---|
| Step ID | `QUALITY-TY-PARALLEL` |
| Status | 実装済み・PR検証合格 |
| Date | 2026-07-19 |
| Branch/base | `agent/add-ty-parallel-type-checking` / `a958bc7` |
| Test IDs | `QT-Mypy`、`QT-Ty`、`QT-Ruff`、`QT-Lock`、`QT-Workflow` |
| External impact | GitHub Actionsの既存`quality` jobだけを変更。AWS、Discord、OpenAI、registryへ接続しない |

### 23.1 Implementation

- ty 0.0.61をdev dependencyと`uv.lock`へ追加し、Dependabotのuv更新対象に含めた。
- `[tool.ty.src]`で`src`、`tests`、`tools`を全て対象にし、`missing-type-argument`をerror、`possibly-unresolved-reference`をwarningとして有効化した。
- CIの既存required `quality` jobで`mypy`と`ty check`を連続実行する。tyはbetaのためmypy strictを置換せず、両方を独立gateとして維持する。
- 初回ty実行の67 diagnosticsは規則カテゴリを無効化せず解消した。JSON境界のruntime validation後に型を明示し、Outbox conflict再読込みでは`None`を明示的に排除した。
- production imageの禁止dev package一覧へtyを追加し、runtime imageへの混入を拒否する。

### 23.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | ty 0.0.61 | https://github.com/astral-sh/ty | beta・0.0.x・高速型検査としてmypyとの並列運用を選択 |
| 2026-07-19 | Python version | https://docs.astral.sh/ty/python-version/ | projectの`requires-python`から3.14を解決 |
| 2026-07-19 | mypy/Pyright migration | https://docs.astral.sh/ty/coming-from-mypy-or-pyright/ | strict相当規則と未実装差分を確認し、即時置換を見送った |
| 2026-07-19 | ty configuration | https://docs.astral.sh/ty/configuration/ | `pyproject.toml`のproject-local設定を採用 |

### 23.3 Local validation

| Check | Result |
|---|---|
| Baseline mypy | PASS: 104 source/test/tool files、error 0 |
| Initial ty assessment | 67 diagnostics。application 1件、tests/tools 66件を分類 |
| ty after implementation | PASS: `src`、`tests`、`tools`、diagnostic 0 |
| Ruff/mypy/import-linter | PASS: 104 files format/lint、mypy error 0、ty diagnostic 0、1 architecture contract kept |
| Lock | PASS: 97 packages、ty 0.0.61を固定 |
| Focused tests | 106 testsが全test bodyを通過したが、既知のrestricted sandbox Python executor shutdown待機でprocessを手動終了。正式判定はPR CIのfull suiteで行う |
| Source SBOM/audit/package | PASS: CycloneDX 96 components、known vulnerability 0、sdist/wheel build成功 |
| Docs/public/workflow | PASS: 14 note mirror一致、145 public files finding 0、actionlint 1.7.12 finding 0、patch whitespace error 0 |

### 23.4 Remaining evidence

- local restricted sandboxではPodman runtime pathがread-onlyでDynamoDB Localを起動できず、Python executor shutdownも完了しなかった。DynamoDB Localを提供するGitHub-hosted runnerのfull suiteを正式判定とした。
- tyがstableになるか、少なくとも後続2〜3 stepでmypyだけが検出する差分がないことを確認するまでmypyを撤去しない。

### 23.5 PR evidence

| Evidence | Result |
|---|---|
| Pull Request | [#41 Add ty parallel type checking](https://github.com/pitekusu/shittim-chest/pull/41) |
| CI run | PASS: `29679124835`。quality 25秒、tests 33秒、security 36秒、package 12秒、docs-public-safety 10秒、container-arm64 51秒 |
| Type gates | PASS: 同じrequired `quality` jobでmypy strictとty 0.0.61が`src`、`tests`、`tools`全体を検査 |
| Managed checks | PASS: CodeQL Actions/Python、CodeQL aggregate、GitGuardian Security Checks |
| Merge | PASS: PR #41をsquash merge。main commit `94f258c`、remote feature branch削除 |

## 24. uv Docker dependency-layer cache optimization

| Field | Value |
|---|---|
| Step ID | `CONTAINER-UV-LAYER-CACHE` |
| Status | 実装・merge・main検証合格 |
| Date | 2026-07-19 |
| Branch/base | `agent/optimize-uv-docker-layers` / `94f258c` |
| Test IDs | `QT-CONTAINER-CACHE-001`、`CT-CONTAINER-001`〜`003` |
| External impact | Dockerfileと文書だけを変更。AWS、Discord、OpenAI、registryへ接続しない |

### 24.1 Implementation

- `pyproject.toml`と`uv.lock`をsourceより先にcopyし、`uv sync --frozen --no-dev --no-install-project --no-editable`でthird-party dependency layerを生成する。
- `README.md`、`LICENSE`、`src/`をcopyした後の2回目syncでprojectだけを追加し、source-only変更で依存layerを無効化しない。
- `UV_NO_CACHE=1`を撤去し、両syncへ`--mount=type=cache,target=/root/.cache/uv,sharing=locked`を指定した。cacheはbuilderだけで使用しruntime imageへcopyしない。
- `UV_PYTHON_DOWNLOADS=0`を指定し、digest固定したPython base以外のinterpreterをuvが取得しないようfail closedにした。

### 24.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | uv Docker integration 0.11.29 | https://docs.astral.sh/uv/guides/integration/docker/ | `--no-install-project`の中間layer、cache mount、`UV_LINK_MODE=copy`、`UV_PYTHON_DOWNLOADS=0`を採用 |
| 2026-07-19 | Docker build cache | https://docs.docker.com/build/cache/optimize/ | dependency manifest先行copyとcache mountを採用 |

### 24.3 Local validation

| Check | Result |
|---|---|
| Cold production build | PASS: Podman Docker-format build。dependency 33 packagesを先行installし、project 1 packageを後段install |
| Warm production build | PASS: builderの9 stepsを含む全production build stepで`Using cache`を確認 |
| fault-test build | PASS: production共通layerを再利用してCI-only fault targetをbuild |
| Container gate | PASS: Docker互換Podman shimでnative amd64 security/health、7 phase SIGTERM、4 boundary SIGKILL/replacementを完走 |
| Runtime boundary | PASS: non-root/read-only/capability/health contractを維持。AWS、Discord、OpenAI、registry通信なし |

### 24.4 Remaining evidence

- native ARM64の正式判定、image SPDX、全source quality/testはPRの既存required checksで再検証する。
- GitHub Actions間での外部cache共有はこのsliceへ含めず、直後の独立PRでBuildxとGHA cache backendを導入する。

### 24.5 PR evidence

| Evidence | Result |
|---|---|
| Pull Request | [#42 Optimize uv Docker build layers](https://github.com/pitekusu/shittim-chest/pull/42) |
| CI run | PASS: `29679874886`。quality 30秒、tests 34秒、security 50秒、package 12秒、docs-public-safety 12秒、native ARM64 container 61秒 |
| Managed checks | PASS: CodeQL Actions/Python/aggregate、GitGuardian Security Checks |
| Runtime boundary | PASS: registry push、secret、OIDC、AWS、Discord、OpenAI、paid APIなし |
| Merge | PASS: squash commit `3c43c49`、remote feature branch削除 |
| Main verification | PASS: CI run `29679982674`、CodeQL run `29679982260` |

## 25. Buildx・GitHub Actions cache integration

| Field | Value |
|---|---|
| Step ID | `CI-BUILDX-GHA-CACHE` |
| Status | 実装・merge・main初回/再実行検証合格 |
| Date | 2026-07-19 |
| Branch/base | `agent/use-buildx-gha-cache` / `3c43c49` |
| Test IDs | `QT-GHA-BUILDX-CACHE-001`、`CT-CONTAINER-001`〜`003`、`ST-IMAGE-SBOM-001` |
| External impact | GitHub Actionsの`container-arm64` jobと文書だけを変更。image push、AWS、Discord、OpenAI、paid APIなし |

### 25.1 Implementation

- shellの`docker build` 2回を、full SHA固定したDocker公式`setup-buildx-action` v4.2.0と`build-push-action` v7.3.0へ置換した。
- production/fault-testを別stepで`load: true`にし、後続のnative container gateとSyftがlocal Docker daemon上の同一tagを検査する既存contractを維持した。
- production buildは`type=gha,scope=container-arm64-production,mode=max`をimport/exportする。fault buildは同一jobのBuildx builderに残るproduction共通layerを再利用し、GHA cache容量へtest-only layerを重複保存しない。
- cache exportへ`ignore-error=true`を指定した。cacheは性能最適化のため、GitHub cache障害またはevictionでもbuild/test correctnessを失敗させない。
- workflow permissionは`contents: read`のまま、registry push、secret、OIDC、`pull_request_target`を追加しない。repository selected ActionsにはDocker公式2 actionが既に許可され、full SHA pin強制も有効である。
- Buildx job summaryと小容量の診断用`.dockerbuild` recordを生成し、image SBOMと同じ30日保持へ固定した。

### 25.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | setup-buildx-action v4.2.0 | https://github.com/docker/setup-buildx-action/releases/tag/v4.2.0 | full SHA `bb05f3f5519dd87d3ba754cc423b652a5edd6d2c`で固定 |
| 2026-07-19 | build-push-action v7.3.0 | https://github.com/docker/build-push-action/releases/tag/v7.3.0 | full SHA `53b7df96c91f9c12dcc8a07bcb9ccacbed38856a`、`load`、cache inputsを採用 |
| 2026-07-19 | GitHub Actions cache backend | https://docs.docker.com/build/cache/backends/gha/ | ref access restriction、scope、runtime token自動利用を設計へ反映 |
| 2026-07-19 | Docker cache optimization | https://docs.docker.com/build/cache/optimize/ | external cacheとcache mountの責務を分離 |

### 25.3 PR validation

| Evidence | Result |
|---|---|
| Pull Request | [#43 Use Buildx GitHub Actions cache](https://github.com/pitekusu/shittim-chest/pull/43) |
| Initial CI | PASS: run `29680086830` attempt 1。全required checks合格、native ARM64 container 90秒 |
| GHA cache export | PASS: `refs/pull/43/merge` scopeへproduction `mode=max` cacheをexport |
| Same-head rerun | PASS: run `29680086830` attempt 2、job `88174693535`。GHA manifestをimportしproduction主要10 layerとfault共通layerで`CACHED`を確認 |
| Cache effect | PASS: native ARM64 job 90秒から72秒へ短縮。cache hit後もcontainer gate合格 |
| Image SPDX | PASS: cache hit後もDebian 87 package、production Python 35 package、dev package 0 |
| Build records | PASS: production/faultの`.dockerbuild` artifactを生成。30日保持へ固定 |
| Security boundary | PASS: `contents: read`、push false、PR ref cache read/writeとmain cache readだけ。secret、OIDC、registry pushなし |

### 25.4 Merge・main evidence

| Evidence | Result |
|---|---|
| Merge | PASS: PR #43をsquash merge。main commit `90f3012`、remote feature branch削除 |
| Main initial run | PASS: CI run `29680315717` attempt 1、CodeQL run `29680315360`。全job合格、native ARM64 89秒、main scope cache生成 |
| Main cache rerun | PASS: CI run `29680315717` attempt 2、job `88175330877`。main scope manifest import、production主要10 layerとfault共通layerで`CACHED` |
| Main cache effect | PASS: native ARM64 job 89秒から71秒へ短縮。container gateとDebian 87/Python 35 package SBOMが再度合格 |
| Repository state | PASS: selected Docker Actions許可、full SHA pin必須、`contents: read`、Ruleset required `container-arm64`を維持 |

### 25.5 Remaining evidence

- merge後のmain初回runはPR ref cacheを共有せずmain scopeを新規生成する。以降のmain/PR runがdefault branch cacheをreadできることをGitHubのref access restrictionに従って運用する。
- release image cache、ECR push、attestationはSTEP-10で別scope・別workflowとして設計し、test image cacheと混在させない。

## 26. Local DynamoDB Local runner

| Field | Value |
|---|---|
| Step ID | `QUALITY-DYNAMODB-LOCAL-RUNNER` |
| Status | 実装・local/PR検証合格、draft review中 |
| Date | 2026-07-19 |
| Test IDs | `QT-DYNAMODB-LOCAL-RUNNER-001`、`CT-PERSIST-007`〜`009` |
| External impact | local test helperと文書のみ。AWS resource、credential、Discord、OpenAI、GitHub設定を変更しない |

### 26.1 Implementation

- `tools/run_dynamodb_local.py`を追加した。Podmanを優先してDockerへfallbackし、CIと同じDynamoDB Local 3.3.0 digestを`-inMemory -sharedDb -disableTelemetry`で起動する。
- 固定host port 8000を使わず、PID由来の一時containerをrandom loopback portへpublishする。port openだけでなくdummy credentialを使うsigned `ListTables`が成功するまで待機する。
- `DYNAMODB_ENDPOINT_URL`はchild commandへだけ注入する。既定commandは`uv run --frozen pytest`で、完了・失敗を問わずhelper自身のcontainerだけをstopし`--rm`で削除する。
- Codex制限sandboxではrootless Podmanの`/run/user/1000/libpod`がread-onlyで、Podmanがservice接続前に停止することを再現した。helperをhost executionへ切り替えることでrepository/image変更なしに解決した。

### 26.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | DynamoDB Local v3 | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.DownloadingAndRunning.html | v3.x、Docker image、local endpoint、dummy credentialを確認 |
| 2026-07-19 | DynamoDB Local usage | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.UsageNotes.html | `-inMemory`、`-sharedDb`、`-disableTelemetry`、port差替え、productionとの差異を確認 |

### 26.3 Local validation

| Check | Result |
|---|---|
| Restricted sandbox diagnosis | PASS: `podman info`が`/run/user/1000/libpod` read-only sticky-bit failureで停止することを確認 |
| Host runtime | PASS: rootless Podman socket `/run/user/1000/podman/podman.sock`、port 8000空きを確認 |
| Focused persistence integration | PASS: helper経由でDynamoDB Localを起動し5 passed |
| Full regression | PASS: helper既定commandで331 passed、coverage 92.52%、DynamoDB Local integration skip 0 |
| Cleanup | PASS: `shittim-dynamodb-local-*` containerは全試験後0件 |
| Static checks | PASS: Ruff format/check、mypy、ty、helper unit 6 passed（この時点の実績。後続の27章でtyへ一元化） |

## 27. ty single type-check gate

| Field | Value |
|---|---|
| Step ID | `QUALITY-TY-SINGLE-GATE` |
| Status | 実装・local検証合格、PR検証待ち |
| Date | 2026-07-19 |
| Test IDs | `QT-Ty`、`QT-Ruff`、`QT-Lock`、`QT-Workflow` |
| External impact | Python dev dependency、CI quality job、文書のみ。AWS、Discord、OpenAI、registryへ接続しない |

### 27.1 Implementation

- `mypy`とそのtransitive dependencyをdev dependency、lock、`[tool.mypy]`設定、CIから撤去し、`ty check`を唯一のtype-check gateにした。
- `ty`は引き続き`src`、`tests`、`tools`を対象とし、`missing-type-argument=error`と`possibly-unresolved-reference=warn`を維持する。
- `mypy-boto3-dynamodb`はDynamoDB SDKの型stubであり、mypy type checkerとは別物のため保持する。
- ty公式のmigration guideがANN/PYIを併用した注釈網羅性の強化を示すことを確認した。既存のRuff診断量を別途評価できるよう、今回のtype-checker一元化ではANN/PYIの新規有効化は行わない。

### 27.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | ty configuration | https://docs.astral.sh/ty/configuration/ | `pyproject.toml`の`[tool.ty]`をproject-local設定として使用 |
| 2026-07-19 | ty migration guide | https://docs.astral.sh/ty/coming-from-mypy-or-pyright/ | strict相当の2 ruleを維持し、ANN/PYI強化は独立評価とした |

### 27.3 Validation

| Check | Result |
|---|---|
| Lock and sync | PASS: 92 packagesをfrozen同期。mypy、mypy-extensions、ast-serialize、librt、pathspecをlockから除去 |
| Type check | PASS: `uv run --frozen ty check`、`src`・`tests`・`tools`、diagnostic 0 |
| Regression suite | PASS: DynamoDB Local helper経由で331 passed、coverage 92.52% |
| Static quality | PASS: Ruff format/check、import-linter、public surface 147 files、release tool pins |
| Docs/public/workflow | PASS: 14 canonical notes validation、Obsidian mirror byte一致 |

## 28. STEP-09A CDK foundation・Stateful stack

| Field | Value |
|---|---|
| Step ID | `STEP-09A-CDK-STATEFUL` |
| Status | 実装・local検証合格、PR検証待ち |
| Date | 2026-07-19 |
| Test IDs | `CT-CDK-STATEFUL-001`、`QT-Workflow`、`QT-Docs`、`QT-Patch` |
| External impact | repositoryと公開文書のみ。CDK bootstrap、AWS resource、Discord、OpenAI、GitHub設定を変更しない |

### 28.1 Implementation

- rootにexact pinのnpm/TypeScript CDK projectを追加し、当初はNode.js 22.22.2、CDK CLI 2.1132.0、`aws-cdk-lib` 2.261.0、constructs 10.7.0、cdk-nag 3.0.1、TypeScript 7.0.2、tsx 4.23.1、Vitest 4.1.10を`package-lock.json`で固定した。Node.js runtimeは後続の`MAINT-NODE-24-LTS`でNode.js 24.18.0 Active LTSへ更新し、本記録の22.22.2を置換した。
- CDK推奨feature flagを`cdk.json`へ全て明示した。tsx CLIのIPCを使わず`node --import tsx`でappを起動し、制限sandboxとGitHub runnerの双方で同じentrypointを使う。
- `ShittimChest-Prod-Stateful`へon-demand DynamoDB tableとECR repositoryを実装した。tableはPK/SK、ALL projectionのgsi1/gsi2、AWS-managed encryption、deletion protection、PITR 35日、RETAIN。repositoryはECR既定暗号化、scan-on-push、除外なしの完全immutable、RETAINで、untaggedと一意な`candidate-*`だけを14日でexpireする。
- AWS Signer profile `shittim_chest_ecr`を`Notation-OCI-SHA384-ECDSA`、署名有効期間135か月、RETAINで追加した。`AWS::ECR::SigningConfiguration`はrepository filter `shittim-chest`だけを対象とし、Managed Signingをpush時に有効化する。registry-level resourceは今後のruleも同じconstructへ集約する。
- cdk-nag 3のCDK `Validations` pluginをappへ登録し、旧Aspect APIを使用しない。Vitestはtermination protection、CloudFormation retention、table/index、ECR lifecycle/default encryption/完全immutability、Signer profile/rule、全tag、unsuppressed AwsSolutions finding 0を検査する。
- CIへread-only `cdk` jobを追加し、full-SHA固定のsetup-node、`npm ci`、npm audit、typecheck、Vitest、strict synthを実行する。Dependabotへnpm ecosystemを追加した。

### 28.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | AWS CDK prerequisites / Node support | https://docs.aws.amazon.com/cdk/v2/guide/prerequisites.html、https://docs.aws.amazon.com/cdk/v2/guide/node-versions.html | 当初Node.js 22.22.2とlocal exact CLIを採用。runtimeは後続の`MAINT-NODE-24-LTS`でNode.js 24.18.0 Active LTSへ更新 |
| 2026-07-19 | DynamoDB CDK Table | https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_dynamodb.Table.html | PITR specification、deletion protection、RETAINをassert |
| 2026-07-19 | ECR CDK Repository | https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_ecr-readme.html | immutable、scan、lifecycle、RETAINをassert |
| 2026-07-19 | cdk-nag 3 | https://github.com/cdklabs/cdk-nag#usage | Validation Plugin APIを採用 |
| 2026-07-19 | setup-node v7.0.0 | https://github.com/actions/setup-node/releases/tag/v7.0.0 | full SHA `820762786026740c76f36085b0efc47a31fe5020`で固定 |
| 2026-07-19 | ECR Managed Signing / CloudFormation | https://docs.aws.amazon.com/AmazonECR/latest/userguide/managed-signing.html、https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-ecr-signingconfiguration.html | Signer profileとrepository限定registry ruleをStateful stackへ追加 |
| 2026-07-19 | ECR OCI v1.1 Referrers | https://docs.aws.amazon.com/AmazonECR/latest/userguide/images.html、https://docs.aws.amazon.com/AmazonECR/latest/APIReference/API_ListImageReferrers.html | signature、SPDX、provenance、vulnerability assessmentのdigest関連付けを後続release要件に確定 |
| 2026-07-19 | AWS Signer verification | https://docs.aws.amazon.com/signer/latest/developerguide/image-verification.html | Notation strict verificationを自動release gateに確定 |

### 28.4 Scope remaining

- PR/main CI、CodeQL JavaScript/TypeScript、GitHub managed npm dependency inventory、Rulesetの`cdk` required化はPR工程で確認する。
- 実accountは未bootstrapであり、Stateful stackをdeployしていない。bootstrapと初回deployはSTEP-10のrelease順序・権限境界を満たす明示工程で行う。
- VPC、IAM、digest-only ECS task definition/SSMはSTEP-09B、monitoring/Budget/Cost Anomaly Detection/ECS pre-scale admission hookはSTEP-09Cとする。実image push、Managed Signing/Notation実検証、SPDX/provenance/vulnerability OCI referrer attachはSTEP-10とする。

## 29. Node.js Active LTS update

| Field | Value |
|---|---|
| Step ID | `MAINT-NODE-24-LTS` |
| Status | local and CI validation passed; host RakuOS Node 24 alignment completed |
| Date | 2026-07-19 |
| External impact | repository runtime/toolchain only; AWS, Discord, OpenAI resources unchanged |

### 29.1 Decision

- Node.js 22 is no longer the latest Active LTS. Update the CDK project engine and CI runtime to Node.js 24.18.0, the current latest Active LTS.
- Node.js 26 is Current and is scheduled to enter LTS in October 2026, so do not merge a Node 26 type-definition update before the runtime itself reaches LTS.
- Pin `@types/node` to the latest compatible Node 24 release and keep npm lockfile reproducibility.

### 29.2 Latest official reference

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | Node.js releases | https://nodejs.org/en/about/previous-releases | Node.js 24.18.0 Active LTS; Node 26 Current and not adopted |

### 29.3 Local validation

| Check | Result |
|---|---|
| Node runtime | PASS: official Node.js 24.18.0 Linux x64 archive checksum verified; `node --version` = `v24.18.0`, `npm --version` = `11.16.0` |
| npm ci | PASS: lockfileから80 packagesをclean install |
| TypeScript / infrastructure tests / strict synth | PASS: `npm run check:infra`; TypeScript strict diagnostics 0, Vitest 6 tests passed, `cdk synth --strict` passed |
| Dependency audit | PASS: `npm run audit:infra`; known vulnerability 0 |
| Documentation mirror | PASS: `tools/sync_docs.py --check` byte-for-byte match |
| Host RakuOS Node | PASS: `nodejs24` 24.13.1 and `nodejs24-npm` 11.8.0 installed through RakuOS. User-scoped `~/.local/bin/node` and `npm` links point to `/usr/bin/node-24` and `/usr/bin/npm-24`; `node -v` = `v24.13.1`, `npm -v` = `11.8.0`. This preserves the immutable system Node 22 while selecting Node 24 for user development commands. |
| Python regression | PASS: DynamoDB Local helper経由で332 passed、coverage 92.52% |
| Docs/public surface | PASS: 14 canonical notesのmirror byte一致、公開面156 files、secret/実ID/絶対home path検出0 |

## 30. STEP-09B Runtime infrastructure

| Field | Value |
|---|---|
| Step ID | `STEP-09B-CDK-RUNTIME` |
| Status | 実装・local検証合格、PR検証待ち |
| Date | 2026-07-19 |
| Test IDs | `CT-CDK-RUNTIME-001`、`QT-Docs`、`QT-Patch` |
| External impact | repositoryと公開文書のみ。CDK bootstrap、AWS resource、SSM実値、Discord、OpenAIを変更しない |

### 30.1 Implementation

- `ShittimChest-Prod-Runtime`を`StatefulStack`のDynamoDB/ECRに一方向依存させた。`10.42.0.0/24`の2 AZ Public-only VPC、NAT/ALB/VPC endpointなし、no-ingress・IPv4 TCP 443 egressだけのSGを生成する。VPC Flow LogsはMVPの費用対効果判断で見送り、cdk-nagに限定的な理由を記録した。
- ECS serviceはARM64、512 CPU/1,024 MiB、`FARGATE_SPOT` 100%、desired 1、Public IPv4、minimum 0/maximum 100、deployment rollback、AZ rebalancing無効、Container Insights無効、ECS Exec無効とした。
- 平常taskはread-only root、UID/GID `10001`、capability全削除、init process、`stopTimeout=120`、event-loop heartbeat health、`/tmp/shittim-chest`だけ書込可とした。Docker imageにも同directoryをmode `0700`で事前作成した。
- break-glass task definitionは専用task roleとExec log groupを持つがserviceに接続しない。root filesystem書込みと4つの`ssmmessages` actionはbreak-glass側だけに隔離し、平常task roleには含めない。切替workflowはSTEP-10/11とする。
- imageはdefaultを持たない必須CloudFormation parameterとし、64-hexの`sha256:`digestからECR digest URIだけを生成する。runtime/personaは`vNNNN`のconfig versionと非公開SSM pathを参照し、CloudFormationはSecureString値を作成・取得しない。
- execution roleはECR pull、application logs、各parameterの`ssm:GetParameters`だけ。平常task roleは実codeに対応するDynamoDB `ConditionCheckItem/GetItem/PutItem/UpdateItem/Query`に限定し、task trustは`SourceAccount`とTokyo region/accountのECS `SourceArn`を必須にした。
- applicationとbreak-glass Execの各log groupは90日、`RETAIN`、data protection policy付きとした。
- local AWS credentialを検出した`cdk synth`が実account ID付きavailability-zone contextを書込むことを最終検証で検出した。`cdk.context.json`を空へ戻し、CDK appをTokyo region固定・account非依存synthに変更した。public surface scannerにも実12桁AWS account ID検出を追加し、`000000000000`のtest dummyだけを例外とした。

### 30.2 Latest official references

| Checked | Target | Official source | Applied result |
|---|---|---|---|
| 2026-07-19 | ECS Fargate capacity provider | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/fargate-capacity-providers.html | Spot専用とinterruption前提を維持 |
| 2026-07-19 | CDK FargateService 2.261.0 | https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_ecs.FargateService.html | Public IP、capacity provider、minimum/maximum、AZ rebalancing無効を実装 |
| 2026-07-19 | ECS task IAM role | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html | `SourceAccount`/`SourceArn`でconfused deputyを防止 |
| 2026-07-19 | ECS Parameter Store injection | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/secrets-envvar-ssm-paramstore.html | execution roleの`ssm:GetParameters`とnew deployment要件を反映 |
| 2026-07-19 | CloudWatch Logs data protection | https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/mask-sensitive-log-data.html | 90日log groupへmask policyを追加 |
| 2026-07-19 | DynamoDB Service Authorization Reference | https://servicereference.us-east-1.amazonaws.com/v1/dynamodb/dynamodb.json | boto3の実使用operationとtable/indexに対する最小actionを照合 |

### 30.3 Local validation

| Check | Result |
|---|---|
| TypeScript | PASS: `npm run build`、strict diagnostics 0 |
| Runtime assertions | PASS: `npm run test:infra`、2 files / 15 tests |
| CDK synth | PASS: `npm run synth`、`Stateful`と`Runtime`をcredentialなしでstrict synth |
| cdk-nag | PASS: Runtimeのunsuppressed AwsSolutions finding 0。Container Insights/VPC Flow Logs/ECS static non-secret environment/break-glass wildcardだけに理由を限定 |
| Template inspection | PASS: NAT/ALB/VPC endpoint/ingress 0、Spot専用、Public IP、Exec false、normal/break-glass IAM分離、digest/config regexを確認 |
| Python regression | PASS: DynamoDB Local helper経由で331 passed、coverage 92.52% |
| Locked dependency audit | PASS: `npm audit --audit-level=low`、known vulnerability 0 |
| Static/docs/public | PASS: Ruff、ty、import-linter、public surfaceのAWS account ID positive/dummy-negative unit test、14 canonical notesのmirror byte一致、`git diff --check` |

### 30.4 Remaining scope

- AWS accountは未bootstrap、Stateful/Runtimeは未deploy、SSM parameter実値は未登録である。image digest parameterにdefaultはなく、release workflowは検証済みの実digestを必ず渡す。
- ECS標準CPU/Memory、EventBridge task stop/Spot通知、application metric/alarm、Budget、Cost Anomaly Detection、image admission hookはSTEP-09Cで実装する。
- break-glass切替、承認、Exec有効化、調査後の平常revision復帰はSTEP-10/11のworkflow/runbookで実装・実機試験する。

### 30.5 Pull Request validation

| Evidence | Result |
|---|---|
| Pull Request | [#53 Implement STEP-09B runtime infrastructure](https://github.com/pitekusu/shittim-chest/pull/53)、head `6253e57`、draft・mergeable |
| CI | PASS: run `29685453221`。`quality`、`tests`、`security`、`package`、`cdk`、`container-arm64`、`docs-public-safety`が全て合格 |
| CodeQL | PASS: run `29685452669`。Actions、JavaScript/TypeScript、Pythonの全matrix合格 |
| Managed security | PASS: GitGuardian Security Checks |
| AWS impact | PASS: CIはcredential/OIDCを使用せず、bootstrap、change set、deploy、resource作成0件 |

## 31. Future slice template

各後続stepは次を複製して追記する。

1. Metadata: step ID、status、date、branch/base、requirements、test IDs。
2. Scope/non-scopeと外部state変更。
3. Environmentと公式資料確認日/version。
4. command別のexpected/actual/result、coverage、security finding。
5. artifact/digest/SBOM/attestation/check runへの非秘密参照。
6. failure、再実行、known issue、risk acceptance、owner。
7. PR/CI/deploy結果とrollback要否。
