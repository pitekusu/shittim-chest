---
aliases:
  - The Shittim Chest 実装検証記録
tags: [project, shittim-chest, implementation, test-evidence]
status: active
created: 2026-07-16
updated: 2026-07-30
---

# 実装・試験・検証記録

## 1. 目的・記録規約

本書は各implementation sliceで実施した試験と検証の実測証跡を累積する。設計上の合否条件は[[18_試験・品質保証設計]]、実装順序と要求対応は[[19_実装計画・トレーサビリティ]]を正とする。

- sliceごとにID、scope、検証結果、coverage、security finding、未実施範囲、既知問題、PR/CI結果を記録する。
- 失敗、再実行、条件付きskipを削除せず、後続結果から参照できる形で残す。
- secret、本番runtime値、user content、Discord/OpenAI本文、credential形式、ローカル絶対home pathを記録しない。
- local合格とGitHub CI、production integration、deploy合格を区別し、未実施を成功扱いしない。
- 本書はpublic-safeなObsidian正本からrepository `docs/`へmirrorする。

2026-07-20に本書を圧縮した。コマンド逐次実行ログ、check run IDの羅列、個別test case名の列挙、長い差分説明は集約し、未解決事項・残課題・risk acceptanceと外部impactは省略していない。詳細な逐次証跡は各Pull RequestとGitHub Actions runを外部証跡とする。各sliceの最終PR/merge commitはAGENTS.md進捗表も正とする。

external impactの共通注記: 個別記載がない限り、各sliceは実Discord token/Application/Guild、production AWS resource/credential、OpenAI有料API、GitHub本番設定を使用・作成・変更していない。

## 2. STEP-01 Python domain foundation

- `STEP-01-PYTHON-DOMAIN-FOUNDATION`、PR `#1`、merge `7fa642e`、2026-07-16。RQ-002/004/011/015/017。
- Scope: Python 3.14.6/uv 0.11.29基盤、UUIDv7 `DebateId`/`AttemptId`、10 phase・21許可edge・Spot checkpoint/recoveryのimmutable state machine、`retry_of`/`failed_from_phase`を保持するretry、unit/property testと公開surface・mirror検証。
- 検証: PASS。66 tests、domain line/branch 100%、Ruff、mypy strict、pip-audit known vulnerability 0、CycloneDX 1.5 export 42 components、sdist/wheel build、public surface 41 files finding 0、14文書mirror。
- 失敗・やり直し: property testの自己参照failure、public surface誤検知、`tools/`のmypy module名重複など9件。全て修正・再実行でResolved。
- 残課題: 当時はproject CI未実装でGitHub Actions evidenceなし（STEP-02で追加）。GitHub App write権限不足（403）を確認し、以降のwrite操作は認証済み`gh` CLIを既定経路とする運用判断の起点。

## 3. STEP-02A Dependabot version updates

- `STEP-02A-DEPENDABOT-VERSION-UPDATES`、PR `#3` merge `794ebf6`、修正PR `#5` merge `b11b291`、検証PR `#6` merge `65e8653`、Ruff更新PR `#7` merge `453c4fe`、2026-07-17。
- Scope: `.github/dependabot.yml`でuv ecosystemのweekly version updateを有効化。minor/patchをgroup化、majorは個別review、同時open 5件。
- 検証: PASS。YAML semantic assertion、既存全gate、GitHub activation、managed security coexistingを確認。
- 失敗・やり直し: 初回Dependabot jobは`[tool.uv].required-version ==0.11.29`に対しupdater同梱uvが0.11.8のみで`tool_version_not_supported`失敗。`required-version = ">=0.11.8,<0.12"`へ修正し、修正後mainの新規job successとRuff 0.15.22更新PR生成を確認（failure log: https://github.com/pitekusu/shittim-chest/network/updates/1463693967 ）。
- 残課題: `required-version`の下限を変更する場合は、Dependabot updater image同梱uv versionを都度再確認する。

## 4. STEP-02 GitHub CI・managed SBOM

- `STEP-02-GITHUB-CI-MANAGED-SBOM`、PR `#10`、merge `e2fdaad`、2026-07-17。RQ-009/011/015。
- Scope: `ci.yml`に`quality`/`tests`/`security`/`package`/`docs-public-safety`を実装。Actionをfull SHA pin、Gitleaks/actionlintをrelease archive SHA-256で固定、CycloneDX 1.5 strict schema検証、GitHub managed SPDXとmain lockの週次照合、`tools/check_docs.py`、isolated wheel install検証。
- 検証: PASS。92 tests、domain 100%、Gitleaks full history leak 0、SBOM 60 components、CI run `29545502266`、managed SBOM比較`29545563028`。
- Ruleset: required check 5件をGitHub Actions App ID `15368`へstrict固定、CodeQL `high_or_higher`をmerge blocker化、effective-rules APIで`current_user_can_bypass=never`を確認。
- 残課題: direct push、偽装同名check、fork PR等のnegative testは未実施。fork受入れ開始時またはsecurity hardening sliceで安全な専用PRにより追加検証する。

## 5. STEP-02B Betterleaks段階移行

- `STEP-02B-BETTERLEAKS-MIGRATION`、PR `#12`、merge `942758f`、2026-07-17。
- Scope: Betterleaks 1.6.1とGitleaks 8.30.1の並行full-history scan、cosign 3.0.6によるSigstore provenance検証、実行時生成のpositive/negative fixture contract、tool pin集中管理（`.github/tool-versions.json`）と週次latest検知。`--redact`使用、provider validation無効（検出候補を外部APIへ送らない）。
- 検証: PASS。100 tests、domain 100%、両scanner finding 0、provenance `Verified OK`、CI run `29547746351`。
- 失敗・やり直し: actionlint `-version`の複数行出力でpin比較不一致（先頭行比較へ修正）。CodeQLがgenerated contractの一時worktree書込みをclear-text storageとして検出（抑制せずstdinからGit objectを構成する方式へ変更）。両方とも実credential漏えいではなく、修正後全gate再合格。
- 残課題: Gitleaks撤去は観測期間後のSTEP-02Cで判断。

## 6. STEP-02C Gitleaks撤去

- `STEP-02C-RETIRE-GITLEAKS`、PR `#13`、merge `d0f601a`、2026-07-17。
- Scope: Gitleaks pin/download/実行/監視を撤去。Betterleaks署名・digest検証、full-history、generated contract、GitHub managed Secret scanning/Push protection、`security` check名は維持。
- 検証: PASS。100 tests、domain 100%、Betterleaks単独full history finding 0、CI run `29548428650`。
- 判断記録（risk acceptance）: 異種scannerの検出差分というdefense-in-depthは失う。両者は同一maintainer系譜で対象履歴・fixtureに差分なし、Gitleaksはfeature complete。具体的なcoverage gapが再現した場合は第二scannerを別ADRで再導入する。

## 7. STEP-03 Application core

- `STEP-03-APPLICATION-CORE`、PR `#15`、merge `34ccc54`、2026-07-17。RQ-002/004/011/017。
- Scope: 投票domain、immutable application model、Protocol、`accept_debate`/`run_debate`/`cancel_debate`/`retry_debate`/`resume_recoverable`、phase/session deadline、TaskGroup並列化、`EvidenceService`分離、`CandidateOrderer`注入、import-linter layer contract導入。
- 検証: PASS。142 tests、全対象line/branch 95.30%（application service 91%、domain voting 98%）、import-linter 1 contract kept。
- 失敗・やり直し: artifact付きsnapshotの型不整合、nested timeoutとsession timeoutの`TimeoutError`混同（markerで`phase_deadline_exceeded`と`session_deadline_exceeded`を分離）、mypyのtests二重検出。全てResolved。
- 残課題: fencing token、transaction、3 lease slot、outbox整合性、schema migrationはSTEP-04で確定。SDK response/Discord payload/OpenAI schemaのcontractはadapter stepまで推測で確定しない。

## 8. STEP-04A Persistence contract・serialization

- `STEP-04A-PERSISTENCE-CONTRACT`、PR `#16`、merge `54948d7`、2026-07-17。RQ-003/004/005/006/011/017。
- Scope: Guild/channel・Discord operation IDを保持するsnapshot、3 slotを型で制限する`LeaseGrant`、persisted resultを返すrepository Protocol、boto3非依存serializer、PK/SK縦分割、thread/recoverable GSI属性、schema 1→2 up-convert、未知schema fail closed、outbox/panel双方向変換、22文字nonce、400KB事前拒否。
- 検証: PASS。155 tests、domain/application 93.57%、11 DynamoDB contract tests（AWS/network接続なし）。
- 残課題: boto3依存、transaction expression、DynamoDB Local、400KB実API受理はSTEP-04Bで検証。

## 9. STEP-04B DynamoDB persistence adapter

- `STEP-04B-DYNAMODB-ADAPTER`、PR `#18`、merge `9aafe6e`、2026-07-17。RQ-003/004/005/011/017。
- Scope: boto3 1.43.50を`asyncio.to_thread`隔離する`DynamoDbDebateRepository`/`DynamoDbOutboxRepository`。受付・phase更新・terminal解放・FAILED retry・lease renew・outbox claim/rescheduleを全てtransaction化し、fencing tokenとMETA `ConditionCheck`でfenceする。operation IDの正本は`OPERATION#<id>/RESULT`へ分離。CI tests jobはDynamoDB Local 3.3.0をdigest固定で起動。
- 検証: PASS。163 tests、92.40%。quota 30件、同時3件、4件目busy、terminal解放、旧worker拒否、renew済みexpiry保持、outbox chunk順/reclaim/sent冪等性をDynamoDB Local＋SDK Stubberで確認。main CI run `29553563948`。
- 失敗・やり直し: 再送をoperation IDだけで読めない配置問題（専用keyへ変更）、Localの`ClientRequestToken`保持による`IdempotentParameterMismatchException`（token hashへtable/aggregate/slot/fencingを含有）、Attempt META丸ごとPutによるlease expiry巻き戻し余地（条件付きUpdateへ変更）、runtime環境でのstub import error（`TYPE_CHECKING`境界へ移動）。全てResolved。
- 残課題: DynamoDB Localは本番のtransaction conflict/throttling/IAMを再現しない。cancel mappingはSDK Stubberで補完し、IAM/throttling/実service smokeはCDK/deploy stepまで未実施。

## 10. STEP-05A OpenAI Structured Outputs adapter

- `STEP-05A-OPENAI-STRUCTURED-ADAPTER`、PR `#20`、merge `d6ea561`、2026-07-17。RQ-002/011/012/016。
- Scope: stable `AsyncOpenAI.responses.parse()`による初回意見/最終案/投票/決定adapter。Pydantic strict schema、最大6並列、provider errorの安定domain code変換、本文を含まないusage/failure recorder契約、`store=false`。Responses API Multi-agent betaは非採用。
- 検証: PASS。178 tests、92.40%、15 contract tests（refusal/incomplete/invalid、429/auth/model/5xx/transport、cancel再送出）。main CI run `29555748245`。
- 残課題: 旧OpenAI詳細設計の`assumptions`/`risks`等の追加fieldはdomain・serializer・migration・Discord表示を含む別sliceの未決事項。Terra昇格はP1未決事項（後のSTEP-05C.1Bで不採用決定）。実API・Web search・CloudWatch出力は未使用。

## 11. STEP-05B Evidence Router・Web search

- `STEP-05B-WEB-EVIDENCE`、PR `#21`、merge `44a35fa`、2026-07-17。RQ-013/016。
- Scope: `question-router-v2`（追加model callなし、未知表現はfail-safeに`optional`）、hosted `web_search`を`tool_choice=required`・`max_tool_calls=4`・`include=["web_search_call.action.sources"]`・`store=false`で1 request、全participant共通のimmutable Evidence、DynamoDB schema v3。optional失敗は`optional_unavailable`で続行、required失敗は`required_evidence_unavailable`でFAILED。
- 検証: PASS。187 passed、5 opt-in skipped、91.50%。classification、request shape、provider unavailable分岐、Evidence round-tripとschema v2→v3を確認。main CI run `29558517220`。
- 残課題: OpenAI実APIによるopt-in integration、citationのDiscord clickable表示、CloudWatch recorderは後続sliceで検証。

## 12. STEP-05C 品質昇格Policy・Shadow評価基盤

- `STEP-05C-ESCALATION-SHADOW`、PR `#22`、merge `a6f43cb`、2026-07-17。
- Scope: 投票後に`escalation-shadow-v1`（`split_vote`/`winning_axis_low`/`winning_average_low`）を追加OpenAI requestなしで計算・保存（`executed=false`）。`luna_standard`/`terra_standard`/`luna_pro`のSDK非依存Policy、DynamoDB schema v4、`tools/evaluate_escalation.py`（`--live`と`OPENAI_API_KEY`の両方を要求、出力はrepository外）。
- 検証: PASS。196 passed、5 skipped、91.81%。通常討論のrequest数が10 callsのまま、Policy contract、assessment round-trip、schema v3→v4を確認。
- 残課題: blind A/B採点後の採用判断（→STEP-05C.1Bで決定）。production auto-escalation、Discord表示、CloudWatch metricは未実装。refusal/policy block/schema failure/429/timeoutを別Policyで回避しない。

## 13. STEP-05C.1A Blind評価workflow強化

- `STEP-05C.1A-EVALUATION-WORKFLOW`、PR `#24`、2026-07-17。
- Scope: `evaluate_escalation.py`のv2 artifact（採点者用回答とPolicy key・性能指標を分離出力、major/operational failure分類）、`score_escalation.py`（evaluation ID・fixture SHA-256・A/B mapping検証、content-free集計）。
- 検証: PASS。207 passed、5 skipped、91.81%。CLI fail-closed（`--live`なしでexit 1・artifact未作成）を確認。有料評価は未実行。
- 残課題: 実API評価前に利用可能modelと料金を再確認しoperatorの明示承認を得る（→STEP-05C.1Bで実施）。

## 14. STEP-05C.1B 実API・preference-only評価

- `STEP-05C.1B-LIVE-PREFERENCE-EVALUATION`、PR `#26`、2026-07-17。External impact: 明示承認済みの有料OpenAI Responses API評価を実行（AWS/Discord/GitHub設定は変更なし）。
- Scope: `gpt-5.6-terra` standardと`gpt-5.6-luna` `reasoning.mode=pro`を10件ずつ実行し、単独operatorが継続可能なA/B/tie preference-only採点へ変更（`review_escalation.py`、`score_escalation.py --preference-only`）。
- Live結果: 両Policy 10/10成功。blind preferenceはTerra 2勝・Luna pro 4勝・tie 4。合計input 373,874 / output 92,386 token、推定`$1.198697`。
- 失敗・やり直し: 初回実行はBilling未設定の`insufficient_quota`で20件全て生成前失敗（token・費用0）。Billing設定後の再実行で全件成功。
- Production決定（2026-07-17、operator）: 構成の単純性・費用予測・運用復旧経路を優先し、本番はLuna standard固定。Terra standard/Luna proは評価再現用に限定し、昇格threshold、追加token/deadline、昇格用Discord表示は実装しない。shadow assessmentは`executed=false`を維持。
- 検証: PASS。214 passed、5 skipped、91.81%。raw回答・Policy key・API keyはGit・Obsidianへ保存せず、repository外mode `600` artifactのみ。

## 15. STEP-06A Discord contract foundation

- `STEP-06A-DISCORD-CONTRACT-FOUNDATION`、PR `#27`、merge `47af41f`、2026-07-17。RQ-001/003/004/005/006/011。
- Scope: SDK非依存の4 identity slot、runtime config、stable Discord error code、message splitter、UUIDv7 nonce、panel custom ID codec、prepared outbox factoryをapplication層へ追加。`bind_discord_context`は`ACCEPTED`中だけ3 IDを一括保存。DynamoDB schema v5。outbox/panel型をapplication層へ移し依存方向を修正。
- 検証: PASS。221 passed、5 skipped、92.70%。main CI run `29570175552`。
- 残課題: discord.py publisher（STEP-06B）、interaction runtime（STEP-06C）、SIGTERM/checkpoint/resume（STEP-07）は当時未実装で、いずれも後続sliceで実装・検証済み。

## 16. STEP-06B Discord publisher

- `STEP-06B-DISCORD-PUBLISHER`、PR `#30`、merge `96a1ace`、2026-07-17。RQ-004/005/011。
- Scope: discord.py 2.7.1 runtime依存、`DiscordPyPublisher`。nonce指定で`enforce_nonce=true`となるSDK shapeをversion contract化、`allowed_mentions=AllowedMentions.none()`、2回目以降は履歴照合（最大500件、同一nonce内容不一致なら送信しない）、429はSDK/Retry-Afterへ委譲、`max_ratelimit_timeout=30`必須、Discord操作45秒timeout（60秒claimより前にreschedule）。
- 検証: PASS。245 tests、92.74%。Discord成功後のDynamoDB完了失敗注入で履歴採用時にSDK sendが1回のままであることを確認。
- 残課題: CloudWatch metric/alarm、実Bot permission、実Discord rate limit smokeはproduction stepで確認。

## 17. STEP-06C Discord interaction runtime

- `STEP-06C-DISCORD-INTERACTIONS`、PR `#31`、merge `9799cb9`、2026-07-17。RQ-001/006/011/017。
- Scope: GUILDS-only Intentの4 client builder、4 READY gateway、supervisor、Guild Command `/shittim question:<1〜1000文字>`、先行ephemeral defer、starter/Public Thread/Cancel・Retry panel、component照合、schema hash差分時だけの明示sync。Python 3.14で`Client.event()`がdeprecated APIを経由するため、moderator専用Client subclassの`on_interaction`へ明示handler接続。
- 検証: PASS。PR CIで266 passed、92.55%、pip-audit 0。main merge後CodeQL合格。
- 残課題: 実Discord Guildでのcommand sync、permission、thread、component、disconnect smoke testはDiscord Application構成後に実施。

## 18. STEP-07A Runtime lifecycle

- `STEP-07A-RUNTIME-LIFECYCLE`、PR `#33`、merge `0f386f5`、2026-07-17。D-002/005/006、RQ-011/012/017。
- Scope: fail-closed `RuntimeAdmissionGateway`、SDK非依存Protocolで所有する`RuntimeLifecycle`、SIGINT/SIGTERMの`UnixSignalHandlers`（90秒で`RuntimeShutdownTimeout`、ECS `stopTimeout=120`との差30秒をruntime終了余裕とする）、1 Bot切断で受付閉鎖・60秒連続切断でCHECKPOINTED保存、4 READY復帰でrecoverable scan再起動。
- 検証: PASS。273 passed、5 skipped、92.55%。
- 残課題: outbox drain（STEP-07B）、production bootstrap（STEP-07C）、container fault injection（STEP-08）、CloudWatch heartbeat/metric、EventBridge Spot event、ECS health/task definition（STEP-08/09）は当時未実装で後続sliceへ引き継ぎ。

## 19. STEP-07B Outbox recovery

- `STEP-07B-OUTBOX-RECOVERY`、PR `#34`、merge `04bbda0`、2026-07-17。RQ-004/005/011/012。
- Scope: `DiscordOutboxRecovery`。lease取得済みattemptの未`SENT` operationをstrongly consistent・paginatedなbase-table Queryで全件取得し、永続利用可能時刻まで待機してから保存順でpublish。retryable errorは永続scheduleへ委譲しapplication側で同一requestを直接retryしない。`RepositoryConflict`はfencing喪失としてterminal化せず終了。outbox drainをphase処理より先・300秒active-processing deadline外へ配置。本文なしmetric`discord_outbox_recovered`/`discord_outbox_retry_scheduled`。
- 検証: PASS。287 passed、92.52%。DynamoDB Local固定digestでfuture/claimedを含む`list_pending`を確認。
- 残課題: 実GuildでのRead Message History、権限剥奪、locked/archive thread、長時間rate limit smokeはDiscord Application構成後。CloudWatch metric emitter、outbox age alarm、dashboardはSTEP-09/11。

## 20. STEP-07C Production composition・process fault injection

- `STEP-07C-PRODUCTION-COMPOSITION`、PR `#35`、merge `e863ae3`、2026-07-17。RQ-004/008/011/012/016。
- Scope: strict/frozen/extra-forbid設定model（`startup_configuration_invalid`でfail closed）、唯一のcomposition root `bootstrap.py`（client/semaphoreをprocess単位で再利用、本番PolicyはLuna standard固定）、`python -m shittim_chest`、content-free telemetry（root logger WARNING以上固定）、実Unix subprocessによるSIGTERM/SIGKILL fault fixture。
- 検証: PASS。DynamoDB Local 5件を含む304 passed、92.52%。SIGTERMで当時のRuntime受付gate停止・checkpoint・cleanup、SIGKILLでactive永続state残存とreplacement recoveryを実processで確認。main CI run `29583519941`。Scale-to-Zero後はこのgateをIngress claim/drainとし、HTTP永続受付は独立して継続する。
- 残課題: container境界のSIGTERM/SIGKILL、Discord POST・DynamoDB transaction直前/直後SIGKILL、CloudWatch EMF/alarm、ECS Spot eventはSTEP-08/09/11で検証。

## 21. STEP-08A Production container foundation

- `STEP-08A-PRODUCTION-CONTAINER-FOUNDATION`、PR `#37`、merge `7742f0b`、2026-07-17。RQ-004/007/008/011/012/015。
- Scope: digest固定multi-stage `Dockerfile`（Python `3.14.6-slim-trixie`、uv 0.11.29）、UID/GID `10001`・exec entrypoint・`SIGTERM`のproduction target、shell utility付き`break-glass` target、event-loop heartbeat health（Discord/OpenAI可用性は含めない）、allowlist式`.dockerignore`、DependabotへDocker ecosystem追加。
- 検証: PASS。Podman Docker-formatで両target build、read-only root/capability 0/no-new-privileges/`/tmp`のみ書込可、productionにuv・raw source・test・cacheなし、312 passed、92.52%。amd64のみでnative ARM64はSTEP-08Bへ保留。
- 失敗・やり直し: Podman既定OCI formatが`HEALTHCHECK`を保持しないため`--format docker`へ統一。
- 残課題: STEP-08Bでnative ARM64・container fault・image SPDX、STEP-09でFargate task definition接続。image push/ECR/AWS resource/追加課金なし。

## 22. STEP-08B Native ARM64・container fault・image SBOM

- `STEP-08B-ARM64-CONTAINER-GATE`、PR `#39`、merge `2cca51a`、2026-07-17。External impact: GitHub ActionsとRulesetのみ変更（`container-arm64`をRuleset ID `19047946`のrequired checkへ追加）。
- Scope: native `ubuntu-24.04-arm` job（QEMU/self-hosted不使用）、CI専用`fault-test` target、全7非終端phaseのPID 1 SIGTERM（120秒未満exit 0・同phase checkpoint）、transactionとDiscord POST直前/直後SIGKILL→replacement後のtransaction event 1件・content hash一致message 1件をdurable file fixtureで検証、Syft v1.48.0によるSPDX 2.3 image SBOM（30日artifact）。
- 検証: PASS。324 passed、92.52%、skip 0。native ARM64 run 53秒（QEMUなし）、SPDXはDebian 87・production Python 35 package・dev混入0。
- 失敗・やり直し: Docker EngineとPodmanのhealth metadata位置差、rootless Podman/SELinux bind mountの権限問題、初回ARM64 runでrunnerのPython 3.12から`uuid.uuid7()`をimportして停止（gate toolを標準ライブラリのみで起動可能にし、固定phase一覧とdomain状態機械の一致をunit test化）。全て修正後PASS。
- 残課題: STEP-09でtask definition接続、STEP-10でpush済みdigestからのrelease SBOM再生成とprovenance/SBOM attestation。

## 23. QUALITY-TY-PARALLEL（ty並列type-check gate）

- `QUALITY-TY-PARALLEL`、PR `#41`、merge `94f258c`、2026-07-19。External impact: 既存`quality` jobのみ変更。
- Scope: ty 0.0.61をdev dependencyへ追加し、`src`/`tests`/`tools`を対象に`missing-type-argument=error`、`possibly-unresolved-reference=warn`でmypyと並列運用。初回67 diagnosticsは規則カテゴリを無効化せず解消。production imageの禁止dev package一覧へtyを追加。
- 検証: PASS。ty diagnostic 0、mypy error 0、CI run `29679124835`全required check合格。
- 残課題: tyがstableになるか後続数stepでmypy独自検出差分がないことを確認するまでmypyを維持（→27章で一元化済み）。

## 24. CONTAINER-UV-LAYER-CACHE（uv Docker dependency-layer最適化）

- `CONTAINER-UV-LAYER-CACHE`、PR `#42`、merge `3c43c49`、2026-07-19。
- Scope: manifest先行copy＋`uv sync --frozen --no-dev --no-install-project --no-editable`のdependency layer、2回目syncでproject追加、`UV_NO_CACHE=1`撤去とBuildKit cache mount（`sharing=locked`）、`UV_PYTHON_DOWNLOADS=0`でfail closed。
- 検証: PASS。cold buildで33 dependency package先行install、warm buildで全step cache hit、container gate完走。CI run `29679874886`。
- 残課題: GitHub Actions間の外部cache共有はこのsliceに含めず、直後のPR `#43`で導入。

## 25. CI-BUILDX-GHA-CACHE（Buildx・GitHub Actions cache統合）

- `CI-BUILDX-GHA-CACHE`、PR `#43`、merge `90f3012`、2026-07-19。
- Scope: full SHA固定のDocker公式`setup-buildx-action` v4.2.0/`build-push-action` v7.3.0へ置換、production/faultを`load: true`、productionのみ`type=gha,scope=container-arm64-production,mode=max`、cache exportは`ignore-error=true`（性能最適化のみでcorrectnessを失敗させない）、`.dockerbuild` recordを30日保持。
- 検証: PASS。PR/mainともsame-head rerunでGHA manifest importと主要layerの`CACHED`を確認し、native ARM64 jobが90秒→72秒（mainは89秒→71秒）へ短縮。cache hit後もcontainer gateとSPDX（Debian 87/Python 35）合格。
- 残課題: release image cache、ECR push、attestationはSTEP-10で別scope・別workflowとし、test image cacheと混在させない。

## 26. QUALITY-DYNAMODB-LOCAL-RUNNER

- `QUALITY-DYNAMODB-LOCAL-RUNNER`、2026-07-19（原記録時点はdraft review中。最終merge状態はAGENTS.md進捗表を参照）。
- Scope: `tools/run_dynamodb_local.py`。Podman優先・Docker fallback、CIと同じDynamoDB Local 3.3.0 digestを`-inMemory -sharedDb -disableTelemetry`で起動、PID由来一時containerをrandom loopback portへpublish、signed `ListTables`成功まで待機、`DYNAMODB_ENDPOINT_URL`はchild commandへだけ注入。
- 検証: PASS。helper経由で331 passed、92.52%、integration skip 0、試験後の残存container 0件。
- 残課題（運用規約として確定）: Codex制限sandboxではrootless Podmanの`/run/user/1000/libpod`がread-onlyでservice接続前に停止する。image/endpoint/test skipを変更せず、helperをhost実行へ切り替える。

## 27. QUALITY-TY-SINGLE-GATE（tyへの一元化）

- `QUALITY-TY-SINGLE-GATE`、2026-07-19（原記録時点はPR検証待ち。最終merge状態はAGENTS.md進捗表を参照）。
- Scope: `mypy`とそのtransitive dependencyをdev dependency・lock・設定・CIから撤去し、`ty check`を唯一のtype-check gateにした。`mypy-boto3-dynamodb`は型stubのため保持。ANN/PYI強化は別途評価とし今回は有効化しない。
- 検証: PASS。92 packages frozen同期、`ty check` diagnostic 0、DynamoDB Local helper経由で331 passed、92.52%。

## 28. STEP-09A CDK foundation・Stateful stack

- `STEP-09A-CDK-STATEFUL`、PR `#47`、merge `e2e9e3f`、2026-07-19。
- Scope: exact pinのnpm/TypeScript CDK project（`node --import tsx`起動、推奨feature flag明示）、`ShittimChest-Prod-Stateful`にon-demand DynamoDB table（PK/SK、gsi1/gsi2、deletion protection、PITR 35日、RETAIN）、ECR repository（完全immutable、RETAIN、14日lifecycle＝後の31章で直近5世代へ変更）、AWS Signer profile `shittim_chest_ecr`（`Notation-OCI-SHA384-ECDSA`、135か月、RETAIN）と`shittim-chest`限定Managed Signing rule、cdk-nag 3 `Validations` plugin、CIへread-only `cdk` job。
- 検証: PASS。TypeScript strict 0、Vitest assertions、credentialなしstrict synth、unsuppressed AwsSolutions finding 0。
- 残課題: 実accountは未bootstrap・未deploy。bootstrapと初回deployはSTEP-10のrelease順序・権限境界を満たす明示工程で行う。実image push、Managed Signing/Notation実検証、OCI referrer attachはSTEP-10。

## 29. MAINT-NODE-24-LTS

- `MAINT-NODE-24-LTS`、2026-07-19。External impact: repository runtime/toolchainのみ。
- Scope: CDK project engineとCI runtimeをNode.js 24.18.0 Active LTSへ更新（STEP-09A当初の22.22.2を置換）、`@types/node`はNode 24互換最新へpin。
- 検証: PASS。公式archive checksum検証、`npm ci` 80 packages、TypeScript strict 0、Vitest 6 tests、strict synth、npm audit 0、Python回帰332 passed、92.52%。host側もRakuOS `nodejs24`をuser-scoped linkで選択（immutable system Node 22を保持）。
- 残課題: Node.js 26はCurrentで2026-10にLTS入り予定。runtimeがLTSへ移行するまでNode 26型定義更新をmergeしない。

## 30. STEP-09B Runtime infrastructure

この節はPR `#53`時点の履歴証跡である。当時のSpot・desired 1は本書第48節のDraft PR `#85`でOn-Demand・desired 0/max 1へ置き換えたため、現行設計として読まない。

- `STEP-09B-CDK-RUNTIME`、PR `#53`、merge `3c5ccc9`、2026-07-19。
- Scope: `ShittimChest-Prod-Runtime`（Statefulへ一方向依存）。`10.42.0.0/24` 2 AZ Public-only VPC（NAT/ALB/VPC endpointなし、no-ingress・IPv4 TCP 443 egressのみ、Flow Logsは費用対効果で見送りcdk-nagに限定理由）、ARM64 512 CPU/1,024 MiB・`FARGATE_SPOT` 100%・desired 1・Public IPv4・deployment rollback・Exec無効、平常taskはread-only root・UID/GID `10001`・capability全削除・init・`stopTimeout=120`・heartbeat health、break-glass task definitionはservice非接続でIAM分離、imageは64-hex digestの必須parameterからdigest URI生成、execution/task role最小権限（`SourceAccount`/`SourceArn`必須）、log group 90日・RETAIN・data protection policy。
- 検証: PASS。TypeScript strict 0、Vitest 15 tests、credentialなしstrict synth、unsuppressed AwsSolutions finding 0、CI run `29685453221`全7 job合格（credential/OIDC不使用、resource作成0件）。
- 失敗・やり直し: 最終検証でlocal AWS credentialを検出した`cdk synth`が実account ID付きAZ contextを`cdk.context.json`へ書込むことを検出。contextを空へ戻しTokyo region固定・account非依存synthへ変更し、public surface scannerへ実12桁AWS account ID検出（`000000000000` dummyのみ例外）を追加。
- 残課題: AWS account未bootstrap、両stack未deploy、SSM parameter実値未登録（image digest parameterにdefaultなし、release workflowが検証済みdigestを必ず渡す）。ECS標準metric、EventBridge task stop通知、application metric/alarm、Budget、Cost Anomaly Detection、image admission hookはSTEP-09C。break-glass切替・承認・復帰はSTEP-10/11のworkflow/runbook。

## 31. MAINT-ECR-ENHANCED-SCAN（ECR拡張スキャン・直近5世代保持）

- `MAINT-ECR-ENHANCED-SCAN`、PR `#56`、2026-07-20。
- Scope: operator指示により`AWS::ECR::RegistryScanningConfiguration`を追加（`ScanType: ENHANCED`、`CONTINUOUS_SCAN`、repository filter `shittim-chest`のみ、基本scan-on-pushは無効化）。lifecycleを`sinceImagePushed` 14日から`imageCountMoreThan` 5へ変更し、untaggedと全tag付きimage（`release-*`、`git-<full-sha>`含む）をそれぞれ直近5世代だけ保持。
- 検証: PASS。TypeScript strict 0、Vitest 16 tests、credentialなしstrict synth、synth済みtemplateでregistry scanning・`ScanOnPush: false`・2件のcount型lifecycle ruleを確認。
- 残課題: 拡張スキャンの無料trial終了後はAmazon Inspectorのimage scan課金が発生する。USD 20 project budget、USD 30 account budget、USD 10 Cost Anomaly Detection通知で監視する。rollback可能なdigestは直近5世代に限定される（note 14へ明記済み）。deploy後のInspector finding通知とrunbookはSTEP-09C/11で整備する。

## 32. MAINT-FARGATE-TMPFS（heartbeatストレージのtmpfs化）

- `MAINT-FARGATE-TMPFS`、PR `#57`、2026-07-20。
- Scope: 2026-01-06公式発表を確認しFargate tmpfs mountを採用。STEP-09Bのbind mount（`Volumes`+`MountPoints`）を廃止し、平常/break-glass両task definitionの`linuxParameters.tmpfs`で`/tmp/shittim-chest`を1 MiB・`nosuid,nodev,noexec,uid=10001,gid=10001,mode=0700`で宣言。CDK `LinuxParameters`が`uid=`/`gid=`/`mode=`付きoptionを表現できないためL1 `addPropertyOverride`で宣言。用途はevent-loop heartbeatのみ。
- 検証: PASS。TypeScript strict 0、Vitest 16 tests、credentialなしstrict synth、synth済みtemplateの両task definitionに`Tmpfs`・`Volumes`なしを確認。
- 残課題: Fargate task differences開発者ガイドには同日時点で`tmpfs`非対応のstale記載が残存（What's NewとAPI referenceを優先しnote 14へ記録）。実挙動との差異は初回deploy時のtask起動（tmpfs mountの有無とheartbeat書込）で実動作確認し、失敗時はSTEP-09Bのbind mountへ戻す。

## 33. MAINT-NPM-AUDIT-ALLOWLIST（npm audit例外gate）

- `MAINT-NPM-AUDIT-ALLOWLIST`、PR `#58`、2026-07-20。
- Scope: aws-cdk-lib 2.261.0がbundleする`brace-expansion` 5.0.6にhigh advisory（GHSA-3jxr-9vmj-r5cp、glob DoS）が公開され`cdk` jobの`npm audit --audit-level=low`がfail。bundled依存はnpm overridesで置換できないことを実機確認し、`audit:infra`を`npm audit --json | node infra/check-audit.mjs`へ変更。`infra/npm-audit-exceptions.json`の期限付きentryだけを例外とし、未知advisory、metadata欠落、非JSON出力、期限切れは全てfail closed。
- 検証: PASS。例外有効時exit 0、未知advisory/advisory metadata欠落/非JSON入力でexit 1をlocal実測。`npm run check:infra`（tsc、Vitest 16 tests、strict synth）再合格。
- 残課題（risk acceptance）: 実害はsynth時に自前のCDK codeのglobだけを処理するbuild-time toolのため限定。aws-cdk-libが`brace-expansion >= 5.0.7`同梱版をreleaseしたら例外entryを撤去する。例外期限は2026-08-20で、期限切れ後はgateが自動failする。

## 34. MAINT-GRYPE-GATE（SBOM脆弱性scan・code scanning連携）

- `MAINT-GRYPE-GATE`、PR `#59`、2026-07-20〜21。
- Scope: operator指示によりGrype 0.116.0を`ci.yml`の新`grype` jobとして組み込んだ。`security`のCycloneDX source SBOMと`container-arm64`のSPDX arm64 image SBOMをartifact経由でdownloadし、`sbom:`入力でscanする。結果は`-o sarif=`でSARIF生成し`github/codeql-action/upload-sarif` v4.37.2（full SHA固定）でcode scanningへupload（job権限`security-events: write`）。alertはSecurity tabで確認し、high以上の新規alertはmain Rulesetのcode scanning ruleでblockする。全件JSON結果は`grype-scan-<sha>`として30日artifact保持。vulnerability DBはUTC日付keyのactions/cacheで1日cacheし、miss時だけ`grype db update`、scanは`GRYPE_DB_AUTO_UPDATE=false`。Grypeは`.github/tool-versions.json`でversion+archive SHA-256固定（weekly latest検知へ自動追加）。
- 失敗・やり直し: 初回CIで`Artifact not found`。upload-artifact v7の`archive: false`は単一file専用でfile名がartifact名となり`name`入力が無視される仕様（公式README確認）だった。file名指定へ修正した2回目は`Artifact download failed after 5 retries`となり、`archive: false`の生file artifactがdownload-artifact v7.0.0で取得できないことを確認したため、SBOM受け渡しとgrype結果は全て`archive: true`（zip、`<name>-<sha>`）へ統一した。SARIF版の初回は同一categoryへの複数SARIF run uploadがcode scanningの2025-07仕様変更で拒否されたため、uploadを2 stepに分け`grype-source-sbom`/`grype-image-sbom`のcategoryを分離した。
- SARIF検証（2026-07-21、PR run `29843685664`）: code scanning analyses APIでtool `Grype`の2 categoryを確認（image 171件、source 0件）。`grype` job全工程（SBOM download、checksum検証install、DB cache update、scan、artifact、SARIF upload）が合格した。
- 方針転換: 当初は`tools/report_grype.py`によるJob Summary・annotation・high以上でjob失敗のgateだったが、3回目のCIでgateが正しく動作しimage SBOMのhigh以上30件（critical 7・high 23、うち修正版ありは1件のみ）を検出した。Debian trixie package（perl-base/libc6/ncurses等）とpython 3.14.6の大半が未修正で、base image更新でも解消しないため、operator判断でSARIF/code scanning方式へ転換した。未修正の既存alertはSecurity tabで理由付きdismissとしてtriageし、新規high以上alertをruleset code scanning ruleでblockする運用とする。転換に伴い`tools/report_grype.py`と`tests/unit/tools/test_report_grype.py`は削除し、Job Summary生成とannotationは廃止した。
- 検証: PASS。ruff format/check、ty、import-linter、327 passed・92.52%、pinned actionlintで3 workflow syntax合格、`.github/tool-versions.json` validate合格。SARIF uploadとcode scanning表示はCI実行で検証する。
- 残課題: merge後にmain runでSARIF uploadを確認し、Grype toolのcode scanning rule（high以上でblock）と`grype` required checkをmain Rulesetへ追加する。fork PRでは`security-events: write`が降格されuploadできないため失敗しうる（外部contributor受入れ時に記録済みの制約として扱う）。pip-audit（Python、hash付き）とGrype（SBOM全件、OS package含む）は補完関係とし、Grype検出をもってpip-auditを緩和しない。

## 35. MAINT-SYFT-1.49.0・RULESET-REQUIRED-CDK

- 2026-07-22。main Ruleset「Protect main」の運用変更とweekly release-tool gateの赤を解消する保守slice。
- Scope: (1) `grype` required status checkとGrype code scanning rule（high以上の新規alertでblock）をRuleset ID `19047946`へ追加（34章の残課題を完了。main run `29874144596`のanalysesでtool `Grype` 2 category・image 171件/source 0件を確認後に実施）。(2) `cdk` jobをrequired status checksへ追加（note 15の追加予定を完了。required checksは`quality`・`tests`・`security`・`package`・`docs-public-safety`・`container-arm64`・`grype`・`cdk`の8件、全てGitHub Actions App由来・strict）。(3) weekly release-tool run `29898956982`が`syft: pinned v1.48.0, latest v1.49.0`でfail-closedしたため、公式releaseのchecksumsとarchive実測SHA-256（`c7c32de1…fe0fa4`）を照合してpinをSyft v1.49.0へ更新。Ruleset変更はAPI PUTレスポンスで確認、AWS・Discord・費用impactなし。

## 36. MAINT-GRYPE-SARIF-LOCATION（code scanning previewの修復）

- 2026-07-22。operator報告「code scanning alertが`Preview unavailable / couldn't find this file`」への対応slice。
- 原因: SBOM scanのGrype SARIFはlocationをSBOM metadata由来のディレクトリURI（全open alertが`shittim-chest/`・line 1）として出力し、GitHub code scanningはfileへの行anchorがないとpreviewを描画できない。code-scanning APIで全open alertのlocationを確認して特定した。Grype 0.116.0 CLIリファレンスを再確認したが、SARIF locationを制御するflag・設定は存在せず、`-o template`はSARIF全体のGo template再実装となり保守面で不利益のため不採用。
- Scope: `ci.yml`の`grype` jobにjq後処理stepを追加し、upload前に`artifactLocation.uri`を実在file（source SBOM scanは`uv.lock`、image SBOM scanは`Dockerfile`）・`startLine: 1`へ書き換える。package名・version等の固有情報はSARIF messageに既に含まれるため欠落なし。変換はpin済みGrype 0.116.0で生成した実SARIF（mini SBOM scan）に対しjqを実行して検証済み。
- 影響: location変更により既存alertはfixed扱いでcloseし、同一内容が新locationでopenし直す（alert番号は変わるが履歴は残る）。AWS・Discord・費用impactなし。

## 37. MAINT-DHI-VULNERABILITY-POLICY（DHI移行・脆弱性運用改善）

- 2026-07-22、branch `agent/adopt-dhi-vulnerability-policy`、PR `#62`。AWS deploy、ECR push、Ruleset変更、secret値の取得・変更はこのsliceに含めない。
- Scope: production/runtimeをDocker Official Python slimからDHI Community Python `3.14.6-debian13`へ変更し、builder/break-glassは対応する`-dev`を使う。tagとOCI image index digest、ARM64 manifest digest、DHI `nonroot` identity `65532:65532`、home `/home/nonroot`、1 MiB heartbeat tmpfsを`container-policy.json`で一元管理し、Dockerfile・container gate・CDKを同一契約で検証する。productionはshell/package manager非搭載、break-glassは独立DHI dev targetで必要な調査toolだけを追加する。
- Registry実測: runtime index `sha256:c43e37b1...a850b3e`はARM64 manifest `sha256:983c8722...a882d`、dev index `sha256:1df3badf...4928b`はARM64 manifest `sha256:e868dd92...896b1a`を持つ。runtime config `User=65532`、passwd/group `nonroot:65532`、home `/home/nonroot`、DHI runtime label、`/bin/sh`・`/bin/bash`・`apt`・`dpkg`非搭載を確認した。
- Grype policy: full JSONは全findingを保存し、`--only-fixed`のJSON/SARIFと`--fail-on high`でfixable High/Criticalをブロックする。DHI OpenVEXはDocker Scout 1.23.1の`--verify --skip-tlog`でDocker公開鍵signatureを検証してGrypeへ適用する。それ以外の未修正High/Criticalはdigest固定・owner・根拠・期限90日以内の個別acceptanceがなければfail closedとする。一括dismissは使わない。
- 変更前baseline（main ARM64 CI artifact）: image 203,267,819 bytes、SPDX package 132（Debian 87）、Grype 171 findings / 71 unique CVE / Critical 7 / High 23 / fixable High/Critical 1。
- 変更後の予備検証（local AMD64、ARM64最終判定の代替にしない）: production/break-glass build成功、image 180,870,208 bytes（変更前比約11.0% 削減）、SPDX package 152（Debian 100）、full Grype 206 findings / 52 unique CVE / Critical 6 / High 40 / fixable High/Critical 0。署名検証済みOpenVEX 506 statementsは全て`not_affected`で、Grype適用後のactive matches 0。risk gateはraw reportのHigh/Critical 23個別package/CVE pairをvendor VEXでcoverし、local acceptance 0でPASS。raw finding数はVEXで消さずartifactに残す。
- Native ARM64最終検証: PR `#62` run `29919903433`でPASS。DHI indexから記録済みARM64 manifestを解決し、Docker上でUID/GID `65532:65532`、home、shell/package manager/uv/source/test非搭載、read-only root、1 MiB・`nosuid,nodev,noexec` tmpfs、heartbeat、全phaseのSIGTERM、transaction境界のSIGKILL復旧を確認した。ARM64 imageは181,878,650 bytes、SPDX 152 packages（Debian 100）。raw Grypeは206 active / 52 unique CVE / Critical 6 / High 40 / fixable High/Critical 0で完全reportへ保存した。署名検証済みDHI OpenVEX 506 statements（全て`not_affected`）適用後はactive 0、risk gateはHigh/Criticalの23 package/CVE pairをvendor VEXでcoverし、local acceptance 0でPASS。source scanも0件。
- 失敗・やり直し: 初回PR run `29919572796`は選択許可Actionsに未登録の`docker/login-action`を追加したためjob生成前に`startup_failure`。GitHub設定を拡張せず、Secretを標準入力する`docker login --password-stdin`へ置換した。次のrun `29919760148`は`python tools/run_container_gate.py`実行時にrepository rootがmodule search pathへ入らず`ModuleNotFoundError: tools`となったため、`python -m tools.run_container_gate`へ修正した。修正後runでは全8 jobがPASSした。
- 制約・残課題: rakuOSのrootless PodmanはDHI image root mountのtraverse権限で`65532`実行が`Permission denied`となるため、native ARM64 GitHub Docker runnerの結果を最終判定とした。Actions/Dependabotへread-only DHI credentialを別々に登録済みだが値は取得していない。Dependabot daily Docker updateの実runはdefault branch merge後に確認する。Community tierのためremediation SLAはなく、digest更新PRとscanner/VEX evidenceを日次監視する。
- 追加修正: sandboxのDNS遮断中に`npm audit --json`がerror JSONを返した際、既存checkerが`vulnerabilities` 0件と誤判定し`clean`で通過するfail-openを発見した。`error`の拒否と`auditReportVersion`/`metadata.vulnerabilities`/`vulnerabilities`の必須構造検証を追加し、registry error・不完全reportがexit 1、実advisory API接続で既存期限付き1件のみnpm audit合格を確認した。
- Local validation: `uv lock --check`、Ruff format/check、ty、import-linter、digest固定DynamoDB Local付きpytest 351 passed・coverage 92.52%、CycloneDX 1.5 source SBOM 91 components、pip-audit 0 known vulnerability、npm audit（期限付き1件のみ）、TypeScript strict、Vitest 16 passed、`cdk synth --strict`、container policy、public surface 170 files、14 Obsidian mirror、actionlintが合格した。

## 38. 公式資料確認記録（トピック別・最新）

同一トピックの古い重複は削り、最新の確認だけを残す。各slice本文の設計反映を正とする。

| 確認日 | 対象 | 公式資料 | 設計反映 |
|---|---|---|---|
| 2026-07-17 | GitHub secure Actions | https://docs.github.com/en/actions/reference/security/secure-use | read-only permission、GitHub-hosted runner、full SHA、fork境界 |
| 2026-07-17 | Dependabot version updates/options | https://docs.github.com/en/code-security/reference/supply-chain-security/dependabot-options-reference | `uv` v0対応、weekly group、open PR limit |
| 2026-07-17 | GitHub SBOM export / Dependency Graph | https://docs.github.com/en/code-security/concepts/supply-chain-security/dependency-graph-data | managed graph採用、custom submission省略、SPDX 2.3週次照合 |
| 2026-07-17 | GitHub Ruleset rules / REST API | https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/available-rules-for-rulesets | source App固定required check、strict policy、CodeQL merge protection |
| 2026-07-17 | Betterleaks 1.6.1 / security policy | https://github.com/betterleaks/betterleaks/blob/main/.github/SECURITY.md | latest releaseのみsupportのためweekly fail-closed監視、provider validation無効 |
| 2026-07-17 | cosign verify-blob / Sigstore CI | https://docs.sigstore.dev/cosign/verifying/verify/ | certificate identity、OIDC issuer、bundle検証 |
| 2026-07-19 | uv GitHub Actions / Docker integration 0.11.29 | https://docs.astral.sh/uv/guides/integration/docker/ | setup-uv full SHA、frozen sync、`--no-install-project`中間layer、cache mount、`UV_PYTHON_DOWNLOADS=0` |
| 2026-07-17 | uv CycloneDX export / cyclonedx-python-lib 11.11.0 | https://docs.astral.sh/uv/concepts/projects/export/ | preview出力をstrict JSON schemaとlock inventoryで保護 |
| 2026-07-17 | DynamoDB data types/item size/TransactWriteItems/GSI/Query | https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_TransactWriteItems.html | 400KB事前拒否、同一item二重action禁止、GSIは候補発見のみ、1 MB pagination |
| 2026-07-19 | DynamoDB Local 3.3.0 | https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.DownloadingAndRunning.html | digest固定、`-inMemory -sharedDb -disableTelemetry`、dummy credential、production差異の認識 |
| 2026-07-17 | boto3/boto3-stubs 1.43.50 | https://pypi.org/project/boto3/ | typed client、同期SDKのworker thread隔離 |
| 2026-07-19 | DynamoDB Service Authorization Reference | https://servicereference.us-east-1.amazonaws.com/v1/dynamodb/dynamodb.json | 実使用operationとtable/indexへの最小action照合 |
| 2026-07-17 | OpenAI Python 2.46.0 / Structured Outputs / error codes | https://developers.openai.com/api/docs/guides/structured-outputs | `responses.parse()`、refusal/incomplete分類、major/operational failure分離 |
| 2026-07-17 | GPT-5.6 model family / pro mode | https://developers.openai.com/api/docs/guides/latest-model | Luna standard固定、proはmodel独立のmode、代表評価先行 |
| 2026-07-17 | OpenAI Web search / Responses create | https://developers.openai.com/api/reference/resources/responses/methods/create | hosted `web_search`、source include、`max_tool_calls=4`、`store=false` |
| 2026-07-17 | OpenAI prompt指針 | https://developers.openai.com/api/docs/guides/prompt-engineering#coding | 短いpersona promptと代表fixtureによる変更評価 |
| 2026-07-17 | Responses Multi-agent beta | https://developers.openai.com/api/docs/guides/responses-multi-agent | beta非採用。stable ResponsesとPython orchestration |
| 2026-07-17 | discord.py 2.7.1 / Discord Message / Rate limits / Gateway | https://docs.discord.com/developers/resources/message | content 2,000、nonce 25、`enforce_nonce`、allowed mentions、Retry-After委譲、GUILDS-only Intent |
| 2026-07-17 | Discord Interactions / Application Commands / Components | https://docs.discord.com/developers/interactions/receiving-and-responding | 3秒defer、Guild command、STRING length、custom ID 100文字 |
| 2026-07-17 | Python 3.14.6 asyncio/signals/subprocess/typing/UUID | https://docs.python.org/3.14/library/asyncio-task.html | TaskGroup、timeout、signal ownership、`CancelledError`再送出、UUIDv7 |
| 2026-07-17 | Pydantic 2.13.4 / httpx 0.28.1 / pytest-asyncio 1.4.0 / import-linter 2.13 | https://pydantic.dev/docs/validation/latest/concepts/models/ | strict/frozen/extra-forbid境界、layer contract |
| 2026-07-17 | ECS Fargate Spot / ContainerDefinition | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/fargate-capacity-providers.html | Spot SIGTERM前提、`stopTimeout`最大120に対し内部90秒deadline |
| 2026-07-19 | ECS task IAM / SSM Parameter injection / FargateService 2.261.0 | https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html | `SourceAccount`/`SourceArn`でconfused deputy防止、execution role最小化 |
| 2026-07-20 | ECS tmpfs on Fargate | https://aws.amazon.com/jp/about-aws/whats-new/2026/01/amazon-ecs-tmpfs-mounts-aws-fargate-managed-instances/ | Fargate tmpfs採用（開発者ガイドのstale記載は初回deployで実動作確認） |
| 2026-07-20 | npm overrides / bundled dependencies | https://docs.npmjs.com/cli/v11/configuring-npm/package-json#overrides | bundled依存はoverrideで置換できないため期限付きexception gateを導入 |
| 2026-07-22 | Grype CLI 0.116.0 SARIF location | https://oss.anchore.com/docs/reference/grype/cli/ | SARIF location制御のflag・設定なし。jq後処理で実在file（`uv.lock`/`Dockerfile`）へ付替 |
| 2026-07-20 | Grype CLI / configuration 0.116.0 | https://oss.anchore.com/docs/reference/grype/cli/、https://oss.anchore.com/docs/reference/grype/configuration/ | `sbom:`入力、`json=`/`sarif=`出力、`GRYPE_DB_AUTO_UPDATE`、DB max age 120hと1日cacheの整合 |
| 2026-07-21 | GitHub code scanning SARIF upload | https://docs.github.com/en/code-security/code-scanning/integrating-with-code-scanning/sarif-support-for-code-scanning、https://github.com/actions/upload-artifact | `security-events: write`でSARIF upload。upload-artifact v7の`archive: false`はfile名がartifact名になり複数file不可のため受け渡しはzipへ統一 |
| 2026-07-17 | GitHub native ARM64 runner GA | https://github.blog/changelog/2025-08-07-arm64-hosted-runners-for-public-repositories-are-now-generally-available/ | `ubuntu-24.04-arm`をnative required gateへ採用 |
| 2026-07-22 | Syft 1.49.0 | https://github.com/anchore/syft/releases/tag/v1.49.0 | arm64 archive SHA-256固定、SPDX 2.3 image SBOM（v1.48.0からweekly検知で更新） |
| 2026-07-19 | ty 0.0.61 | https://docs.astral.sh/ty/coming-from-mypy-or-pyright/ | strict相当2 rule維持で唯一のtype-check gate |
| 2026-07-19 | setup-buildx-action v4.2.0 / build-push-action v7.3.0 / GHA cache backend | https://docs.docker.com/build/cache/backends/gha/ | full SHA pin、`load: true`、scope分離、`ignore-error=true` |
| 2026-07-19 | AWS CDK prerequisites / Node versions | https://docs.aws.amazon.com/cdk/v2/guide/node-versions.html | Node.js 24.18.0 Active LTS、local exact CLI |
| 2026-07-19 | Node.js releases | https://nodejs.org/en/about/previous-releases | Node 24採用、Node 26はLTS入りまで不採用 |
| 2026-07-19 | CDK DynamoDB Table / ECR Repository / cdk-nag 3 / setup-node v7.0.0 | https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_dynamodb.Table.html | PITR/deletion protection/RETAIN、完全immutable、Validation Plugin API、full SHA pin |
| 2026-07-20 | ECR Managed Signing / RegistryScanningConfiguration / OCI Referrers / Signer verification | https://docs.aws.amazon.com/AmazonECR/latest/userguide/managed-signing.html | `ENHANCED`/`CONTINUOUS_SCAN`、Signer profile、Notation strict verificationとreferrer照合をrelease gateへ確定 |
| 2026-07-30 | AWS Signer Notation ECR認証 / Amazon ECR Docker Credential Helper 0.12.0 / GitHub OCI attestation | https://docs.aws.amazon.com/signer/latest/developerguide/image-signing-steps.html、https://github.com/awslabs/amazon-ecr-credential-helper/releases/tag/v0.12.0、https://github.com/actions/attest | `notation login`を廃止し、NotationはSHA-256固定ARM64 helperとOIDC AWS credentialを使用する。build/`actions/attest`/`gh` OCI clientは一時Docker loginを使用する |
| 2026-07-19 | CloudWatch Logs data protection | https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/mask-sensitive-log-data.html | 90日log groupへmask policy |
| 2026-07-19 | Docker build best practices / cache optimize | https://docs.docker.com/build/building/best-practices/ | multi-stage、digest固定、`.dockerignore` allowlist、cache責務分離 |

| 2026-07-22 | DHI Community・移行 | https://docs.docker.com/dhi/how-to/use/、https://docs.docker.com/dhi/features/ | Community無償・Apache-2.0、`dhi.io`認証、dev/runtime分離、non-root/distroless、SLA非保証 |
| 2026-07-22 | DHI VEX・検証 | https://docs.docker.com/dhi/how-to/scan/、https://docs.docker.com/dhi/how-to/verify/、https://docs.docker.com/dhi/core-concepts/vex/ | Scout公開鍵signature検証、`--skip-tlog`の境界、Grype `--vex`、`not_affected`判定 |

## 39. Future slice template

各後続stepは次を複製して追記する。

1. Header: step ID、PR番号、merge commit短縮SHA、date、requirements/test IDs。
2. Scope/non-scopeの1〜2行要約と外部state変更。
3. 検証結果の要約: 合格した主要ゲート、テスト数、coverage、代表CI run URL 1件。
4. 失敗・再実行・条件付きskipの事実と最終結果。
5. 未解決事項・残課題・risk acceptanceとowner。
6. 外部impact（共通注記から外れる場合だけ個別記載）。
7. 新規トピックの公式資料確認は37章の表へ追記する（確認日・対象・URL・設計反映）。

## 40. STEP-02D-A GitHub Actions workflow completion通知

- 実装日: 2026-07-23
- Pull Request: `#71`（draft、mergeable/CLEAN、CI・CodeQL・Grype含む全check合格）
- 対応試験: `QT-GH-DISCORD-A`、`QT-Docs`、`QT-Workflow`
- scope: 標準libraryのみの共通通知package、`workflow_run`通知、Discord Embed上限・無害化・mention、GitHub API、有限Webhook retry、設計・試験文書。
- non-scope: 実Discord Webhook通信、Secret/Variable登録、通知有効化、PR/push通知、Security Digest、AWS、Bot/Application。

検証結果:

1. `uv --cache-dir /tmp/uv-cache run --frozen ruff format --check .`、`ruff check .`、`ty check`: 全て合格。
2. dummy AWS credentialとmetadata無効化を明示したhost実行の`uv --cache-dir /tmp/uv-cache run --frozen pytest`: 378 collected、373 passed、5 skipped、domain/application coverage 92.52%。新規通知unit/contractは27件合格。
3. actionlint 1.7.12は`.github/tool-versions.json`のSHA-256と一致するarchiveから実行し、workflow finding 0。
4. `python -m tools.check_docs`: 15文書の公開構造検査に合格。`sync_docs.py --check --source <canonical-source>`: Obsidian正本とmirrorの15文書がbyte一致。
5. 単体試験でname/path allowlist、GitHub管理の同名workflow除外、失敗job 8件上限、Unicode・Markdown・双方向制御文字、Embed 6,000文字上限、role mention、429/5xx/transportの最大4回retry、400即時失敗、URL非表示を確認した。
6. `DISCORD_NOTIFICATIONS_ENABLED` は未設定のままであり、実Webhook URL、thread ID、role ID、GitHub Secret/Variableは作成・取得・表示していない。

制約と後続:

- 通知はat-least-onceであり、手動rerunやDiscord応答喪失で重複し得る。
- STEP-02D-Bでmetadataだけの限定`pull_request_target`、PR/merge/direct push、negative workflow policy testを追加する。
- STEP-02D-CでDependabot/CodeQL/Grype Digestと停止検知を追加し、operatorがDiscord/GitHub実設定とmanual smoke testを実施した後だけ通知を有効化する。

## 41. STEP-02D-B Repository event通知・target workflow安全境界

- 実装日: 2026-07-23
- Pull Request: `#72`（`#71` baseのstack draft、mergeable/CLEAN、CI・Grype含む全check合格）
- 対応試験: `QT-GH-DISCORD-B`、`QT-Workflow`
- scope: PR `opened`/`reopened`/`ready_for_review`/`closed`、merge由来main push抑制、direct push、関連PR API判定不能通知、通知専用`pull_request_target`、専用policy checker。
- non-scope: Dependabot PRからのWebhook送信、Security Digest、実Discord/GitHub設定、通知有効化。

検証結果:

1. 通知関連の47試験で、通常PR、Dependabot skip、未対応action skip、merge/未merge close、merge push抑制、direct push、API判定不能、非main push、未知API itemを確認した。
2. negative policyは、許可外`pull_request_target`、write/write-all権限、PR head checkout、追加checkout、artifact、cache、self-hosted runner、inline/multiline shellの未信頼event展開、追加Secretを拒否した。
3. 実workflowは`contents: read`と`pull-requests: read`だけを使用し、`ref: ${{ github.sha }}`と`persist-credentials: false`を強制した。PR head、PR由来script、artifact、cacheは取得しない。
4. Dependabot PRはworkflow jobとPython applicationの両方で、Webhook設定読込み前にskipする。Dependabot SecretへWebhookは追加していない。
5. actionlint 1.7.12、Ruff、ty、専用policy commandが合格。hostの全pytestは398 collected、393 passed、5 skipped、coverage 92.52%。通知は未設定のenable variableによりfail-safe skipを維持した。

## 42. STEP-02D-C Security Digest・停止検知

- 実装日: 2026-07-23
- Pull Request: `#73`（`#72` baseのstack draft、mergeable/CLEAN、CI・Grype含む全check合格）
- 対応試験: `QT-GH-DISCORD-C`、`QT-Workflow`
- scope: 毎日09:37 JSTのDependabot/Security Digest、全page取得、PR check/mergeability、workflow/analysis停止検知、通知workflow失敗のSecurity thread警告。
- non-scope: 実Webhook、Discord Forum/thread/role作成、GitHub Secret/Variable登録、通知有効化、manual smoke test。

検証結果:

1. 関連62試験でOpen alert 0件、Critical/High role mention、上位5件、severity/tool集計、PR check/mergeability、7日停滞、8日workflow鮮度、CodeQL失敗・停止、current mainのCodeQL/Grype欠落を確認した。
2. GitHub API失敗時はDependabot/Security件数を表示せず、Security threadへrole付きのcount-free監視失敗を1件送ることと、その後にworkflowが失敗することを確認した。
3. notification workflow自体の成功は重複通知せず、Repository EventsとSecurity Digestの失敗だけをSecurity threadで警告する。
4. GitHub公式workflow syntaxで`vulnerability-alerts: read`がDependabot Alerts読取用と確認した。actionlint 1.7.12の未追随診断だけを除外し、専用policyで当該workflow 1件のread以外を拒否する。
5. Discord/GitHub実値は取得・保存・送信していない。operatorが4固定threadとWebhookを作成し、Secret/Variable登録後にmanual Digestと実PRのsmoke testを行うまで`DISCORD_NOTIFICATIONS_ENABLED` はfalse/未設定とする。
6. hostの全pytestは413 collected、408 passed、5 skipped、coverage 92.52%。Ruff、ty、actionlint限定除外、notification policy、15文書構造・byte一致も合格した。
7. 本文を出力しない読取専用の実GitHub API確認で、2026-07-23時点のOpen Dependabot Alert 1件、Open Code scanning Alert 0件、main `739d023`のCodeQL・Grype analysis、GitHub管理CodeQL path `dynamic/github-code-scanning/codeql`を確認し、実response形状と収集logicの一致を検証した。

## 43. 通知role廃止・Forum公開範囲変更

- 実装日: 2026-07-23
- Pull Request: `#74`
- scope: `DISCORD_ALERT_ROLE_ID`の任意化、role未設定時の全mention無効化、フレンド限定server内の`@everyone`閲覧方針、試験・運用文書。
- non-scope: 実Forum/thread/Webhook作成、GitHub Secret/Variable登録、通知有効化、manual smoke test。

検証結果:

1. CI failure、High/Critical alert、監視失敗を含め、role ID未設定時は`content`を生成せず`allowed_mentions.parse=[]`だけを送る回帰試験を追加した。
2. `DISCORD_ALERT_ROLE_ID`はGitHub Repository Variableへ登録しない。Forumはフレンド限定serverの`@everyone`へ公開し、server member管理を閲覧の信頼境界とする。
3. 実Webhook URLとthread IDは引き続きrepository、Obsidian、log、artifactへ保存せず、`DISCORD_NOTIFICATIONS_ENABLED=false`のまま実設定とsmoke testを待つ。
4. role任意化を含む通知対象42試験は全件合格。host全試験は415 collected、410 passed、5 skipped、coverage 92.92%で合格した。
5. Ruff format/lint、ty、通知workflow policy、文書構造、`git diff --check`、Obsidian正本と15文書mirrorのbyte一致が全て合格した。
6. PR CIは初回のCDK auditでnpm公式endpointの一時的なHTTP 503によりfail closedした。コードや監査条件を変更せずfailed jobsを再実行し、CDK、CodeQL、Grype、ARM64 containerを含む全checkが合格した。

## 44. Discord通知smoke test・Python互換性修正

- 実施日: 2026-07-23
- scope: GitHub hosted runnerのsystem Python互換性、通知packageのPython 3.12 AST regression test。
- non-scope: Discord表示の受入、本番通知有効化。

検証結果:

1. `DISCORD_NOTIFICATIONS_ENABLED=true`でSecurity Digestをmanual dispatchしたところ、Webhook送信前のimportで`except UnicodeDecodeError, ValueError:`がSyntaxErrorとなった。Python 3.14では許可されるがGitHub hosted runnerのsystem `python3`では利用できない構文だった。
2. 直ちに`DISCORD_NOTIFICATIONS_ENABLED=false`へ戻し、一時PR `#75`を未mergeでclose、remote smoke branchを削除した。実Webhook URLはActionsでmaskされ、取得・表示していない。
3. RuffのPython 3.14 formatterが複数例外の括弧を省略するため、互換な例外tuple定数を`except`対象にする構文へ修正し、通知package全fileをPython 3.12 grammarで`ast.parse`する回帰試験を追加した。
4. 互換性修正をmerge後、Security Digest、CI completion、PR open/closeのsmoke testを最初から再実行する。本項の時点では通知は無効のままとする。
5. 修正後のhost全試験は416 collected、411 passed、5 skipped、coverage 92.52%。Ruff、ty、Python 3.12 AST互換性、通知workflow policy、文書構造、Obsidian正本と15文書mirrorのbyte一致が合格した。

## 45. Discord通知表示修正

- 実施日: 2026-07-23
- scope: 複数行field、Markdown escape、dependency alert表示、workflow開始日時のJST化。
- non-scope: 通知内容の追加、通知先変更、mention有効化。

検証結果:

1. 実Discord smokeで監視警告のLFが`�`へ置換され、Dependabot alertのpipe、hyphen、dotが過剰escapeされ、workflow開始日時がUTC ISO文字列のまま表示されることを確認した。
2. CRLF/CRをLFへ正規化してLFだけを保持し、他の表示制御文字は引き続き`�`へ置換する。link・mention・強調に関係するMarkdown記号だけをescape対象とした。
3. `high | brace-expansion | — | 5.0.7`の完全一致、複数行警告、`2026-07-23T00:00:00Z`から`2026-07-23 09:00:00 JST`への変換を回帰試験へ追加した。
4. 表示修正PR `#78`をmerge後、manual Security Digest、CI completion、下流Discord通知を再実行し、全workflowが成功した。
5. host全試験は419 collected、414 passed、5 skipped、coverage 92.52%。Ruff format/lint、tyも合格した。
6. Security Digest run `29999436878`、CI run `29999438305`、下流Discord Workflow Notifications run `29999613891`が成功した。送信stepはDiscordの成功応答を受け、通知失敗はなかった。
7. 最終smoke合格後、Repository Variable `DISCORD_NOTIFICATIONS_ENABLED=true`を維持して恒久有効化した。

## 46. STEP-06D Discord requester名のスナップショット保存

- 実施日: 2026-07-26
- branch: `agent/step-06d-requester-name-snapshot`
- base: `main`
- PR: `#81`
- merge commit: `8fd90b5`
- scope: `AcceptDebateRequest`/`DebateSnapshot`へ`requester_username`と`requester_display_name`を追加。Discord interactionから`user.name`/`user.display_name`を取得。DynamoDB Debate METAへ保存しschema v6。v5→v6 migrationはlegacy fallbackとして両フィールドへ`requester_id`を設定。operation ID replayはname snapshotを含むrequest bindingを強制。認可は`requester_id`のみ。
- non-scope: Webアーカイブ、検索GSI、Cognito、Streams、名前の後追い更新、実Discord/AWS接続。

検証結果:

1. 取得元は`interaction.user.name`と`interaction.user.display_name`。`str(user)`やREST再fetchは使わない。
2. 正本はDebate METAのみ。Attempt/outbox/panelへは重複保存しない。
3. v5 debate_meta migrationは実名ではなく`requester_id` fallback。v4と未知versionはfail closed。
4. host試験: `uv run --frozen pytest` 421 passed / 5 skipped、coverage 92.58%。DynamoDB Local helper経由は426 passed。
5. Ruff format/lint、ty check、import-linterを含む品質gateを実施。
6. 名前をlog/metrics/OpenAI requestへ追加していない。PK/SK/GSI/lease/fencingは不変。

## 47. STEP-06D CI: brace-expansion GHSA-mh99-v99m-4gvg 例外

- 実施日: 2026-07-26
- scope: `infra/npm-audit-exceptions.json`へGHSA-mh99-v99m-4gvg（high、brace-expansion <=5.0.7 OOM DoS）の期限付き例外を追加。
- 理由: aws-cdk-lib 2.261.0がbundleするbrace-expansion 5.0.6が対象。bundled依存はnpm overridesで置換不可。2.262.1でも5.0.7同梱のため未解消。修正版は5.0.8。
- 期限: 2026-08-20（既存GHSA-3jxr-9vmj-r5cpと同じ）。aws-cdk-libがbrace-expansion >=5.0.8を同梱したら両entryを撤去する。
- non-scope: aws-cdk-libのversion bump、production deploy。

## QUALITY-RUFF-0.16（2026-07-27）

- 範囲: Ruff 0.15.22 → 0.16.0 の専用ツール昇格。Markdown内Pythonフェンスformatを既定受け入れ。明示lint `select`は維持。
- 変更: `pyproject.toml`/`uv.lock`、`docs/01` Protocol stub 4箇所（Obsidian正→sync）、`docs/18` 公式確認記録行追加、`AGENTS.md` Technology Baseline。
- 検証: `uv lock --check`、`ruff format --check .`、`ruff check .`、`ty check`、`lint-imports`（PR CIで quality/tests 等を確認）。
- 除外: lint select拡大、docs Markdown formatの `extend-exclude`、アプリ依存の同梱更新なし。
- 未解決: なし。次の ruff minor（0.17）は専用調査PR。Dependabot python group に混在した場合は ruff を外してから merge。

## 48. Discord HTTP ingress・On-Demand Fargate scale-to-zero

- 実施日: 2026-07-28
- branch: `feat/discord-http-scale-to-zero`
- Draft Pull Request: `#85`
- 要件・設計正本: Obsidianの`100_Ondemand Fargate`配下3文書（当時は公開mirror対象外。2026-07-29から同じ相対pathで公開mirror対象）
- scope: 署名付きDiscord HTTP Interaction、永続FIFO 20件、Status Message、runtime reconciler、On-Demand Fargate `desiredCount=0`・最大1 task、3/15/30分境界、deployment fencing、schema v7、control manifest v2、CDK・CI・文書。
- non-scope: AWS CDK bootstrap/deploy/diff、ECR push、production DynamoDB write、Discord Application/Interactions Endpointの作成・変更、実Webhook/Interactionの送信。

論理commitは各回pushし、最初の実質commit後からDraft PRを維持した。現在の実装列は次のとおり。

1. `2c8da8c` domainのingress/runtime state model
2. `ebd3293` validation branch test
3. `9c82eec` idempotent ingress queue
4. `8f8356a` fenced runtime state
5. `20d8f3e` Discord raw-body signature/parser
6. `73b82c9` durable ingress acceptance
7. `28a4061` durable public status publication
8. `efb7924` application command boundary
9. `071ee76` recovery後のingress drain
10. `51855dd` wakeとstartup deadline reconciler
11. `142f53f` 30分complete-idle scale down
12. `c3ab149` scale-down transactionの回帰修正
13. `6bad6d7` scale-down convergence race test
14. `ad5c8a9` graceful shutdown連携
15. `bb1bb05` On-Demand scale-to-zero CDK
16. `488c922` read-only deployment guardとPR check report
17. `dbf3909` local E2E・runtime/deployment race test

`64bcfde`は作業中に更新された`origin/main`を取り込んだmergeであり、機能の論理commitではない。

確定済み検証証跡:

1. Commit 13時点でDynamoDB Localを含む1384 testsが合格し、domain/application coverageは90.54%だった。CDKは23 tests、TypeScript strict、cdk-nag、credentialなしstrict synthが合格した。
2. Commit 14時点でDynamoDB Localを含む1389 testsが合格し、coverageは90.50%だった。Ruff、ty、import-linter、public-surface 273 testsも合格した。
3. E2Eは署名付きHTTP Interactionから永続ingress/status、reconciler wake、runtime owner/READY/recovery gate、ingress drain受付までをDynamoDB Localで実行した。Discord/ECS/final command/contextの外部portだけをrecording fakeとし、interaction tokenが永続recordへ入らないことを確認した。
4. `CT-GUARD-RACE-001`〜`003`でbreak-glass acquireと初回wake/re-wake、successor fence取得後のstale releaseを決定的に確認した。`CT-SCHEMA-009`でmanifest v1 markerのfail-closed・無変更を確認した。これらはlocal/DynamoDB Local contractであり、liveの`IT-GUARD-002`〜`006`完了とは扱わない。
5. GitHubではbranch、Draft PR、workflow、既存機構によるDiscord CI/PR通知だけを変更対象とした。AWSとDiscord Developer/Applicationへのwriteは0件である。
6. Commit 14 headのGitHub Actions run `30366923122`は`quality`、`tests`、`security`、`package`、`cdk`、`container-arm64`、`docs-public-safety`、`grype`がすべて合格した。CodeQL run `30366918546`のActions、Python、JavaScript/TypeScript解析も合格した。
7. Commit 15直前の最終local gateは、DynamoDB Localを含む1408 tests、domain/application coverage 90.50%、Ruff、ty、import-linter、TypeScript strict、CDK 23 tests、cdk-nag、credentialなし`cdk synth --strict`が合格した。`npm audit:infra`は期限付き2例外内で合格し、CycloneDX 1.5 SBOMは97 dependency components、`pip-audit`は既知脆弱性0件、sdist/wheel buildは成功した。追加したsource-onlyとdestination extraの安全性検証は19 unit tests、通知workflow policyとactionlint、15公開文書検証、274 filesのpublic-surface scan、`git diff --check`も合格した。

control manifest v1はproductionへdeployされていない。初回production contractはv2とし、もしv1 markerを検出した場合はinitializerで修復せず停止し、offline migrationと監査証跡を必須とする。

未実施の実環境受入は、Discord PING・実署名・Status Message、ECS `0→1→0`、3/15/30分の壁時間、実IAM・alarm、deployment lock/break-glass/rollbackである。これらをlocal合格と同義にしない。PR `#85`はrequired CI/CodeQL合格後、`0ca92fb`としてmainへmerge済みである。

## 49. STEP-09C-A 低cardinality運用メトリクス基盤

- 実施日: 2026-07-29
- branch: `agent/step-09c-a-observability`
- base: PR `#85` merge後の`main`
- scope: CloudWatch EMF serializer、Runtime 60秒health producer、Runtime Reconciler invocation producer、10 metric allowlist、content-free/value/task-ownership試験、監視・費用・実装計画文書更新。
- non-scope: AWS/CDK resource write、CloudWatch alarm/dashboard/SNS、EventBridge task停止通知、Budget/CAD作成・変更、subscription、AWS bootstrap/deploy、Discord Application変更、ECS `PRE_SCALE_UP` image admission。

確定事項と検証結果:

1. namespaceは`ShittimChest/Prod`、dimensionは`Service` 1組だけ、値は`runtime`/`reconciler`へ固定した。Environmentは非dimension root propertyで、Discord/Guild/debate identifierと本文はEMFへ渡さない。
2. Runtimeは`BotReady`と`HeartbeatAgeSeconds`を60秒周期で発行する。readiness probeは10秒timeoutか例外でfail closedの0、heartbeatの欠落・未来時刻はageを発行しない。background taskはasync contextが所有し終了時にawaitする。
3. Reconciler成功は`RuntimeStateCode`、desired count、ECS running/pending、Ingress pending、Outbox pending、failure 0を1 documentで発行する。失敗は`ReconcilerFailed=1`、Status publication trigger失敗時だけ`StatusPublishFailed=1`とする。
4. EMFは既存stdout/CloudWatch Logs経由で非同期抽出するため、`cloudwatch:PutMetricData`権限と追加SDK dependencyを導入していない。standard resolution 60秒、10 metric allowlist以外、負数、bool、NaN/Infinity、naive datetime、非production namespaceを拒否する。
5. Ruff format/check、ty、import-linterは合格した。追加境界試験、health、Reconciler、bootstrap、application Reconcilerの限定89 testsが合格した。制限sandboxで停止した既存Discord ingressのthread/asyncio deadline試験はhostで再実行して1 passed。metric sink例外がRuntime/Reconciler成功を失敗へ変えず、private provider detailをlogへ出さない回帰試験も追加した。18文書化後のsync/check回帰38 testsと、digest固定DynamoDB Local helperによるhost全suite 1426 passed、skip 0、coverage 90.39%が合格し、試験後containerをcleanupした。
6. Runtime alarm、AWS Budget、Cost Anomaly Detectionは同一operator emailを使うと決定した。実addressは公開sourceへ保存せず、STEP-09C-B/Cのdeploy parameterとsubscription確認で扱う。
7. image admissionはOperations監視から分離し、署名/referrer/release manifestと同じtrust boundaryを持つSTEP-10-Aへ移した。
8. `100_Ondemand Fargate/`の要件・commit plan・completion checklistを公開可能と判断し、Obsidian正本の同じ相対pathで`docs/`へmirrorした。sync/check toolは直下15件と入れ子3件の完全file set、symlink拒否、byte一致、公開情報scanを再帰的に検証する。

残課題: STEP-09C-BでOperationsStackの最小alarm/dashboard/SNS/EventBridge、STEP-09C-CでBudget/CADと通知到達・旧手動通知撤去、STEP-10-Aでimage admissionを実装する。実CloudWatch EMF抽出とalarm動作はAWS deploy後の受入事項であり、このsliceで合格扱いにしない。

## 50. STEP-09C-B Operations monitoring CDK

- 実施日: 2026-07-29
- branch: `agent/step-09c-b-operations-monitoring`
- base: PR `#92` merge後の`main`
- scope: `OperationsStack`、deploy時operator email parameter、単一SNS topic/email subscription、9 metric alarm、critical/warningの2 composite alarm、1 dashboard、異常ECS task stop EventBridge rule、CDK assertion/cdk-nag/strict synth、設計・運用・試験文書更新。
- non-scope: AWS bootstrap/deploy/diff、SNS subscription確認、test notification、live EMF/alarm/EventBridge受信、Budget/CAD、旧手動通知撤去、Discord Application変更。

確定事項と検証結果:

1. `OperationsStack`はRuntime/Statefulの後に一方向依存し、Container Insights、helper Lambda、CloudWatch Logs event capture、customer managed KMS keyを追加しない。
2. operator emailはdefaultなし、形式検証、`NoEcho`のCloudFormation parameterとし、実addressをGit/Obsidian/template/outputへ保存しない。単一TLS SNS topicへemail subscriptionを作るが、確認と到達試験はdeploy後受入へ分離する。
3. 9 underlying alarmはM-of-Nとmissing dataを明示し、actionを無効化した。critical/warningの2 composite alarmだけがSNS actionを持つ。task 0でhealth通知せず、active runtime中のBot/heartbeat標本停止はcriticalとなる。
4. EventBridgeは対象cluster/service、STOPPED、`TaskFailedToStart`/`EssentialContainerExited`/`SpotInterruption`/`TerminationNotice`だけを対象にし、計画停止codeを除外した。targetはcontent-freeな限定fieldだけを送る。
5. dashboardはalarm status、runtime state/desired/running/pending、Ingress/Outbox、BotReady/heartbeat、Reconciler/status failure、ECS CPU/Memory、主要6 DynamoDB APIのthrottle合計を8時間表示する。
6. TypeScript strict、30 CDK tests、cdk-nag、credentialなし`cdk synth --strict`は合格した。digest固定DynamoDB Local helper経由の全Python suiteは1426 passed、coverage 90.39%だった。Ruff format/lint、ty、import-linter、18文書のmirror/public-surface検証も合格し、`npm audit:infra`は期限付き2例外内で合格した。AWS/Discordへのwriteは0件である。

残課題: STEP-09C-CでProject 20 USD/account 30 USD Budgetと既存AWS managed service monitorを参照する10 USD Cost Anomaly Detection subscriptionを追加する。実SNS確認、通知到達、旧手動通知撤去はdeploy後に行い、local合格と同義にしない。

## 51. STEP-09C-C Cost governance CDK

- 実施日: 2026-07-29
- branch: `agent/step-09c-c-cost-governance`
- base: PR `#93` merge後の`main`
- scope: `us-east-1`独立`CostGovernanceStack`、Project 20 USD/account 30 USD Budget、actual 80/100%・forecasted 100% email通知、既存AWS managed service monitor参照、10 USD CAD subscription、CDK assertion/cdk-nag/strict synth、設計・CI/CD・運用・試験文書更新。
- non-scope: AWS bootstrap/deploy/diff、Budget/CAD作成・変更、email到達、実monitor ARN取得、cost allocation tag有効化、旧手動10 USD通知撤去、Discord Application変更。

確定事項と検証結果:

1. AWS公式endpoint表でBudgetsとCost Explorerに東京endpointがないため、account-globalなcost governanceだけを`us-east-1`へ分離した。workloadのStateful/Runtime/Operationsは`ap-northeast-1`を維持する。
2. Project Budgetはdeprecatedな`CostFilters`/`CostTypes`ではなく`FilterExpression`/`NET_UNBLENDED_COST`を使い、`user:Project=shittim-chest`を対象にする。Project tagがBillingでActiveになるまでdeployしない。Account Budgetはfilterなしでaccount全体を対象にする。
3. 2 Budgetはそれぞれactual 80%、actual 100%、forecasted 100%を同じdeploy時emailへ送り、Budget Actionや自動停止を作成しない。
4. AWS managed service monitorのquota 1を守るため`AWS::CE::AnomalyMonitor`は0件とし、defaultなしの`ExistingServiceAnomalyMonitorArn` parameterを参照する。CADはDAILY email、absolute total impact 10 USD以上とする。
5. `OperatorNotificationEmail`はOperationsと同じlogical input contract、defaultなし、`NoEcho`とする。実email、AWS account ID、monitor ARN実値をGit/Obsidian/template outputへ保存しない。
6. TypeScript strict、38 CDK tests、cdk-nag、credentialなし4 stack `cdk synth --strict`は合格した。digest固定DynamoDB Local helper経由の全Python suiteは1426 passed、coverage 90.39%だった。Ruff format/lint、ty、import-linter、18文書のmirror/public-surface検証も合格し、`npm audit:infra`は期限付き2例外内で合格した。AWS/Discordへのwriteは0件である。

残課題: deploy前に`ap-northeast-1`と`us-east-1`のbootstrap、Project cost allocation tag Active、既存AWS managed service monitor ARN、両stackへ同じoperator emailを確認する。deploy後にBudget/CAD email到達を確認してから旧手動10 USD Budget/CAD subscriptionを撤去する。これらをlocal合格と同義にしない。

## 52. STEP-10-A Release image admission

- 実施日: 2026-07-29〜2026-07-30
- branch: `codex/step-10-a-release-image-admission`
- base: STEP-09C-C merge後の`main`
- Draft Pull Request: `#105`
- head commit: PR `#105`で追跡
- scope: immutable GitHub OIDC release identity、plan/Environment-deploy workflow、平常/break-glass ARM64 imageの独立build・scan・署名・attestation、Notation strict verification、ECR referrer、canonical release manifest、fenced CloudFormation change set、ECS `PRE_SCALE_UP` image admission、週次/manual drift検出、local/CDK/AWS/GitHub検証。
- non-scope: Runtime/Operations/CostGovernance deploy、image push、初回Production Release、Discord Application/Interactions Endpoint変更、実Bot/OpenAI API通信、通知email到達。

確定事項と検証結果:

1. plan roleはimmutable main subject、deploy roleはreviewerとmain限定の`production` Environment subject、drift roleはread-only subjectへ分離した。実際に発行されるGitHub OIDC claimをworkflow内でrepository ID、owner ID、ref、Environmentまで再検証し、長期AWS credentialを保存しない。
2. 平常imageとbreak-glass imageを別々に1回だけbuild/pushし、digest、AWS Signer profile、Notation strict signature、ECR enhanced scan、SLSA provenance、SPDX SBOM、vulnerability assessmentの3 GitHub OCI attestation/referrerをplanとdeployの両方で照合する。release manifest、template、change set、Lambda bundleはhashとcommitへ固定する。
3. ECS lifecycle hookは対象service revisionの平常image digestだけを受け、Signer status、Notation signature referrer、3 GitHub predicate referrerをAWS APIからfail closedで確認する。不一致、timeout、provider errorは`FAILED`とし、ECS rollbackへ接続する。暗号学的Notation verificationはrelease workflowを正とし、hookはserver-side防御層とする。
4. deploy前にDynamoDBへfenced deployment lockを取得し、attested change setだけをStateful、Runtime、Operations、CostGovernanceの順で実行する。release evidenceは90日保持し、週次/manual driftはread-onlyで4 workload stackとReleaseIdentityを確認し、差分時だけ単一GitHub Issueへ集約する。
5. local validationはRuff、ty、import-linter、full pytest 1,381 passed / 91 skipped、coverage 90.30%、DynamoDB Local 91 passed、TypeScript strict、Vitest 44 passed、cdk-nag、credentialなしstrict synth、npm/pip audit、CycloneDX 1.5 SBOM、sdist/wheel、actionlint、notification/public-surface/docs gateが合格した。PR headのrequired CI、Grype、CodeQL、GitGuardianも全件合格した。
6. live preflight後、東京とVirginiaへCDKToolkit v32をchange set確認付きでbootstrapし、termination protectionを有効化した。Statefulは追加だけのchange setを実行し、DynamoDB ACTIVE/PAY_PER_REQUEST/PITR 35日/削除保護、immutable ECR、enhanced continuous scan、active Signer profile、ECR Managed Signingを確認した。
7. account既存のGitHub OIDC providerをimportして重複作成を回避し、ReleaseIdentityの3 roleをdeployした。GitHub repositoryはimmutable OIDC subjectを有効化し、`production` Environmentのrequired reviewer、exact `main` policy、administrator bypass無効を確認した。非secret release/drift variableと`infrastructure-drift` labelを登録した。
8. live CloudFormation検証でECR enhanced scanning filterが`WILDCARD`、Managed Signing filterが`WILDCARD_MATCH`である差を検出し、templateと回帰試験を修正した。IAM Access Analyzerで無効action名を検出し、ECR referrer取得を`ecr:BatchGetImage`、DynamoDB transactionを実APIのcomponent actionへ修正した。最終identity policy findingは0件、Stateful/ReleaseIdentityのlive CDK diffは0件だった。trust policyにはexact Environment subjectでも出る汎用branch推奨とaudience claim型確認だけを意図したinformational findingとして残した。
9. external impactとしてCDK bootstrap asset、DynamoDB、ECR/Inspector、Signer、IAM role、GitHub OIDC/Environment/repository設定を作成・変更した。さらにprivate notification secret、versioned SSM Standard SecureString 11件を設定し、Billingの`Project` user-defined cost allocation tagを`Active`へ更新した。AWS account ID、ARN、operator email、private Discord/OpenAI値は取得せず公開sourceへ保存していない。Runtime/Operations/CostGovernance、container image、Discord Applicationは変更していない。
10. operator手順を1 commandへ集約する`tools/configure_production_inputs.py`を追加した。GitHub release role ARNとactive AWS identityのaccountを非表示で照合し、GitHub secret名とSSM metadataだけで不足を判定する。秘密値は`getpass`、GitHub CLI標準入力、boto3 request bodyだけで扱う。11件のStandard SecureStringを既存値の取得・復号・上書きなしで作成し、runtime/persona schema、version、Discord snowflake/Public Key/token重複、email、UTF-8/4 KiB境界をwrite前に検証する。さらにlocal-only pointerから保存済み`PersonaConfig v0002`を読み、4 slotのtable、3 participant本文、display name、path、schemaを値非表示で検証して再利用するよう修正した。実operator sourceで4件を検証済みで、persona再入力は不要になった。追加unit 15 tests、Ruff、tyは合格した。operator実行後のmetadata-only `--check`でGitHub通知email設定済み、SSM 11/11を確認し、値は取得していない。
11. 初回releaseのplan実行では本番deploy前のfail-closed gateが段階的に不足を検出した。ECR scan evidence、Signer installer component versionを修正後、Notation 1.3.2が安全なcredential storeなしの`notation login`を拒否したため、AWS公式推奨のAmazon ECR Docker Credential Helper 0.12.0へ変更した。ARM64 binaryを公式SHA-256で固定し、対象private ECR hostnameだけを`credHelpers`へ割り当て、`AWS_ECR_DISABLE_CACHE=true`でhelperのauthorization tokenをdisk cacheしない。Notation独自のpassword取得、pipe、保存は廃止した。一方、GitHub `actions/attest`のOCI registry pushは通常のDocker credentialを要求することをlive planで確認したため、build/attestation用とdeploy再検証用の2 contextだけでOIDC roleから得た短命ECR passwordを`docker login --password-stdin`へ直接渡し、ephemeral runner外へ残さない責務分離とした。Betterleaksのgeneric API key誤検知にはrelease workflow pathと当該公開SHA-256値の完全一致だけをallowlistし、provider validationは無効のままとした。この記録時点では全attemptがplanで停止しており、Runtime、Operations、CostGovernanceへのdeployは0件である。
12. 2026-07-31に全Production Release attemptを時系列で再監査した。失敗は、存在しないAction pin・未追跡file、Signer/Cost Explorer/ECR/Inspectorの実API契約誤認、不足IAM、ゼロfinding response、installer component version、Notation credential store、OCI client認証、CloudFormation response field、artifact展開path、未実行の`jq` command、UUIDv7 guard、failed-job rerun artifact、CDK file asset未publishに分類できた。共通原因はproduction runを統合試験として使い、実CLI response、artifact layout、rerun、全CDK asset closureを事前に同じ形で検証しなかったことである。狭い文字列testを一件ずつ追加する運用を終了し、境界ごとの実行可能helperとfake provider回帰へ置き換えた。
13. 最後のattemptでは`restrictDefaultSecurityGroup=true`が生成したcustom-resource provider ZIPだけがbootstrap S3へpublishされず、Runtimeが`NoSuchKey`でrollbackした。workflowは4 stackの`.assets.json`からDocker asset 0件と全file assetを列挙し、4 templateとRuntime provider ZIPの計5件を`cdk publish-assets --force`でCloudFormationより前にpublishする。各root keyは64桁content hashの`.json`/`.zip`だけを短命plan roleから直接書き込み、`head-object --checksum-mode ENABLED`のS3 checksum、local source、assembly manifest、canonical release manifestを相互に固定する。標準bootstrap file-publishing roleの広いobject delete権限はrelease planへ委譲しない。
14. Managed Signing、ECR scan、Inspector coverage待機を独立helperへ分離した。期待profileの`FAILED`/不一致、ECRのterminal status、Inspectorのterminal/重複status、AccessDenied/Validationはcontent-free errorで即時停止し、明示したthrottling/service transientと`ScanNotFound`だけを有限retryする。change set cleanupも独立helperへ統合し、作成・削除中を待ち、未実行setを削除後`ChangeSetNotFound`まで確認する。partial createの一時的な未検出は3回連続で確認し、API認可失敗を「存在しない」と誤分類しない。plan開始時、plan失敗、deploy finally、独立cleanup jobの4経路が同じ実装を使う。force-cancel後の次runは4固定stackの全pageから正規名・同一account/Region・non-nestedの未実行setだけをdescribe後に回収し、再列挙でゼロを確認する。cleanup artifact取得失敗は`outcome`で検出し、plan成功時は旧`plan_attempt`のexact nameを使う。部分deploy済みの`EXECUTE_COMPLETE`をskipして後続未実行setを削除する回帰に加え、manifest中の先行stackが`EXECUTE_IN_PROGRESS`/`EXECUTE_FAILED`でも後続未実行setをすべて回収してから非0終了する対称fake-AWS回帰を追加した。attempt modeは実行中・実行失敗を即時拒否する。failed-jobs-only rerunは旧attempt artifactをdeployせず、全job rerunを明示要求する。
15. live stateはStateful/ReleaseIdentityが安定、deployment lockはopen、Runtimeが`ROLLBACK_COMPLETE`、Operations/CostGovernanceがresource 0件の`REVIEW_IN_PROGRESS`である。Runtime rollbackで旧templateの`Retain`により空のLogGroup 7件だけが残ったため、次回release前にstackと空LogGroupを確認付きで削除する。新templateは全7件を`RetainExceptOnCreate`/update replacement retainとし、初回create rollbackでは孤児化せず、通常delete/updateでは90日保持する。ECRの多数のtag/referrerは反復したplanによるもので、lifecycle policyのtagged/untagged各5世代保持を変更せず、成功release後に収束を確認する。
16. hardeningのlocal実行形検証では、全Python suite 1,507 passed / 91 skipped、coverage 90.43%、DynamoDB Local込みsuite 1,598 passed・coverage 90.51%、release helper・asset・workflow focused 206 passed、Vitest 45 passed、TypeScript strict、cdk-nag、actionlint、Ruff、ty、import-linterが合格した。実account向けstrict synthでも4 templateと1 provider ZIPだけが完全列挙され、4 workload stackのasset destinationに`assumeRoleArn`がないこと、ReleaseIdentityだけはoperator更新用の標準synthesizerを維持すること、Runtimeの7 LogGroupが`RetainExceptOnCreate`であることを確認した。live read-only確認では既存12 change setがnon-nestedの`CREATE_COMPLETE/AVAILABLE`で、sweeperが想定するresponse fieldを返した。live IAM先行更新、失敗stack/空log/change set cleanup、初回release、Discord/SNS/Cost受入は未実施であり、local合格と同義にしない。
17. PR `#120` merge後、ReleaseIdentityをoperator change setで先行更新し、2 IAM policyをin-place変更、termination protection維持、targeted live IAM assertion、CDK diff 0、drift `IN_SYNC`を確認した。未実行change set 12件、`ROLLBACK_COMPLETE` Runtime、stack削除後も0 bytes/0 streamのexact LogGroup 7件を確認付きで削除した。Production Release run `30570534245`は、required gate、OIDC/private handle/account、cost tag、全5 CDK file asset公開/checksum、両image build/push、Signer/scan、6 attestation、4 referrer、Lambda bundleまで成功したが、Statefulの`DescribeChangeSet.Parameters`がAWS CLIの公式例どおりJSON `null`だったため、array限定validatorがdeploy前に停止した。failure cleanupは4 stackの未実行setを全削除し、Runtime/Operations/CostGovernanceはresource 0の`REVIEW_IN_PROGRESS`、deployment lockはopen、workload deployは0件である。修正はfield欠落/`null`だけを空collectionへ正規化し、期待parameterがあるstackでは引き続き不足として拒否し、object/stringを拒否する回帰を追加した。修正後はtargeted 34 tests、全Python suite 1,511 passed / 91 skipped・coverage 90.30%、DynamoDB Local込み1,602 passed・coverage 90.39%、Ruff format/check、ty、import-linter、public/docs/tool-version gate、source SBOM 97 components、pip-audit 0 known vulnerabilities、sdist/wheel buildが合格した。
18. PR `#121` merge後のProduction Release run `30572603632`は新しいmain SHA・attempt 1でplanを完走し、Environment承認後もmanifest/attestation、両image identity、AWS evidence、immutable change setを再検証してdeployment fenceを取得した。Statefulは`UPDATE_COMPLETE`、RuntimeはECS Service作成時に`HookDetails should be of type JSON object as string`でtask起動前にrollbackした。CloudFormation公式契約どおりescape hatchの`HookDetails`を`JSON.stringify({schemaVersion:1})`へ変更し、synthesized templateのstring型を回帰に固定した。failure diagnosticsの`DescribeEvents`は実行後operationがstack resourceで認可される経路をdeploy roleが欠いていたため、4固定stack ARNとrelease change-set ARNの両方へ最小許可する。cleanup helperは再実行不能な`EXECUTE_FAILED`をactive executionと誤分類して二次失敗したため、`EXECUTE_COMPLETE`/`EXECUTE_FAILED`/`OBSOLETE`を消費済みとしてskipし、`EXECUTE_IN_PROGRESS`だけを停止対象にした。runはexact fence解放とlock evidence保存に成功し、4 stackのchange set inventoryは0件だった。Runtimeの47 resourceは全て`DELETE_COMPLETE`で、失敗stack metadataも確認付きで削除した。Operations/CostGovernanceはresource 0のplaceholder、Stateful/ReleaseIdentityは安定状態を維持している。修正後はcleanup focused 44 tests、全Python suite 1,512 passed / 91 skipped・coverage 90.30%、DynamoDB Local込み1,603 passed・coverage 90.39%、Vitest 45 passed、TypeScript strict、strict synth、Ruff、ty、import-linter、shellcheck、docs/public/workflow policy、source SBOM 97 components、pip/npm audit、sdist/wheel buildが合格した。PR CIのGrype DB更新で固定中DHIの`libexpat1`/`libexpat1-dev` 2.7.1にCVE-2026-25210とCVE-2026-41080が新規検出された。DHIの現行runtime/dev ARM64 layerが両package 2.8.2を含むことを直接確認して両index digestを更新したが、Grype 0.116.0はCVE-2025-59375/CVE-2026-25210/CVE-2026-41080/CVE-2026-45186を旧trixie package recordへ誤対応し、DHI VEXにもstatementがなかった。Debian公式trackerのfixed version、実SBOM、runtimeにXML parser呼出しがないことを根拠に、4 CVE×2 packageの8件を実build image digest限定・`under_investigation`・2026-08-29期限で受容する。image digest変更、fixable化、DHI VEX追加、XML parsing導入で自動的にstale/再評価となり、恒久除外や自称`not_affected`にはしない。
19. PR `#122` merge後のProduction Release run `30590810196`はplan冒頭で最新main `grype` checkがfailureのため約8秒でfail closedし、deployはskipされた。PRで受容へ固定したDocker exporterのmanifest digestと、同じsourceをmerge後に再buildしたdigestが一致せず、過去の同一commitでもCI `load`とrelease registry pushのmanifest digestが異なることを実artifactで確認した。さらにreleaseは同じ単一digest policyを異なるproduction/break-glass imageへ順番に適用するため、期限付きacceptanceが存在する限り必ず片方を拒否する潜在不具合だった。Docker公式の再現可能build契約に従い3 CI buildと2 release buildへ`SOURCE_DATE_EPOCH=0`を固定し、exporter固有manifestではなく最終config digestをtarget別identityとするpolicy schema v2へ移行した。CIはproduction/break-glassを双方build・SBOM・署名検証済みruntime/dev VEX・raw/actionable Grypeで検査し、releaseはpush済みmanifestから両config digestを再取得してcanonical manifest schema v3へ固定する。診断run `30591763167`はcontainer job成功・artifact保存後に意図的にcancelし、exact ARM64 evidenceで両targetの残存集合が同じ4 CVE×2 packageの8件、追加未承認High/Critical 0件であることを確認した。cleanup二次失敗は未作成Runtimeへattempt-nameで`describe-change-set`したためであり、exact `ValidationError`かつ`Stack/stack`と`does not exist`を含む場合だけ既にcleanとして扱う。汎用`does not exist`、`ChangeSetNotFound`、AccessDeniedは引き続き区別してfail closedとし、name/attempt/manifestの3回帰を追加した。PR `#123`の初回CI run `30592306819`では両targetのfixable High/Criticalが0である一方、診断runとのconfig digest差でtarget別validatorが停止した。`SOURCE_DATE_EPOCH`はBuildKitのconfig/historyには伝播していたが、`UV_COMPILE_BYTECODE=1`を実行するbuilder環境へ露出しておらずPythonがtimestamp型`.pyc`を生成し、image内file metadataにもexporterの明示rewriteがなかった。Dockerfileでglobal/stage `ARG`とbuilder `ENV`を固定し、CIのDocker exporter 3件とreleaseのregistry exporter 2件を`rewrite-timestamp=true`へ変更した。PRは検証中の失敗runを増やさないため一時closeし、同一sourceを別runで2回buildして両targetのconfig digest一致を実証するまで再開しない。

20. 修正後の閉じたPR branchで診断CI run `30593010968`を実行し、quality/tests/security/cdk/container/package/docsを合格、production/break-glass双方のbuild・native container gate・SBOM・署名検証済みVEX・Grype artifact保存まで完了してから意図的にcancelした。exact artifactではsource/production/break-glassのfixable High/Criticalはすべて0件、VEX後のproduction/break-glass未修正集合は既知の4 CVE×2 packageの8組と完全一致した。両targetの新しいconfig digestへ8件の期限付きacceptanceを更新し、exact full report/VEX/config-digestを入力したfail-closed validatorが両方合格した。次はこのpolicy更新後の同一headを閉じたPRと再開後PRで別々にbuildし、両targetのconfig digest完全一致と全required checks成功を確認する。
21. policy更新後のbranch workflow_dispatch run `30593356510`はGrypeを含む全8 jobが成功したが、同じheadのPR run `30593610376`はproduction/break-glass両config digest不一致だけでfail closedした。branch runはbuilderの全uv layerがcache hit、PR synthetic merge refは同じDockerfile/lock/source treeでもcold buildだった。両production SBOMの5,790 fileをpathとSHA-256で突合すると5,789件が一致し、差は`shittim_chest-0.1.0.dist-info/RECORD` 1件だけだった。sourceから`SOURCE_DATE_EPOCH=0`でwheelを独立2回buildしたwheel bytesとwheel内RECORDは完全一致し、Python 3.14.6でsite-packages 4,055 bytecodeをrandom hash seedの別processで再compileしても全件一致したため、base、dependency、`.pyc`、wheel生成ではなくarm64 uv installがcold/cache reuseで出力するapplication RECORD行順へ原因を限定した。PyPA仕様はRECORDを各installed fileの三列CSV、LF/CRLFとして定義し、row orderをidentityにしていない。final sync後に全RECORDをCSV parseし、非空path、三列、path重複なしをfail closed検証してrow全体sort/LF正規化するstdlib-only helperを追加する。PRはこの修正のcold/cache再実証まで再度一時closeした。
22. RECORD正規化追加後のbranch run `30594595997`はDockerfileが追加helperを`COPY`する一方、deny-allの`.dockerignore`が`tools/`をbuild contextから除外したため、production build開始時の`COPY ... not found`で停止した。事前policy checkがDockerfileだけを検査してbuild contextを検査しなかったことを再発防止上の欠落とし、`tools/`配下では正規化helper一件だけを再包含するordered rule、rule欠落・順序逆転のnegative test、実scratch container buildを追加した。修正後の全Python suiteは1,537 passed / 91 skipped、coverage 90.43%、Ruff、ty、import-linter、container policyが合格した。閉じたPR branchのrun `30594907996`ではproduction/fault-test/break-glass build、native fault gate、SBOM、署名検証済みVEXまで成功した。Grypeはsource/production/break-glassのfixable High/Critical 0件を確認し、両targetのVEX後残存集合が従来と同じ4 CVE×2 packageの8組であることをexact artifactで再確認した後、新しいtarget別config digestへ期限付きacceptanceだけを更新した。exact full report/VEX/config-digestを入力したfail-closed validatorはproduction `vendor_vex=15 / acceptances=8`、break-glass `vendor_vex=33 / acceptances=8`で合格した。PR再開前に、この同一headのbranch Grype成功とPR merge-ref cold buildのconfig digest完全一致を確認する。
23. acceptance更新後のbranch run `30595402149`はGrypeを含む全8 jobが成功したが、同じheadのPR merge-ref run `30595618423`は両config digest不一致だけで停止した。production SBOMの5,790 fileは再び5,789件一致し、差はapplication RECORDだけだった。行sort後も差が残るため、同一headのbranch/PR診断run `30596195075` / `30596189779`でproduction imageからRECORDをcontent-safe artifactとして取得し、必要成果物保存後に両runを意図的にcancelした。CSV比較では全行・順序・sizeが一致し、唯一`shittim_chest-0.1.0.dist-info/uv_cache.json`行のhashだけが異なった。uv内部metadata本体はcheckout directory時刻を保持し、cache-hit/cold installで内容が変わるため、`SOURCE_DATE_EPOCH`、file mtime rewrite、row sortでは正規化できない。runtimeに不要な同一dist-infoの`uv_cache.json`本体とRECORD行だけをfinal sync後に除去し、別path、symlink、非regular fileを拒否する。CIはimageからRECORDを再取得して当該行0を検証・30日保持する。実取得したbranch/PR RECORDへ新処理を適用した結果はbyte完全一致し、focused 33 tests、Ruff、ty、container policy、actionlintが合格した。次は変更後のbranch/PR config digest一致と全required checks成功を確認する。

残課題: config-digest risk gateと不存在stack cleanup修正をPR/mergeし、merge後mainのproduction/break-glass config digest再現と`grype`成功を確認してから、新しいmain SHAでreleaseを一度だけ実行する。release後にECS admission、workload drift、SNS/Budget/CAD email、Discord PING/署名/Status、ECS `0→1→0`と3/15/30分の実時間境界を受入し、今回成功したrollback/fence解放およびlocal/CI合格と区別して追記する。
