---
aliases:
  - The Shittim Chest 実装検証記録
tags: [project, shittim-chest, implementation, test-evidence]
status: production-1.0
created: 2026-07-16
updated: 2026-09-05
---

# 実装・試験・検証記録

## 1. 本書の役割

現在の到達点、代表的な受入、未実施operator drillを要約する。exact command、全check、PR review、
run attempt、artifact digest、incident logの完全な履歴はGitHub Pull Request／Actionsを正とし、
本書へ逐次複製しない。

## 2. 1.0 baseline

- baseline main SHA: `83c5c1889ba4172e7553ccb21f544cc6ea969ce9`
- participant roster／agentic Web searchを含むProduction Release: run `31765425667`
- RuntimeConfig pointer: `v0004`
- DynamoDB shared schema: current v8、previous v7
- container risk policy: schema v6、active local acceptanceは`security/container-risk-acceptance.json`を正とする
- container risk review (2026-09-04): unfixable High `CVE-2026-85091`を`zlib1g`と
  `zlib1g-dev`の2件について、production config digest限定、`under_investigation`、
  2026-10-04期限でactive local acceptanceへ登録した。exact digestと判定証拠は本書へ
  複製せず、該当policy fileとPR／CI evidenceを正とする。

このSHA以降の変更ではGitHubのcurrent mainとlatest successful Releaseを正とする。上記値を
将来runのconfig digestやartifact identityとして流用しない。

## 3. Accepted capabilities

| Capability | CI／contract | Production acceptance |
|---|---|---|
| signed HTTP受付とStatus | pass | 3秒程度の受付、誤ったtimeout表示なし |
| ECS 0→1→0 | pass | durable wake、通常討論、idle停止 |
| 初回意見／最終案 | pass | 3 Bot、固定順、重複なし |
| anonymous vote | pass | 3票確定後に実名／理由公開、Python winner |
| winner presentation | pass | moderator集計後、winner personaが歓喜と最終案を表示 |
| checkpoint／Outbox | pass | interruption／retry／content conflictの収束をoffline検証 |
| farewell | pass | 通常channelへparticipant 1件、参考link、停止を阻害しない |
| participant roster | pass | 互いのdisplay name／価値観を認識しpersonaを維持 |
| agentic Evidence | pass | 検索不要のskipと現在情報のshared searchを各1件確認 |
| supply chain | pass | production imageのSBOM／VEX／Grype／risk／sign／attestation |
| monitoring／cost／drift | pass | core 5 stack＋Records 3 stack、通知／budget／weekly drift運用 |

## 4. Important resolved incidents

| Incident | Root cause | Enduring control |
|---|---|---|
| 初回Runtime rollback | `DescribeTaskDefinition`をresource scopeした | 対象Actionだけ`Resource: "*"`、application exact validation |
| release image不一致 | pytest bytecodeがpath contextへ混入した | bytecode禁止、fresh SHA checkout、actual context test |
| referrer検証失敗 | 同digestの過去attestationも数えた | attestation前後のACTIVE referrer差分だけを検証 |
| Discord初回応答失敗 | cold invocationが3秒期限を超えた | HTTP ingress／SnapStart／短いinitial response |
| Statusの取り違え | debateではなく共有状態へ遅延更新した | Debate／Attempt単位のfenceと収束 |
| terminal delivery停滞 | process lossとDiscord POST結果が曖昧だった | GenerationCheckpoint、Outbox v2、history reconciliation |
| Outbox送信確定停滞 | Discord POST成功直後の`SENT` transactionがlease heartbeatと競合し、既知message IDを保持した即時再試行がなかった | 同一message ID／確定時刻によるbounded completion retry、SDK例外境界、再POSTなしの回帰試験 |
| farewell欠落 | shutdown直前処理と厳密なsource照合 | 停止5分前、citation 1件、限定retry、best-effort |
| static digest loop | cross-run完全一致をrequired化した | run内evidenceを検証し、静的baselineを廃止 |

## 5. Pending operator drills

次は未実施でも1.0 software acceptanceを否定しない。productionへの影響とrollbackを個別承認して
実施し、完了時だけ本節を更新する。

- 4 Bot token rotation
- DynamoDB PITR restore to a separate table

## 6. Records implementation state

- repository／contract／Records CI境界とDebateTable Stream／専用OIDC roleは実装済み。
- Archive／Statistics／Sessionのstateful resource、Projector／Backfill Lambda、deterministic bundle、
  Records専用Release／Backfill workflowはM2のsoftwareとして実装済みである。
- 初回Records ReleaseのLogGroup名競合によるApplication Stack rollbackは、各Lambdaの明示LogGroup
  bindとrollback shell削除後の新規Releaseで解消した。RecordsStatefulは`CREATE_COMPLETE`、
  RecordsApplicationは`UPDATE_COMPLETE`で、Projector／Backfill Lambdaは`Active / Successful`である。
- M2の本番dry-runとapplyは完了し、52候補すべてを検証して52件をArchiveへ投影済み、
  apply checkpointは`complete=true`である。
- 受入時点でProjector DLQは0、unexecuted Records Change Setは0である。
- ProjectorはCOMPLETED partitionをstrongly consistentに再取得し、既存Python winner規則を再利用する。
- dry-runはArchive writeを0件に保ってapplyと同じpure projectionを検証し、Statistics Tableの
  dry-run専用checkpointへ内部cursorと累積件数だけを保存する。apply checkpointとは分離する。
- OAuth／Session／Read API／requester avatar cacheはM3としてProduction Releaseまで完了した。
  API Gatewayは6 route、Auth／Read Lambdaは分離された`live` aliasを使い、Records Stackはstableである。
- M3 ReleaseはAuth入力を含む5 SecureStringのmetadataだけを確認し、bundle checksumをChange Set、
  manifest、Lambda Versionで照合した。自動smokeではanonymous Session 200と未認証Records 401を確認し、
  unexecuted Records Change Set 0で完了した。
- M4 Production Releaseとcustom domain公開は完了し、browser OAuth loginから認証済み一覧まで到達した。
- M4のReact／Vite+ SPA、LINE Seed JP self-host、一覧／詳細、名前付き投票、保存済み最終決定、
  login transition、responsive／reduced-motion UIとRecordsEdge Stackはsoftwareとして実装済みである。
  desktop／mobile Playwright、axe、visual snapshot、CDK／cdk-nag、deterministic Web artifactで検証し、
  Records専用Releaseは3 Change Set、Web ZIP、Web SBOM、検証済み公開hostnameをattested manifest
  schema v4へ結合する。公開hostnameはMemorial upload bucketの固定CORS originである
  `https://shittim.pitekusu.dev`のhostnameと完全一致する場合だけmanifestへ保存できる。Core Releaseは同一main SHAの
  最新completed Records Release runがsuccessであることを確認し、exact run／attemptのimmutable artifactを固定する。
  attestation、manifestのcommit SHA、固定CORS hostnameとの一致を検証後にhostnameを引き継ぐ。新しい
  failure／cancelledから過去のsuccessへfallbackしない。
- M4初回受入で検出した期間未指定一覧Query、presigned avatar URL、mobile detail、
  投票graphの問題は限定修正とRecords Release後のbrowser確認で解消し、M4受入を完了した。
- M5-Aは15分ごとの全Archive再集計、勝利／依頼snapshotの原子更新、認証済み
  Rankings API、合計、勝利表彰stage、依頼割合ring、数値と比較barを併記するresponsiveな
  `/insights`画面をsoftwareとして追加した。時系列dataがないため推移graphは表示せず、
  同順位時は表彰stageの高さを揃えて表示する。
  Production Releaseとbrowser受入まで完了し、初回snapshot、3人の勝利ranking、依頼上位10人、
  current avatar、responsive表示を確認した。
- M5-BはFargate／Lambda／OpenAI／その他AWSのUSD日次台帳、Frankfurter v2の日次USD／JPY rate、
  日本時間の期間選択、JPY 4内訳とstacked barを持つsoftwareを実装した。OpenAI費用入力の登録、
  Production Release、初回180日台帳の作成まで完了し、productionでcollectorが稼働している。
- 2026-08-29のlive read-only調査では、Runtime reconcilerのEMFがLambda JSON logの`message`へ
  文字列として内包され、Outbox metricが抽出されていないことを確認した。EMFをroot JSON eventとして
  直接出力する修正と、root形状を固定するunit testを追加した。
- 同日の概算費用0円は、12:17 JST時点でFrankfurterの当日rateが未公開だった一方、Collectorが
  7日windowの完全性を要求して失敗したことが直接原因だった。進行中当日の末尾欠落だけを許容し、
  直前日rateによる当日限定の暫定換算を`partial`で返す修正を追加した。確定済み過去日の欠落は
  引き続き失敗させる。あわせてHTTP clientのrequest URL INFO logを抑止し、project IDをlogへ残さない。
- Projector用identity HMAC key、presentation v0001、OAuth config、Discord client secret、Session keyは
  登録済みで、Releaseでは値を取得しない。
- runtime prompt互換層はsoftwareとCore Production Releaseを完了し、active pointer未登録時のlegacy fallbackを
  維持している。ADMINの初期HMAC認可、Status API、全員向けWeb導線、当初の403画面はProduction Releaseと
  browser受入まで完了している。prompt本文、raw identifier、SSM値は本書、PR、artifact、logへ保存しない。
- ADMIN statusのsoftware対象は従来8 serviceにAPI Gateway、EventBridge／Scheduler、CloudFormation、SNSを
  加え、第2段階としてSSM設定準備、Budgets／Cost Anomaly、Signer、外部費用collector鮮度まで拡張した。
  16 sectionは日本語labelのcard／tableとして表示し、raw parameter名やprivate resource identifierを公開しない。
  Production Releaseとlive AWS responseの受入を完了している。
- ADMIN第3段階はこの変更で、Config Lambda、5 prompt API、immutable SSM revision、content-free audit、
  idempotency／競合回復、editor／履歴／rollback UIをsoftware実装した。RecordsStateful、Core image、lockfileは
  変更しない。Production Releaseと初回active revision作成は未実施であり、後者はRelease後の独立した管理操作とする。
- 2026-08-30のaccess policy変更では、service status取得・明示更新とprompt現行値・履歴・revision参照を
  全認証済み利用者へ開放した。prompt反映・rollbackだけはserver-sideのexact管理者再判定を維持し、非管理者の
  Webは本文・履歴比較を閲覧専用で表示してwrite操作を出さない。匿名401、非管理者prompt write 403、Origin／CSRF／
  idempotency境界をAPI、component、browserで確認した。Production Releaseは未実施である。
- prompt revision保持契約は使用中を含む最新5世代へ限定した。使用中行は操作不能な`使用中`表示、過去行は
  現行との差を`-`／`+`で示す。active切替とaudit完了後だけ6世代目以降のSSM本文、manifest、summaryを順に削除し、
  active保護、途中失敗からのidempotent回復、exact SSM delete権限をdomain／adapter／CDK／Web testで固定する。
- 親愛度はsource schema v8、人格別の質問評価、全員成功時だけの原子的なprofile更新、Archive v2、Records
  projection、3人格別の全質問者ranking、議論詳細／Discord／Web表示を実装した。Records Production Releaseを
  先行し、Core Production ReleaseでFargate imageとsource schema v8を配信済みである。Core配信前の既存議論は
  遡及評価せず、既存質問者だけを500点で条件付きseedする。配信後の最初の自然な質問によるDiscord表示、議論詳細、
  ranking、次回答の態度反映はlive受入待ちである。
- 親愛度Coreの最初のProduction Releaseは、productionの固定control 11件が均一なv7である一方、candidate
  toolingがstrict v8 validationを先に要求したため、Environment承認後かつstack write前に停止した。既存stack、
  v7 control、open deployment lockは維持された。forward fixではuniform v7／v8だけをpreflightで受理し、v7→v8と
  lock取得を同一transactionに統合した。candidate Runtime未適用時は11件をv7へ原子的に復元し、適用状態が曖昧なら
  lockを保持する。lock取得直後の11件はcontent-free SHA-256をimmutable auditへ固定し、response-loss replayと
  rollback直前の一致を必須とする。単体、workflow policy、DynamoDB Localで移行／維持／復元／fail closedを固定した。
  forward fix後のProduction Releaseでは固定control 11件をv8へ移行し、candidate Runtimeの有効化、rollbackなし、
  open lock、全4 stackのstable状態、unexecuted Release Change Set 0を確認して完了した。
- メモリアルロビーPR1はsource schema v9／previous v8、Records identity HMACと同じopaque requester keyへの
  profile移行、reset回数／cycle、1 cycle 1人の解放選出とRecords互換契約を配信済みである。v8 profileは
  本人の次回成功評価時だけ原子的に移行し、導入前から1,000点の場合も未解放のまま固定しない。
- PR2は5つのowner-only API、専用API／Worker Lambda、temporary upload、generation queue／DLQ、checkpoint／履歴、
  atomic reset、画像／文章生成とprivate保存をRecords Releaseで配信済みである。Release中にgeneration、upload、
  reset、active prompt切替のlive writeは行わず、専用keyはmetadataだけを確認した。
- PR3は`/memorial`のlazy route、sidebar／mobile導線、未解放表示、約3秒の全画面演出、
  owner-only upload／生成進捗／履歴／reset UI、ranking王冠、既存の親愛度独立投稿内の解放通知とWeb URLを
  software実装した。Runtimeはstrictな`RecordsPublicHostname`からtaskのcanonical URLを構成し、RecordsEdgeは
  exact Memorial upload originだけをCSPへ追加する。PR2のbackend／storage契約、新規AWS resource、IAM、APIは
  変更していない。期限切れupload予約の再選択、local試行の破棄、reset後のarchive-only再入室もWebで固定した。
  focused Python／Web／CDK／workflowとvisual screenshot、required CI、reviewを確認し、main統合とRecords Releaseを
  完了した。初回Core ReleaseはGitHubが公開hostnameのjob outputをsecret候補として抑止したため、CloudFormation実行前に
  fail closedした。全Change Set削除と未変更のStack／ECSを確認し、同一SHAの署名済みRecords manifestをCore evidenceへ
  同梱してdeploy側で再検証・直接参照する方式へ変更した。最終Release状態はGitHub evidenceを正とする。

exact test数、PR、run、artifact、Change SetはGitHub evidenceを正とし、本書へ固定しない。

## 7. Evidence retention

- source／test result: Git commit、PR、required check、CodeQL
- image／SBOM／VEX／risk: CI／Release artifactとattestation
- deploy: Production Release summary、Change Set、CloudFormation event
- live behavior: content-free timestamp、terminal state、operator observation。opaque Debate IDが必要な
  場合はprivate operator evidenceへ限定し、public docs／PRへ複製しない。
- incidents: minimal direct-cause log、cleanup、lock／Change Set／stack safe state

question、output、persona、token、private identifierを証拠へ複製しない。

## 8. 個人開発向けの検証規模整理（2026-09-05）

- 共通infraのaudit／type check／Records 3 stackの試験はCI `cdk`へ集約し、二重実行を撤廃した。
- 自動coverage取得と90% gateを撤廃し、必要な調査時だけ取得する。通常PR／main pushではfault imageを
  buildせず、追加の強制停止・復旧訓練は手動実行とした。production imageの基本検証は維持する。
- 装飾中心のE2E 4件、装飾の単体試験1件、専用snapshot 4枚と細かなCSS値検査を削除した。
  長いroute animation検査は短いnavigation／focus試験へ置き換え、通常操作・認証・整合性の試験は維持する。
- focused Python、Web check／build／component／browser、infra試験、Records synthで確認した。
  本変更によるlive操作やProduction Releaseは実施していない。
- Core、運用tool、Records、Web、infraを再棚卸しし、大型fileの重複fixture・型注釈・mutation準備を整理した。
  workflowの不正変更caseは維持し、生成schemaや本番inventoryの逐語的な照合と装飾だけのassertを減らした。
- schema側の参加者重複拒否は実際のWeb validator経由で確認する。Inspectorの複数tag・翻訳待ち表示も
  小さなfixtureで維持し、DynamoDBやメモリアルの異なる失敗・復旧caseは削除していない。
- Runtime／RecordsApplicationの読込専用infra試験はsuite単位でtemplateを生成する。再整理した箇所の
  focused Python／Web試験、lint／型検査、infra試験とworkflow policy検査で確認した。
- 再点検では正常討論の重複完走、OpenAI検索response、メモリアル生成worker、Inspector responseと
  Web upload操作の重複準備を整理した。DynamoDB統合試験は実transactionを通す準備だけを集約し、
  競合、Outbox、旧record移行のcaseを維持した。装飾の細部は代表snapshotへ任せ、主要font・操作・
  accessibilityの確認を残す。新しいfile分割・汎用framework・skip・CI gate緩和は追加しない。
- 追加整理のfocused Python／Web、DynamoDB Local、lint／型検査、変更したbrowser操作は成功した。
  当時のbrowser snapshot差分は別の依存更新工程で解消した。baselineの更新や許容差の緩和を
  この試験整理には含めない。
- さらにCore、Records、Web、infraの試験関連171 fileとCI実行範囲を棚卸しし、大きなfile、
  長いtest本体、重複準備、componentとE2Eの責務重複を確認した。API fixture、画像の重複描画、
  Discord channel payload、CDKの期待するfunction一覧とpolicy型を整理する。
  表示名だけのworkflow mutationと空振りassertを削り、JSON roundtripは既存正常caseへ統合する。
  ErrorResponseの自己確認は実際のunknown field拒否へ置き換え、winner不一致はwinnerの試験へ集約する。
- 大きなDynamoDB試験は複数段階のtransaction、CAS、再試行、二重反映防止を確認しているため維持する。
  CI gate、production code、IAM、lockfile、新しいfile分割や汎用試験frameworkは変更しない。
- 最終追加整理ではCore 394、Records 69、Web 43、対象E2E 2、infra 76 caseが成功した。
  関係するlint／型検査と文書・公開情報・差分検査も成功。前回からの未commit整理を含め、
  試験18 fileで763行を純減した。

## 9. 固定ツールの更新漏れ対策（2026-09-05）

- Buildx／BuildKit本体はDependabotのAction参照更新とは別で、旧週次checkerにも登録されていなかった。
  archive形式の6 pinに加え、Buildx、BuildKit、Cosign、ECR Credential Helper、Notation、AWS Signer plugin、
  Python、Node、uv、pnpmを実ファイルから読んで比較する。AWS installerのmutable配布物もchecksumで監視する。
- Buildx 0.37.0、BuildKit 0.33.0、Cosign 3.1.3、Betterleaks 1.8.1、Docker Scout 1.24.0、
  Syft 1.51.1、Grype 0.118.0、Node 24.20.0、uv 0.11.33、pnpm 11.25.0を更新候補として実装した。
  BuildKit registry indexのdigestとARM64提供、downloadしたCLI archiveのchecksumを確認し、
  CosignによるBetterleaks checksum bundleの署名検証とscanner正常／検出contractを確認した。
- Python 3.14.7のstable tagは存在するが、uv 0.11.33のmanaged Python downloadで取得できなかったため、
  CI／開発用Pythonは3.14.6を維持する。DockerfileのPython 3.14.7は既存のDHI imageであり変更しない。
  uv 0.12以降やVite+の系列変更は既存の互換性制約に従って別検討とし、既存のtsx更新PRも同梱しない。
- npm／pnpm／uvの直接依存も照合し、CDK、boto3／stubs、Pydantic、型定義・開発toolに更新候補を確認した。
  これらは既存のDependabot対象として別PRで扱い、OpenAI SDK 3系やVitest 5系へのmajor更新は
  互換性確認を伴う工程に分離する。今回のtool更新でapplicationのlockfileは変更しない。
- focused Python／workflow policy、Actionlint、型／lint、uv lock／SBOM、Node更新後のinfra型検査、
  Web frozen install／check／test／buildを確認した。Issue新規作成・更新・無変更・解決・取得失敗・重複拒否は
  GitHub writeなしでoffline確認した。実networkの監視結果はPythonの保留候補だけを検出した。
- この工程ではcommit、PR、merge、workflowの手動起動、AWS／Discord write、Production Releaseを行わない。
  ARM64 image／SBOM／VEX／risk gateの最終確認は通常PRのcanonical CIで行う。

## 10. uv 0.12互換性移行（2026-09-05）

- PythonのDependabot更新は、実行側uv 0.12.7とCore／Recordsの`required-version <0.12`が
  不一致となり、依存解決前に`tool_version_not_supported`で失敗していた。
- workflowとDockerのuvを0.12.10へ揃え、両projectのuv／build backend許可範囲を
  `>=0.12.7,<0.13`へ変更する。container policy、Dependabotの次系列ignore、週次監視も揃える。
  Docker indexのdigestとLinux ARM64対応、公式CLI archiveのchecksumを確認した。
- uv 0.12の変更点はbuild backend設定に互換性があり、既存workflowは明示的にprojectを選択するか
  `uv run python`を使用しているため、script起点のproject探索変更による実行先変更はない。
- uv 0.12.7／0.12.10の両方で既存Core／Records lockを読み込めた。applicationのlockfileと
  dependency versionは変更しない。Python 3.14.7は新uvでmanaged downloadできたため、
  前節の保留を解除し、CI／開発用pinも3.14.7へ更新する。
- Records Releaseを先に、同一SHAのCore Production Releaseを後に実行する。
  実際のARM64 image／SBOM／VEX／risk、必須CI／CodeQL、Dependabot復旧と本番配信結果は
  各PR・Actions runを実行記録とする。tsx等の別dependency更新は同梱しない。
- PRの初回CIはuv更新後の実測image config digestが既存risk acceptanceの対象外となり停止した。
  VEX適用後のHigh／Criticalは従来と同じ`CVE-2026-85091`の`zlib1g`／`zlib1g-dev`だけで、
  package version、DHI runtime、アプリの公開範囲は変更していない。オーナーの続行指示を受け、
  対象CVE・承認条件・2026-10-04の期限を変えず、新しい実測digestだけを追加する。
