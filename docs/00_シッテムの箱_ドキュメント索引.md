---
aliases:
  - シッテムの箱
  - The Shittim Chest
  - Discord マルチエージェント討論Bot
  - シッテムの箱(Discord BOT)
tags: [project, discord, bot, openai, aws, shittim-chest]
status: detailed-design-complete
created: 2026-07-16
updated: 2026-07-17
---

# シッテムの箱 ドキュメント索引

- 日本語名: シッテムの箱
- 正式英名: **The Shittim Chest**
- canonical ID: `shittim-chest`
- repository: [pitekusu/shittim-chest](https://github.com/pitekusu/shittim-chest)
- repository visibility: Public

## Project overview

Discord上で3つのprivate-configured personaが初回意見、相互参照後の最終案、匿名投票を行い、moderatorが進行、集計、最終決定、記録を担当する単一Guild向けBotを構築する。

詳細設計は2026-07-16時点で完了している。Public repositoryとmanaged GitHub security/CIは構成済みである。STEP-01〜05C.1Aはmerge済みである。STEP-05CはPR `#22`、merge commit `a6f43cb`で決定的な品質signal、Luna standard/Terra standard/Luna pro Policy、shadow-only評価、DynamoDB schema v4、明示的opt-inのblind A/B評価toolを実装した。STEP-05C.1AはPR `#24`、merge commit `1360411`で盲検artifact分離、failure分類、rubric validation、content-free集計を実装した。STEP-05C.1Bでは有料実APIで10件20回答を生成し、preference-only盲検評価でLuna pro 4勝、Terra standard 2勝、同点4件となった。Luna proは候補であり、本番自動昇格、threshold、token/deadline上限は未決定・無効である。Responses API Multi-agent betaは使用しない。Discord adapter、runtime recovery wiring、container、CDK、AWS resource、Discord Applicationは未実装である。

## Implementation status

| Slice | State | Evidence |
|---|---|---|
| Python domain foundation | 実装済み | `pyproject.toml`、`uv.lock`、`src/shittim_chest/domain/`、`tests/unit/domain/` |
| GitHub CI | 実装済み | PR `#10`、main commit `e2fdaad`。5 strict required check、CodeQL、managed SBOM照合が合格 |
| Secret scanner migration | STEP-02B・STEP-02C完了 | PR `#13`、main commit `d0f601a`。Betterleaks単独full-history scan、生成fixture contract、Sigstore検証、週次version監視が合格 |
| Application core | STEP-03完了 | PR `#15`、merge commit `34ccc54`。投票domain、Protocol、accept/run/cancel/retry/resume、deadline/cancellation、142 tests、全CI/CodeQL合格 |
| Persistence contract | STEP-04A完了 | PR `#16`、merge commit `54948d7`。Guild/channel/operation ID保持、3-slot fenced lease型、idempotent repository契約、縦分割item、schema v1→v2、outbox/panel record。AWS接続なし |
| DynamoDB adapter | STEP-04B完了 | PR `#18`、merge commit `9aafe6e`。boto3 1.43.50、受付/retry/terminal transaction、20秒lease heartbeat、outbox、GSI pagination、DynamoDB Local 3.3.0、SDK stub、163 tests/92.40%、PR/main CI・CodeQL・managed Dependency Graph合格 |
| OpenAI Structured Outputs adapter | STEP-05A完了 | PR `#20`、merge commit `d6ea561`。stable Responses API、strict schema、private persona、content-free usage記録 |
| Web Evidence | STEP-05B完了 | PR `#21`、merge commit `44a35fa`。`question-router-v2`、hosted `web_search`、source/citation抽出、DynamoDB schema v3 |
| Quality escalation foundation | STEP-05C完了 | PR `#22`、merge commit `a6f43cb`。`escalation-shadow-v1`、3 signal、Terra standard/Luna pro Policy、schema v4、opt-in blind A/B tool。自動昇格は無効 |
| Blind evaluation workflow | STEP-05C.1A完了 | PR `#24`、merge commit `1360411`。scorer/key/性能指標の分離、失敗分類、rubric検証、content-free集計。有料API評価は未実行 |
| Blind live evaluation | STEP-05C.1B集計完了・Policy未決定 | 10件20回答成功。preference-onlyはLuna pro 4勝、Terra standard 2勝、同点4件。推定費用合計`$1.198697` |
| Discord/runtime | 未実装 | [[19_実装計画・トレーサビリティ#3. Implementation sequence]] |
| AWS/Discord production | 未実装 | 明示的なproduction実装・deploy承認が必要 |

各sliceの実行command、試験結果、coverage、security/package検証、未実施範囲は[[20_実装・試験・検証記録]]を正とする。

## Document map

### Requirements・decision

- [[01_要求仕様書・基本設計書]] — system scope、functional/non-functional requirement、basic architecture
- [[02_議論事項・意思決定記録]] — 検討経緯、決定済み事項、廃止案、deploy時入力

### Detailed design

- [[10_アプリケーション・Python詳細設計]] — package、domain、state machine、Protocol、async、shutdown
- [[11_Discord詳細設計]] — 4 Application、権限、Intent、command、thread、panel、rate limit
- [[12_OpenAI・プロンプト詳細設計]] — Responses、Structured Outputs、persona、evidence、投票、cost
- [[13_DynamoDB・データ整合性詳細設計]] — table/GSI、transaction、fenced lease、outbox、migration
- [[14_AWS・CDK詳細設計]] — VPC、ECS/ECR、IAM、Parameter Store、stack、IPv4、Spot
- [[15_GitHub・CI-CD詳細設計]] — CI、OIDC、production deploy、SBOM、rollback、drift
- [[16_セキュリティ・プライバシー詳細設計]] — threat、secret、prompt injection、data、audit
- [[17_運用保守・監視・障害対応設計]] — metric、alarm、dashboard、maintenance、Runbook
- [[18_試験・品質保証設計]] — test layer、正常/異常、fault injection、formal E2E、acceptance
- [[19_実装計画・トレーサビリティ]] — requirement mapping、implementation sequence、DoD

### Implementation evidence

- [[20_実装・試験・検証記録]] — sliceごとの実装範囲、実行command、結果、artifact、未実施範囲

## Document responsibility・conflict handling

- [[01_要求仕様書・基本設計書]]は「何を満たすか」という要求と外部制約を定義する。
- [[02_議論事項・意思決定記録]]は選択理由、受容risk、廃止案を記録する。
- 各詳細設計書は対応領域の実装可能なinterface、data、failure処理を定義する。
- [[18_試験・品質保証設計]]は合否判定、[[19_実装計画・トレーサビリティ]]は対応関係と実装順序を定義する。

単純な文書優先順位は設けない。矛盾を発見した場合は実装を停止し、ADRへ影響と判断を記録して、要求、意思決定、詳細設計、試験、traceabilityを同じ変更で更新する。

## Source of truth・GitHub mirror

本folderの14文書を公開可能な正本とする。GitHub repositoryの`docs/`はreview・履歴・CI検証用の一方向mirrorであり、直接編集しない。同期sourceは`--source`または`SHITTIM_DOCS_SOURCE`で明示する。本番固有値とpersona内容は本folder外の非公開operator sourceとversion付きSSM parameterへ分離する。

## Confirmed decisions

| ID | Decision | State |
|---|---|---|
| D-001 | private runtime configで指定する単一Guild、非空channel allowlist、`/shittim` | 決定済み・未実装 |
| D-002 | 4 Applicationを1 Python processで実行し、4 READY時だけ受付 | 決定済み・未実装 |
| D-003 | stable OpenAI Responses、Structured Outputs、共通Evidence、3人格投票。Responses API Multi-agent betaは非採用 | 決定済み・STEP-05A一部実装 |
| D-004 | DynamoDB transaction、3 slot fenced lease、outbox、PITR | 決定済み・STEP-04A/B一部実装 |
| D-005 | ARM64 Fargate Spot singleton、Public IPv4、stop-before-start | 決定済み・未実装 |
| D-006 | SIGTERM checkpoint、`stopTimeout=120`、restart時の自動再開 | 決定済み・未実装 |
| D-007 | CDK TypeScript、GitHub OIDC、manual production deploy | 決定済み・未実装 |
| D-008 | 作業turnごとに最新公式一次資料を確認・記録 | 決定済み |
| D-009 | Public GitHub Free、main Ruleset、Production Environment、単一attested releaseを採用 | 決定済み・一部設定済み |
| D-010 | 平常taskはread-only・ECS Exec無効、承認時だけbreak-glass版へ切替 | 決定済み・未実装 |
| D-011 | 業務dataはTTLなし、復旧保証はDynamoDB PITRの35日まで | 決定済み・未実装 |
| D-012 | Web searchをMVPで使用し、OpenAI既定の最大30日abuse monitoring保持を受容・開示 | 決定済み・STEP-05B実装済み |
| D-013 | FAILED attemptはimmutable terminalとし、同一debate/thread内に`retry_of`で結ぶ新attemptを作る | 決定済み・domain実装済み |
| D-014 | 各実装sliceの試験・検証結果を公開可能なObsidian成果物へ追記しGitHubへmirrorする | 決定済み・運用開始 |
| D-015 | Betterleaksの署名・回帰・更新監視を並行期間で確認後、Gitleaksを撤去して単独gateへ移行する | 決定済み・STEP-02C実装済み |
| D-016 | 投票後の決定的signalをshadow記録し、Terra standardとLuna proをblind評価後に自動昇格Policyを決める | 決定済み・STEP-05C基盤実装済み |

詳細な決定と検討履歴は[[02_議論事項・意思決定記録]]を参照する。

## Deploy-time inputs

- 1件以上のchannel ID allowlist
- AWS account IDとalarm/budget通知先
- 4 Discord Application IDとSecureStringのtoken
- version付きRuntimeConfigと4 PersonaConfigのParameter名
- OpenAI project/API key/model availability

実値は公開mirrorへ、token/API keyはObsidianへ記録しない。詳細は[[19_実装計画・トレーサビリティ#4. Deploy-time required inputs]]を参照する。

## Official-source policy

詳細設計、実装、review、障害対応の各作業turnで最新の公式一次資料へ再アクセスし、URL、確認日、対象version/service、設計への反映を記録する。料金、model、Fargate Spot、package version、Discord API制約、DNS recordは実装日とdeploy日の双方で再確認する。確認不能なら推測で確定しない。

各詳細設計書の「公式資料確認記録」は2026-07-16に確認済み。全体の実装開始条件は[[19_実装計画・トレーサビリティ]]を正とする。
