[project]
name = "shittim-records"
version = "0.1.0"
description = "Read-model services for Shittim Chest Records"
requires-python = ">=3.14,<3.15"
license = "MIT"
dependencies = [
    "boto3>=1.43.66,<2",
    "httpx>=0.28.1,<0.29",
    "httpx2>=2.12.0,<3",
    "openai>=3.8.0,<4",
    "pillow>=12.1.1,<13",
    "pydantic>=2.13.4,<3",
    "shittim-chest",
]

[dependency-groups]
dev = [
    "boto3-stubs[ce,dynamodb,ecr,inspector2,s3,sqs,ssm]>=1.43.66,<2",
    "pip-audit>=2.10.1,<3",
    "pytest>=9.1.1,<10",
    "ruff>=0.16.2,<0.17",
    "ty>=0.0.69,<0.1",
]

[build-system]
requires = ["uv_build>=0.12.7,<0.13"]
build-backend = "uv_build"

[tool.uv]
required-version = ">=0.12.7,<0.13"

[tool.uv.sources]
shittim-chest = { path = "../..", editable = false }

[tool.ruff]
target-version = "py314"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC", "RUF", "S"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]

[tool.ty.src]
include = ["src", "tests"]

[tool.pytest]
minversion = "9.1"
testpaths = ["tests"]
addopts = ["--strict-config", "--strict-markers", "--import-mode=importlib"]
